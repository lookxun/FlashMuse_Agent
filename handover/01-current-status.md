# Current Status

Last checked: 2026-07-22 China time.

## 2026-07-22 部署 07-20+07-21 全部上线 + 工作流删除确认弹窗 + 图层右键拦截 + @引用视频首帧/音频倒计时 + 导入选中蓝框层级 — 见 CHANGELOG 顶条为权威。速记：

**✅ 全部已部署腾讯 + 同步阿里、四域名 200；`tsc`+`build` 通过；无 Prisma 迁移；已 push GitHub（三方同步 `ac4c38f`）。`wavesurfer.js` 随镜像 build 自动装入（Dockerfile 内 npm install，无需宿主单独装）。**

1. **部署**：把积压的 07-20（资产库改造 + wavesurfer.js）+ 07-21（@引用迷你资产库 + 从资产库导入音视频 + 视频卡@图标）**两批一次性上线**。提交序列 `0a577e3`→`2e0672b`→`936dbbe`→`ac4c38f`。
2. **工作流空生成节点删除确认弹窗**：空的图片/视频生成节点（无结果）输入框有提示词时删除弹通用确认框，为空直接删。判定 `workflowNodeNeedsDeleteConfirm`，覆盖三删除入口（键盘 Del/右键菜单/runtime.deleteNode）。弹窗 `z-[10002]`（高于节点输入框浮层 z-[9999]），确定按钮黑色 `bg-[#111] px-12`。
3. **图层面板右键不出菜单**：`WorkflowLayerPanel` 根加 `onContextMenu` 拦截。
4. **@引用视频首帧封面**：`asset-mention-picker.tsx` 视频无 poster（上传视频）时用 `<video #t=0.1>` 首帧，不再只显图标。
5. **@引用音频卡时间**：`AudioWaveformPlayer` 加 `secondsCountdown` prop，@引用弹窗音频卡显示"倒计时秒/总秒数"两位数（15/15→00/15）、移右上 `right-[3px] top-[3px]`；其它地方（资产库/导入弹窗音频卡）不变。
6. **导入弹窗选中蓝框/勾层级**：渐变黑=z-10、名字=z-20、蓝框/勾移 DOM 末尾+z-50，不再被渐变挡。
7. **影响评估（用户点名）**：核查 07-20/07-21 改动**不影响生成主链路、扣积分**（图片存盘重排队扣费仍幂等/只 finalize 扣一次；服务端只改资产库过滤展示；前端没动生成提交/扣费）。
8. **部署窗口 ChunkLoadError 非 bug**：旧标签跨部署窗口现象，硬刷即可（容器=阿里 chunk 一致、页面 200）。

## 2026-07-21 从资产库导入补音视频 + @引用资产弹窗大改造 + 视频卡@媒体图标 — 见 CHANGELOG 顶条为权威。速记：

**⚠️ 全部仅本地：`npx tsc --noEmit` 过；未 build / 未部署 / 未 commit；无 Prisma 迁移；无新增依赖。但 07-20 那批（资产库改造 + `wavesurfer.js`）也仍未部署——用户要求下一个 AI 把 07-20+07-21 全部一次性部署上线。**

1. **「从资产库导入」弹窗**：加 上传视频/上传音频两分类；视频显小缩略图+中间播放键（去"视频"字样）、无封面 `<video>` 首帧；音频波形卡；导入落地支持建音频节点（`restoreWorkflowAssetToCanvas` audio 分支 + kind 类型三处加 audio）。音频波形卡时间移左上（避开勾选框）。
2. **⭐「@引用资产」弹窗三处统一大改造**（对话流输入框 / 资产库生成弹窗 / 工作流输入框）：新增共享组件 `src/components/asset-mention-picker.tsx`（左标签+右 5 列 80×80 小缩略图，标题@引用资产，固定高 378）。显示资产库全部分类（对话流/工作流 10 类；资产库生成弹窗隐藏视频/音频=6 类图片）。**视频/音频可引用=方案A**：复用 + 号上传的 `uploadRule` 判断（enabled/maxCount/时长/尺寸/总时长，从 url 读元数据校验），支持就建 ready 参考进杠（不重新上传）、不支持提示。右侧图片/视频=小缩略图（视频无封面用图标占位不加载 video）、音频=波形。**按标签懒加载**：首次只加载当前标签 30 个+全部计数，切标签转圈、下拉流式加载（`loadMentionFilterPage`+`mentionFilterPaging`；工作流经新 props `onLoadReferenceFilter`/`referenceFilterLoading`/`referenceFilterNextOffset`）。
3. **视频卡提示词里 @媒体小图标**（`ReferencedTextContent`）：视频显封面+播放键、音频统一 `RiVoiceprintLine` 深灰 `#8a8a8a`，**纯展示不可点**。
4. 改动文件：新增 `asset-mention-picker.tsx`；改 `chat-workbench.tsx`、`workflow-tldraw-canvas-inner.tsx`、`workflow-tldraw-canvas.tsx`、`audio-waveform-player.tsx`、`handover/*`。


## 2026-07-20 资产库改造 + 音频波形播放器 + 图片存盘不丢改造 — 见 CHANGELOG 顶条为权威。速记：

**⚠️ 全部仅本地：`npx tsc --noEmit` 过；未 build / 未部署 / 未 commit；无 Prisma 迁移；新增 npm 依赖 `wavesurfer.js`（部署时服务器要 `npm install`）。** 本 session 起用户加两条铁律（见下）。

1. **两条铁律**（已写进 `AGENTS.md` 顶部 + `00-README.md` 顶条）：① 动代码前先评估对既有功能的影响、有影响先说清等确认再改；② 默认只做本地不部署，说部署才部署；git 攒够再一次性推。
2. **资产库改造（核心）**：侧栏"上传图片"移到横线下、新建 `上传的资产`(圆点)分组含 上传图片/上传视频/上传音频（对话流+工作流所有上传合并）；上传视频=video-row(一行4)+无 poster 用视频首帧封面、从"生成视频"移出；上传音频=波形播放器方卡(一行5，悬停播放、纯文件名无@)。服务端(`workspace-state` 过滤/计数)+前端(`isAssetInFilter`)同步改，`MediaAsset.mediaType` 透传到前端（含 `media-assets` GET）。`conversation_uploads` 分页收窄到与计数一致(39)。
3. **共享波形播放器** `src/components/audio-waveform-player.tsx`（wavesurfer.js，variant node/card），工作流音频节点改用它；音频节点 600×300；图层音频图标/文件名；对话流输入框音频缩略图图标统一 `RiVoiceprintLine`。
4. **图片存盘不丢改造** `runImageJob`：本地没存好就把交付快照存 `extraJson.pendingImageLocalize` 重排队等到存好再落库（跳过重新生成、扣费幂等），不再 60s 回退远程 url 导致"成功却不进库"。国内本地跨境慢必现、线上抖动偶现，现根治=更稳。
5. **手动补** 一张本地漏掉的分镜图（历史个案，见 CHANGELOG §4）。
6. **下一个 AI 待办（用户点名）**：改造 `@引用资产` 和 `从资产库导入`（目前只图+视频）让音频/视频也显示；交互（音频能否导入工作流音频节点）需和用户确认。



## 2026-07-19 生成链路服务端断线重连改造 + B_146/B_144 修复 + 后台失败原因聚合 + 运维 — 见 CHANGELOG 顶条为权威。速记：

**⚠️ 全部已部署腾讯 + 同步阿里、四域名 200；`tsc` 通过；无 Prisma 迁移；已 push GitHub。** 改动文件：新增 `src/lib/transient-error.ts`；改 `src/lib/openrouter.ts`、`src/lib/error-message.ts`、`src/lib/admin-overview.ts`、`src/lib/generation-jobs.ts`、`src/lib/byteplus-assets.ts`、`src/app/api/video/route.ts`（连同 07-18 遗留的 `chat-workbench.tsx` 一起已 push）。

1. **⭐ 生成链路服务端断线重连（重点）**：新增统一判定 `isTransientServerError`（网络/超时/5xx/Bad Gateway/平台临时[Failed to download media/Transaction API/asset not found]/限流=可恢复；真人/版权/参数/审核拒绝/未知=永久）。接线三处缺口：① 图片任务 `runImageJob` catch 可恢复错误→`scheduleJobRetry`（退避，attempts 封顶 6 才真失败）；② 视频创建 `createVideoTaskWithTransientRetry` 包创建调用重试 3 次；③ `createBytePlusAsset`(auto-review 建素材)重试 3 次。**"真人检测→服务器繁忙"真因=送审重试时 BytePlus 抓我们参考图 url 撞我们瞬时 502→抓图失败→兜底；不是真人逻辑坏。用户定调不补映射、服务端自愈。**
2. **B_146**：Seedream 5.0 Pro + 多参考图报错 → `openrouter.ts` disabled 分支加 `supportsSequentialBatch` gate，Pro 不再发它不支持的 `sequential_image_generation`（三模式统一）。
3. **B_144**：参考图宽高比越界(仅视频模型) → `error-message.ts` 映射成"参考图太窄或太长…换 16:9/9:16/1:1/4:3"。
4. **后台失败原因聚合**：`admin-overview.ts` SQL `regexp_replace` 剥 B_xx 前缀 + 归一"图片平台没有返回图片"族 → 同原因合并成一条、按数量降序、不显示 B_xx。
5. **运维**：腾讯宿主开 BBR(迁移漏了→跨境大文件传输崩溃致视频 aliSyncError 根因)；阿里补同步 92 视频(现为超集、走本地镜像不回源)；同步脚本修 `docker cp` 嵌套 bug（曾致工作台 ChunkLoadError）。


## 2026-07-18 视频三处线上问题根治 + 历史回填 — 见 CHANGELOG 顶条为权威。速记：

**⚠️ 全部已部署腾讯 + 同步阿里、四域名 200；`tsc` 通过；无 Prisma 迁移；代码未 push GitHub。** 改动文件仅 2 个：`src/app/api/video/route.ts`、`src/components/chat-workbench.tsx`。**下一个 AI：这 2 文件改动 + 本次 handover 需 commit+push GitHub 保持三方同步。**

1. **B_122 修复**：真人审核 `waitForBytePlusAssetActive` 在 CreateAsset 刚返回、GetAsset 尚未同步时第一次查到 "asset not found" 就整单毙 → 改成**只有平台 `Failed` 或超 180s 才失败**，瞬态 not found/抖动当"没就绪"继续轮询。此类失败历史 10 例全 ID_315163。
2. **B_135 修复（实测验证过）**：参考音频以原始 `audio_url` 直传被 BytePlus 版权检测拦（`InputAudioSensitiveContentDetected`）；图片走的"上传 Skip 免审素材→`asset://`"能绕过，但音频没走（`isBytePlusHumanReferenceError` 特意排除 copyright）。新增 `isBytePlusRecoverableReferenceError`：**输入素材(图/视频/音频)真人/隐私/敏感/版权都判可恢复→走 Skip 素材重试**，仅 output 侧不可恢复。实验证据：原始链接=复现失败、Skip 素材=成功出片（视频已交付用户桌面 aaa 文件夹）。**"别的平台能过、我们不能"真因=送法不对，非音频侵权。**
3. **对话流"@音频名删不掉"根治（存问题非读问题）**：`ensureMediaFileMentions` 视频提交时强制把媒体 @名拼到提示词最前面，污染了模型prompt/存档content/cleanPrompt/等待卡，删了发送又被重补。音频视频本靠附件数组 `referenceAudios/referenceVideos` 送达、与@名无关。**去掉强制补名**（删函数），copyPrompt 原样读。⚠️ 第一版误在 copyPrompt 剥"所有媒体@名"把用户手打的也删了→已撤回。
4. **历史回填**：4 用户 50 视频 job（ID_315163 28/ID_868181 20/ID_193006 1/ID_332396 1；含 .mp4 参考视频被强制补名）。只剥"开头带音视频扩展名的 @名"，回填 GenerationJob(50)/WorkspaceSession messages(49,5会话)/WorkspaceMessage(51)/MediaAsset.sourcePrompt(50)，复查全库剩 0。备份 `/opt/flashmuse/data/runtime/backfill-forcedmention-2026-07-16T05-44-23-593Z.json`。

## 2026-07-17 上传文件命名全平台统一 + 资产库右侧按入库时间稳定排序 — 见 CHANGELOG 顶条为权威。速记：

**本 session 改动已部署腾讯 + 同步阿里 + push GitHub（连同 07-16 输入框统一那批一起）；`tsc`+`build` 通过；无 Prisma 迁移。** 改动文件：新增 `src/lib/upload-name.ts`；改 `src/app/api/media-assets/route.ts`、`src/app/api/upload-file/route.ts`、`src/app/api/asset-upload-temp/route.ts`、`src/components/chat-workbench.tsx`、`src/components/workflow-tldraw-canvas-inner.tsx`。

1. **上传命名唯一权威** `src/lib/upload-name.ts`（`resolveUploadName`：contentHash 命中→复用旧权威名 currentName||systemName||initialName；否则去扩展名+sanitize+全局唯一 base/base_2，扫描含在途 GenerationJob.reservedNames）。三条上传接口都返回权威 `name`；生成媒体命名逻辑不动。
2. **前端三处只显示服务端返回名**：对话流资产库/输入框(图)、对话流 uploadedFiles(视频/音频/文档，新增 `getUploadedFileMetaName` 保住图标/下载后缀)、工作流 handleUploadNodeFile 四类 + 输入框（经 uploadFilesAsConnectedNodes）。
3. **资产库右侧排序根治**：`visibleAssets` 按 `createdAt`(=服务端 firstSeenAt) 稳定降序；移动分类 `onChangeType` 不再覆盖 `createdAt`。→ 最新入库永远最上、不跳、与刷新一致。
4. **边界（别当 bug）**：只修前向老数据不回填；同批并发不同内容同原名极端下预览名可能短暂重复（入库经 advisory 锁最终唯一）；已插入 prompt 的旧 @名不回改。

## 2026-07-16 输入框 @mention/上传显示 全平台统一 + 唯一引用名 + 上传判重文案根治（已随 07-17 一起部署+推送）— 见 CHANGELOG。速记：

1. **@mention 纯逻辑收敛唯一实现** `src/lib/mention-text.ts`（匹配/删除/移除/替换 + `MENTION_ACCENT=#367cee`），对话流输入框+对话流消息展示+工作流输入框+后台弹窗四处复用；前端 mention 蓝色统一成 `#367cee`。
2. **三输入框选中文本后点 @文件名 = 覆盖选中区**（新增 selection-range 读取，collapsed 回退存储光标）。
3. **资产库生成弹窗输入框全面对齐**：参考缩略图改独立状态（不再从文本派生）；删@文本留缩略图、删缩略图清所有@名；缩略图下@名可插入；"清空输入框"连缩略图一起清。
4. **对话流上传缩略图显示对齐工作流**：上排只放文档，下排 图片+视频+音频 混排 80×80、换行、X 在外角。
5. **切模式发送提示**：`当前模型不支持视频/音频/文件`（替代"最多支持0个文件"）。
6. **视频/音频/文档判重提示**（对话流+工作流，对齐图片）：`XX已存在，无需重复上传！`。
7. **全平台唯一引用名**：同名不同图错开 `名/名_2`，杜绝 @文件名 撞名无法区分（对话流 `addActiveUploadedImages` + 工作流 `WorkflowPromptBox`）。
8. **资产库"上传成功却不显示"根治**：真因=转码后内容哈希与库里一张老工作流图 url 撞车（老图 contentHash 空、判重 miss）。改 `/api/media-assets` POST 返回 `duplicate` + 命中回填 contentHash；`submitAssetUpload` await 结果、只加真新图、文案区分 新增/已存在。

**⚠️ 现状记录（别当新 bug 重查）**：去重是按"内容字节"（转码后内容寻址 url）+ contentHash；同一张图（含改文件名再传）只存一份；老数据 contentHash 多为空，靠 url 撞车兜底判重（本 session 已让 POST 命中时回填）。

## 2026-07-15 后台/工作流「参考素材·提示词·尺寸」统一读取根治 + 对话流视频双卡历史修复 — 见 CHANGELOG 顶条为权威。速记：

**本 session 全部已部署腾讯 + 同步阿里；代码已 push GitHub（连同 07-14 统一根治大 session 那批一起推）；无 Prisma 迁移。服务器 /tmp 临时文件已清；持久备份在 `app-backups/20260714-video-doublecard-fix/`。**

1. **对话流视频双卡**：复查 07-14 修复无误；全库 7 例历史双卡按用户要求手修（两失败卡→留一个；一成功一失败→留成功去失败），两张表都改。
2. **后台弹窗参考素材匹配统一健壮化**：从「仅精确 requestId」改为 requestId→裸id前缀→workflowNodeId→messageId+kind→conversationId 多口径；抽唯一 `buildJobReferenceItems`，后台+工作流接口共用。
3. **参考图破图根因=`asset://` 没解析**：`createImageJob/createVideoJob` 新增 `resolveReferenceUrls` 把客户端发的 `asset://<bytePlusAssetId>` 解析成真实 url 再存（前向）；回填 256 条老 job。
4. **视频没尺寸根因=从没量过视频宽高**：`media-save-queue` 用新 `getLocalVideoDimensions`（ffmpeg 解析，非封面！封面被降采样过）量真实宽高（前向）；回填 238 条老视频。
5. **sourcePrompt 混入 hint**：回填剥掉 190 条历史媒体的 `参考图顺序：...` hint。
6. **@不蓝=后缀不一致**：`AdminPromptWithMentions` 加去后缀容错匹配。
7. **导入资产「使用提示词」无缩略图**：新增 `getGenerationJobByMediaUrl`（媒体 url→原始 job），工作流接口按节点查不到就按 mediaUrl 回溯；对话流生成图回填 291 条 requestId（按 messageJson.images 位置取精确 `:image:序号`）。
8. **前向保证**（别再当 bug 查历史）：新生成天然带全 requestId/cleanPrompt/refs（asset:// 已解析）/尺寸；工作流「使用提示词」+ 后台弹窗都能带回参考缩略图/@蓝字/干净提示词/尺寸。合理边界：`@图片1` 位置占位不蓝；纯临时未入库引用可能无名不蓝；6 月老远程媒体文件已丢无法补。

## 2026-07-14 (统一根治大 session) — 见 CHANGELOG 顶条为权威。速记：

**本 session 全部已部署腾讯；代码未 push GitHub（用户测完一起推）；无 Prisma 迁移。**

1. **Agent/通用模型路由统一根治**：通用/Agent 用 Seedream 5.0 Pro/5.0 Lite/Seedance 2.0 Mini 报"网络连接异常(b76)"= `getBytePlusProviderKey` 复制三份跑偏 + agent-image/agent-video 前缀漏配端点 + 容器没装 curl + "spawn curl ENOENT"误映射成网络错误。已收敛为唯一 `src/lib/byteplus-provider-key.ts`、配置表对称补齐、Dockerfile 装 curl、修误映射。**铁律写进 `AGENTS.md` 顶部 + `04-product-rules.md`：能统一一律统一，禁止复制多份各走各的。**
2. **工作流"使用提示词"回填干净真实 prompt**（输入框+连线文本节点、不含 hint）：统一字段 `job.extraJson.cleanPrompt`；图片 job 补存它、接口返回它、`addNodeFromPrompt` 回填。
3. **后台媒体弹窗**：提示词上方显示参考图/视频/音频缩略图（点开原文件）+ @文件名蓝字；后端按 requestId 关联 GenerationJob 参考素材。
4. **资产库生图去内部强制规则**：只存用户输入（异步 `/api/image` 传 `sourcePrompt: rawPrompt`）；回填历史 34 条。
5. **视频本地存盘不限时根治**：`runVideoJob` 不再"等 60s 就用会过期的远程 url 落库"，改成没存好就重排队等到本地存好再 finalize（存盘队列一直重试到成功或远程 24h 过期）；finalize（图+视频）权威补写生成参数（防兜底抢先建空行锁死）。回填历史空参数媒体 99 条。
6. **⚠️ 现状记录（别当 bug 重查）**：线上 243 视频+378 图的 `MediaAsset.url` 仍是远程 volces 地址，**全是 2026-06-19~21（job 化之前）的老数据**，远程已过期、媒体已丢、无法恢复，非 bug；另有 90 条空参数生成媒体无权威来源、按"不猜"保留。7 月起及当前的视频/图都正常落地。`systemName` 会被不同批次重名占用，别只按名字认定同一个东西。

**备份目录**：`app-backups/20260714-unify-providerkey`、`-cleanprompt`、`-admin-refs`、`-asset-cleanprompt`、`-assetprompt-backfill`、`-video-localsave`、`-mediaparam-backfill`。

## 2026-07-14 (later session) 工作流"使用提示词"带图/@变蓝根治（改读后端 GenerationJob + 名字进库 + 画布去冗余）+ 等待卡计时平滑 + 回填 830 条历史 job（✅ 已部署腾讯；代码本 session 末尾一起 push GitHub）

详见 CHANGELOG 顶条。速记：
- **现象**：工作流 `video_5_w2`/`video_6_w2`（同提示词、连线连的 3 张参考图）右键"使用提示词"只带回文字、无图、@ 不蓝。
- **真根因**：上次部署新加的"每 8 秒兜底 reconcile" + 实时轮询 + resume **双重收尾同一视频**；先收尾者删了入边，后收尾者拍到空快照，老代码 `generationUploads: 空?好值:undefined` 用 `undefined` **把好快照覆盖清空**（updateNode 浅合并）。开着浏览器（路A）也复现，证明不是断线问题。
- **根治**（用户拍板，弃脆弱的画布内快照，改读后端权威 job）：
  1. `GenerationJob` 加 `referenceNames` 列（迁移 `20260714100000`，仅加可空列、毫秒级不锁表）；建任务时反查 MediaAsset 名字写库（改名||终身ID||systemName）。
  2. 新接口 `/api/workflow-generation-references`（按 workflowId+workflowNodeId 查最近成功 job 的参考素材+名字）。
  3. `addNodeFromPrompt` 改为点击时读该接口还原（带图/视频/音频 + @变蓝），查不到/老节点回退读旧 `generationUploads`。
  4. 去掉 3 个 finalize 点写 `generationUploads`（画布瘦身，缓解 canvasJson 膨胀，见 M019）。
  5. 工作流等待卡加每秒 tick，"已等待/渲染%"平滑走秒（不再停顿猛跳；对话流本就有此 timer）。
  6. 一次性回填 **830 条历史 job** 的 referenceNames（含 video_5/6_w2）→ 老视频 @ 也蓝。
- **对话流不涉及**：它"使用提示词"读的是消息里提交时就存的 `imageReferences`/`uploadedFiles`（`copyPrompt`），天然稳，无需改。工作流现在也达到同样"读权威持久数据"效果，只是存在 job 而非消息。
- **三方状态**：已部署腾讯；本 session 末尾把这批代码 + 上一 session 另一 AI 未推的 handover commit(`84582e5`) 一起 push GitHub。有 Prisma 迁移（腾讯 entrypoint 容器启动自动 migrate deploy）。备份 `/opt/flashmuse/app-backups/20260714-refnames/`。
- **新增备忘 M019**：工作流整张画布存单个 canvasJson 大字段的结构隐患（整块读写慢、整块覆盖竞态），以后重构。

## 2026-07-14 对话流视频/图片 3 个 bug 根治（✅ 代码已部署腾讯 + push GitHub；handover 文档本地 commit 暂不推）

用户报线上 Seedance 2.0 Mini（7-13 新视频模型）出错显示异常，排查发现是**对话流通用 bug**被新模型稳定触发（模型本身无 bug）。详见 CHANGELOG 顶条。三个代码 commit 全部 tsc+build 过、部署腾讯、push GitHub：
- **`2db526b`**：① 错误码红字误映射修复——BytePlus `Request id` 里的数字子串（如 `...78401...` 含 `401`）被 `error-message.ts` 的 `/401/` 等命中 → 剥掉 Request id 尾巴 + HTTP 码全加词边界 `\b`。② 对话流视频**两个失败卡**修复——前台轮询器 + 后台恢复 effect 都对同一 job 调无脑 `+1` 的 `markAssistantVideoFailure` → 恢复 effect 加守卫 `runningRequestIdsRef.has(requestId)` 跳过前台仍在跑的请求。
- **`e9ee160`**：对话流视频**等待卡关浏览器重登录后消失**修复——渲染依赖内存 `isActiveVideoPending`（需内存 pending request）→ 改按持久化 `videoPendingCount` 渲染（`isVideoPendingVisible`）；`needsLiveTimer` 加 `hasRecoveringMedia` 保持恢复中媒体的 1 秒计时器。
- **`04dafb0`**：对话流**图片同款双失败卡隐患**修复——图片恢复 effect 加同款 `runningRequestIdsRef` 守卫。
- **四路径核查**：双失败卡=对话流图/视频修、工作流无此问题（节点单 error 字段）；等待卡重登录消失=对话流视频修、对话流图片本来没事、工作流图/视频本来没事（按持久化 `node.data.isRunning` 渲染）。
- **押后**：M018 统一单轮询器重构（当前守卫是低风险即时修，双轮询器合一以后做）。
- ⚠️ **工作树有两个别的 AI 未完成改动**（`workflow-tldraw-canvas-inner.tsx`、`workspace-workflows.ts`）——本 session 未碰/未提交/未部署，下个 AI 勿误提交。
- **错误码回到 60 多的原因**：计数器 `.runtime/error-code-counter.txt` 不随迁移搬，腾讯新卷从 B_1 重来，非 bug。

## 2026-07-13 (deploy session) 部署两批新模型 + 修 Pro 扣费/工作流卡死/统一读取空名 + 回填 workflow_02 历史图名（✅ 全部已部署腾讯 + push GitHub，三方同步 `b94c3ea`）

详见 CHANGELOG 顶条。速记：
- **部署了前两批仅本地的模型改动**（commit `7c66f85`：开关5组+Terra + Seedream 5.0 Pro/Seedance 2.0 Mini + 校准计费/尺寸/多图 + 前端定时器卡顿）。无迁移。
- **排查工作流04超时**：本地跨境网络问题（图片 fetch failed B_197、视频下载271s），非 bug，生产无此问题。
- **对照官方文档核对 Seedance 2.0 全系**：Model ID/比例/时长/计费单价全对；唯一差异 Seedance 2.0 缺 4K（**用户决定先不接**，计费函数已有 4K 档但 UI 选不到）。
- **修 Pro 像素分档扣费 bug**：异步图扣费时拿不到实测尺寸→永远算高档 0.09；改为兜底用已知 `targetDimensions` 判档（1K→0.045、2K→0.09）。
- **修工作流节点成功/失败都不返回**：恢复只在挂载/可见/聚焦三时机触发、无周期兜底；加"只要有进行中节点就每 8s 重跑 reconcile"。
- **修统一读取的洞 + 回填历史数据**：工作流校准 effect 只纠正已有名、不补全空名（`if(!names)return false`）→ 改为从库按 url 补全缺失名；并在线上 DB 事务回填 workflow_02（**ID_636611**，nickname 12424740）三张 7-07 遗留空名图 → image_2/3/4_w2（库+canvas+nextImageNumber=5，备份在本地 temp）。
- 后台上传规则面板补 Pro/Mini 标签（纯文案，开关本就生效）。
- commit `b94c3ea` 部署（openrouter.ts / workflow-tldraw-canvas-inner.tsx / admin-upload-rules-panel.tsx），四域名 200。

## 2026-07-13 (later session) 新增 2 个 BytePlus 模型 + 全量按官网校准计费/尺寸/多图 + 修前端卡顿（⚠️ 全部仅本地，未 commit/未部署；**下次直接部署**）

详见 CHANGELOG 顶条 + 05-next-actions 顶条（部署步骤+验证清单）。速记：
- **新增模型**：Seedream 5.0 Pro（图片，id `byteplus:conversation-image.seedream-5-0-pro`，调用名 `dola-seedream-5-0-pro-260628`，端点 ep-…q5zvf，仅 1K/2K，只支持单图生成）；Seedance 2.0 Mini（视频，id `byteplus:video.seedance-2-0-mini`，调用名 `dreamina-seedance-2-0-mini-260615`，端点 ep-…mwp78）。都加进 图片/视频生成 组、默认开、Mini 显示在 Fast 上面。
- **计费按官网**：图片 Pro 输出按像素分档(≤236万px 0.045/>0.09)+参考图第2张起0.003；视频新增 `getBytePlusVideoPricePerMillionUsd(model,resolution,hasVideoInput)`——Seedance2.0 7.0/4.3·1080p 7.7/4.7·4K 4.0/2.4，Fast 5.6/3.3，Mini 3.5/2.1（有无视频输入两档，扣费点传 referenceVideos）。BytePlus 只返 token、我们 token×单价 自算。
- **多图行为按模型**：4.5/Lite 保留原生"一次出多张"(sequential)；Pro 单图 → 申请 N 次；OpenRouter 一直申请 N 次。产品语义="申请N次"独立候选，不是让模型一次吐N张。
- **尺寸按官网参考像素表**：4.5/Lite 2K/4K 原表正确；Pro 建独立表(2K 与它们不同、1K 专属)；Lite 补 3K（`ImageResolution` 加 "3K"）。发送仍用 `宽x高`。
- **修 3K 显示成 2K**：`getImageResolutionFromDimensions` 由"最长边"改"总像素"判档（三处）。
- **修前端卡顿**：`chat-workbench` 1 秒定时器原无条件常开→空闲每秒全量重渲染；改为 `needsLiveTimer` 按需开。线上也存在（被生产优化掩盖、无肉眼卡），随本批一起修上线。
- **三方状态**：本 session + 上一 session 两批都只在本地（tsc/build 过），未 commit/未推/未部署。**用户：最后一批，下个 AI 直接部署。**

## 2026-07-13 后台模型开关大简化 + GPT-5.6 Terra 新模型 + Agent 改造（⚠️ 全部仅本地，未 commit/未部署；白名单加号已上线腾讯）

详见 CHANGELOG 顶条。速记：
- **后台"模型开关" 7 组→5 组**：① 图片生成 ② 视频生成 ③ 通用模式 ④ Agent 模式 ⑤ 反推/优化提示词。表头"使用位置"→"功能模块"，新增"作用位置"列（黑字圆点，列出每组影响的功能位置）。
- **取消互斥、改相加**：图片/视频/通用三个模块去掉与 BytePlus 重复的 OpenRouter 模型（seedream-4.5 / seedance-2.0-fast / seedance-2.0 / seed-2.0-lite，只在这些模块可用性过滤里返回 false，ID 未从 models.ts 删），OpenRouter 只留独有模型；左右各自开关不互斥、BytePlus 去下拉。
- **反推/优化提示词**：additive，固定顺序 GPT-5.5→GPT-5.4→Seed 2.0 Pro→Seed 2.0 Lite（新增 key prompt.seed-2-0-pro）。
- **新增 GPT-5.6 Terra / Terra Pro**（`openai/gpt-5.6-terra`/`-pro`）到通用模式 OpenRouter；金色显示从 GPT-5.5 改为 GPT-5.6 Terra Pro。
- **Agent 模式**：合并成一组（小标题：规划对话/自动生图/自动生视频），普通=BytePlus Seed 2.0 Pro/Seedream 4.5/Seedance Fast、高级=Terra Pro/GPT-5.4 Image 2/Seedance 2.0；additive、无下拉、去掉备选段。**备选改成兜底共用**：首选不可用时随机用「图片生成/视频生成」里已开启模型（前端 fallbackModels + 后端 image/video 路由放行 conversation 模型）。
- **默认全部打开**：各 provider key 默认开 + `BYTEPLUS_API_KEY_ENABLED` 默认 false→**true**。
- **三方状态**：本批代码只在本地（tsc+build 过），**未 commit/未推/未部署**。用户要求下个 AI **继续加模型后再一起部署**。改动文件：system-settings.ts、models.ts、openrouter.ts、chat-workbench.tsx、admin-system-settings-panel.tsx、api/model-availability|image|video/route.ts。
- **白名单**：腾讯 `ADMIN_EMAILS` 加 `176107103@qq.com` 已上线（env 改动+重启容器，非代码）。

## 2026-07-12 (later session) 生成图统一出生根治 + 资产→节点读取统一(model) + 全平台上传内容哈希去重(阶段3b全量完成) + 三方同步（全部已部署腾讯 + **本次已 commit+push GitHub，三方同步**）

详见 CHANGELOG 顶条。速记（承接同日"资产入库/显示统一大改造"，把没覆盖到的口子补齐）：
- **原则**：所有生成一律由服务端 `finalize` 唯一权威出生（`buildMediaAssetRecord`）、参数齐全；所有"资产变节点/引用/上传"一律从统一口径读真实数据、不兜底默认；覆盖对话流/工作流/资产库全平台 + 未来新模式。
- **① 生成图统一出生根治（最关键）**：byteplus 等**异步存盘**的生成图，交付时 url 还是远程 → `runImageJob` 原来跳过不落库 → 行被客户端 PATCH 兜底分支凭空建出、**model/settings/requestId 全空**（资产库图看不到模型名的真根因）。修复=图片对齐视频：远程交付先 `enqueueRemoteAssetSave`+`waitForMediaSaveJob` 拿本地 url 再 finalize。
- **② 资产→节点读取统一 model**：`workspace-state`/`media-assets` GET 现返回原始 model id；从资产库导入/图层恢复/workflowAssets 都带真实 model（原来导入节点显示成默认 Seedream）。也修了"从资产库导入弹窗不刷新新生成图"。
- **③ 全平台上传内容哈希去重（阶段3b 全量完成）**：服务端 `dedup=1` 开关隔离；对话流+工作流(画布/输入框)+资产库三处全接上、跨平台判重；提示「图片已存在，无需重复上传！」——对话流/工作流输入框弹**输入框上方**、工作流画布弹**画布下方中间**、资产库用自身提示。去掉了资产库旧的按 url 判重。工作流输入框上传修了两个体验（不再选中上传节点导致输入框消失、不放大画面）。
- **④ 历史 model 回填**：仅 12424740 账号，从 GenerationJob/creditLedger 精确回填 7 张空 model 老图；5 张无来源留空不猜。
- **三方状态**：本次把 7-12 两批（"资产入库/显示统一大改造" + 本条）一起 commit+push → **腾讯=GitHub=本地 三方同步**。

## 2026-07-12 资产入库/显示统一大改造 阶段1/2/3a/4（已部署腾讯，**GitHub 未推/本地未 commit**；阶段3b待做）

详见 CHANGELOG 顶条 + 06-memo-tasks M016/M017。速记：
- **用户定调**：资产原始数据出生即冻结、永不变；之后只有改名/移动/删除（只写 UserAssetState）。这次只保证**以后不再错**，历史数据**不删不改**。未来任何新"生图/生视频/上传"口子都必须走统一存/读方案（`src/lib/media-asset-record.ts`）。
- **阶段1**：新建统一模块 `media-asset-record.ts`（classifyAsset 唯一归类 / buildMediaAssetRecord 一次填齐入库 / resolveAssetPreviewMeta 唯一显示投影 / normalizeLegacySourceKind 老叫法兼容）。
- **阶段2**：生成图/视频/上传 全切统一入口；**视频从此存宽高**、存全量 settings；**关掉 3 个"出生后覆盖"元凶**（对话流兜底 workspace-sessions.ts:157 每次保存全覆盖-连initialName、media-assets POST update、upload-file update 全改 create-only）。
- **阶段3a**：上传按内容哈希（SHA-256 原始字节）去重——视频/音频/文档服务端闭环已生效；`MediaAsset` 加 `contentHash` 列+迁移（腾讯已 apply）。图片服务端已就绪但**休眠安全**（要阶段3b 客户端接线才生效）。
- **阶段4**：资产库/@弹窗/工作流资产显示统一到共享投影；修工作流显示模型原始 id 的 bug。
- **部署**：腾讯已上线（备份 `app-backups/20260712013457-asset-unify/`），验证全通过。**三方状态：腾讯=本地（未 commit/未推 GitHub）**。
- **⚠️ 阶段3b 下一次做**：图片上传去重的客户端接线（thread contentHash + honor duplicate 响应），风险高需浏览器验证，步骤见 CHANGELOG 顶条末尾 + 05-next-actions 顶部。
- **摸底脚本**：`scripts/audit-asset-consistency.mjs`（只读，`--user=ID / --all`），可随时复查资产字段完整度/归类可疑项。

## 2026-07-11 (第二 session) 代码改动 + 运维（全部已部署腾讯+推 GitHub，三方同步 `5e6491c`）

本 session 8 件事全部上线，详见 CHANGELOG 顶条。速记：
1. **通用(general) Agent 模式对齐 agent**：剥离历史图片，只带本轮上传参考图（原来发"你好"会把历史所有生成图 base64 全打包烧 token）。agent/通用的隐式意图带图（"把上面那张图改成…"）保留。
2. **生成图 hover 工具菜单**（对话流每张生成图右上角，`ImageResultThumb`）：下载 + @引用两个按钮，hover 才显示。
3. **@文件名/缩略图规则统一**（对话流+工作流）：有缩略图一定发、@名只管顺序；删缩略图→该图**所有**@名全清（修了正则"只删一个"bug，新 helper `removeAllMentionNames`/`removeConnectedReferenceNames`）；工作流断连/删节点/删连线都清下游@名。
4. **迁移阶段4完成**：main/api DNS 切腾讯、腾讯 nginx 443 SSL 直连、马来出链路。
5. **证书自动续期**：acme.sh tls-alpn-01（走443），LE ECC main+api 到 2026-10-09，cron 自动续（约9-10）。
6. **后台"服务器信息"改腾讯**：主服=容器本地读、阿里镜像=容器直连SSH；表头 `阿里云(杭州)_镜像`/`腾讯云(新加坡)_主服`；公网IP用固定常量（阿里101.37.129.164 / 腾讯119.28.116.16）。
7. **腾讯磁盘扩容**：云盘500G但分区只200G→`growpart`+`resize2fs`在线扩到493G（可用366G）。
- **域名指向**：main/api→腾讯119.28.116.16；ali/static→阿里101.37.129.164；马来无指向。
- **唯一遗留**：马来退役待用户决定（AI 未停）。

## ✅ 2026-07-11 — 迁移全部完成（含阶段4）：main/api 直连腾讯 443，马来出链路

**线上现状**：主服务器 = **腾讯云新加坡 119.28.116.16**（Docker 栈 app+db+nginx）。`main`/`api` DNS 已直指腾讯，**腾讯 nginx 443 直接 SSL 终止**（证书从马来复制，SAN 覆盖 main+api，到 2026-09-06）；`ali`/`static`→阿里 101.37.129.164（阿里入口反代腾讯 5000 + 本地镜像 _next/static、generated 做国内加速）。**马来已彻底不在链路里**（DNS 不指它、app 早已 pm2 stop）。

- **阶段4 做了**：确认用户已切 DNS + 放行 443；宿主 80 被 vibesocial 占→改复制马来有效证书到腾讯；compose 加 443:443 + 挂证书；nginx 加 443 server 块（main/api）。验证 --resolve 直连腾讯 https main/api=200 证书有效、公网 DNS main/api/ali 全 200。
- **遗留（见 05 顶部）**：①马来退役待用户决定（AI 未停）②✅证书自动续期已配好（acme.sh tls-alpn-01 走443，LE ECC 到 2026-10-09，cron 约 9-10 自动续）③http(80)无重定向（宿主80被占，实际都走 https 无影响）。
- **⚠️ 腾讯重 build 后必须同步 `.next/static` 到阿里镜像**，否则 chunk 哈希不匹配全站 404。命令见 05/03。

## 🐛 2026-07-11 — 对话流生成图命名 d0 bug 根治（两处根因）+ 历史数据修复（仅腾讯线上，本地/GitHub 未提交）

- **现象**：会话 d35 生成图叫 `image_40_d0`（应 `image_7_d35`），手工修后新生成又撞名。
- **根因1**：`generation-jobs.ts` 对话流命名 `code` 写死 `d0`（预约改动漏了对话流，工作流 deriveWorkflowCode 是对的）。**修**：客户端传 `conversationCode`→路由透传→reserveJobNames 用它（缺失回退读 `WorkspaceSession.summaryJson.conversationCode`）。
- **根因2（违反用户命名规则）**：`/api/media-assets` POST 的 UPDATE 分支允许客户端 `body.name` 覆盖服务端预约的 `systemName`/`initialName`；旧标签页 re-persist stale 名把终生ID冲回去。**修**：UPDATE 分支不再动 systemName/initialName（终生ID出生即定不可变），currentName 只在工作流临时名finalize时更新。
- **用户命名规则（务必遵守）**：生成媒体系统名 = 服务端在生图时**预约的终生ID**、唯一、成功即定、失败释放；对话流 `image_序号_d{会话号}` / `video_序号_d{会话号}`，工作流 `image_序号_wX`；**不是客户端给的，客户端不可改写终生ID**；改名走 `UserAssetState.currentName`(PATCH)。
- **改的文件**（仅腾讯线上，本地是旧版未改）：generation-jobs.ts、image/route.ts、video/route.ts、chat-workbench.tsx、media-assets/route.ts。历史修复 ID_636611 d35 的 10 张为 image_1~10_d35、nextImageNumber=11。

## ⚠️ 2026-07-10 (迁移 session 续) — 腾讯阶段1 补齐"完整独立"缺口 + 迁移专属 bug 修复 + 产品微调（已 commit+push GitHub；仅腾讯部署）

用户在腾讯 `http://119.28.116.16:5000`（空库全新实例）测试后修复的一批（详见 CHANGELOG 顶条）：
1. **补 nginx 容器**（根治 `/generated` 404 图片视频不显示；Next 只服务构建时已存在的 public 文件、`public/generated` 被 dockerignore 排除，马来靠各自 nginx 服务 /generated）。新增仓库文件 `docker-compose.yml`(含 nginx)、`nginx/flashmuse.conf`。
2. **修资产库上传"保存失败"**（EXDEV：`.runtime` 与 `public/generated` 是两个 bind-mount/设备，`local-assets.ts` 跨设备 rename 改 writeFile+unlink）。
3. **资产生成弹窗加"正在加载中"转圈**（远程 url 加载空白期；**未动**远程→本地替换流程）。
4. **注册送积分默认改 0**（`credits.ts` 默认 + schema 列 `@default(0)` + 新迁移 `20260710010000_signup_credits_default_0`；根因是初始迁移 seed 了 1500，现在 fresh 部署天然=0）。
5. **首页 logo 后显示 "Intl." 国际服标识**（`page.tsx` 腾讯 IP 归国际主站分支）。

**测试完成后已把腾讯清空回全新部署状态**：`compose down` + 删空 pgdata/generated/runtime + `up -d`（重建空库+migrate deploy）。用户两个测试账号及全部媒体清除。验证 User/MediaAsset/WorkspaceSession=0、generated 0 文件、signup=0、三容器 Up、home 200。**腾讯 = 干净的可独立运行全新实例，随时可进阶段2/3。**

- **部署状态**：腾讯领先且已清空（含全部修复 + nginx）。马来/阿里未部署这些（腾讯专属/产品微调）。GitHub=本地（已 push）。
- ⚠️ 阶段3 切 venusface 域名重 build 时：`page.tsx` 的 Intl IP 判断、其它硬编码 IP 要按 09 文档第六节改域名；signup=0 会被马来 pg_dump 覆盖需再确认。

## 📍 迁移大事件进度（马来 BytePlus → 腾讯云新加坡）——做到哪了

- **阶段1（腾讯独立部署 + IP 测试）= 完成 ✅**：腾讯 `119.28.116.16` 上已是一个**完整、可独立运行**的实例（app + postgres + **nginx**，端口 5000）。代码与马来线上逐字节一致(仅 next.config 缓存改进)，本 session 又补齐了 nginx/EXDEV/转圈等迁移专属缺口。用户已完成功能测试并确认 OK，实例现已清空为全新状态。
- **阶段2（夜里接阿里 + 停服）= 未开始**。
- **阶段3（数据迁移 + 切阿里反代 + 放开访问）= 未开始**。这是重头：马来 `pg_dump` → 腾讯 `flashmuse-db`；马来 `/generated` rsync → 腾讯 `data/generated`；阿里反代目标从马来 IP 改腾讯 IP；`NEXT_PUBLIC_*`/硬编码 IP 改域名并重 build；关 insecure cookie、设 cookie domain、开 ali-sync(方向腾讯→阿里)。
- **阶段4（收尾、弃用马来）= 未开始**。
- 权威操作细节见 `09-migration-to-tencent.md`（文件上半乱码、末尾有可读补充）+ 本文件 + `05-next-actions.md` 顶部。

## ⚠️ 2026-07-10 (进行中) — 主服务器迁移 马来 BytePlus → 腾讯云新加坡（阶段1完成，用户测试中）

- **详见 `09-migration-to-tencent.md`（权威记录）。** 起因：马来↔阿里跨境 20~50% 丢包是全站慢的总根源；实测腾讯新加坡下载比马来快约 32 倍。用户决定把主服务器迁到腾讯云新加坡 `119.28.116.16`、马来弃用；阿里保留改为镜像腾讯。
- **阶段划分**：①腾讯 IP+端口独立部署测试(已完成) → ②夜里接阿里+停服 → ③数据迁移(DB+媒体)+切阿里反代到腾讯 → ④收尾弃用马来。
- **阶段1已做**：模型可达性验证无 403(去/留通过)；方案A 独立 Docker 栈部署在腾讯 `/opt/flashmuse/`(app+独立postgres，网络/端口与该机其它项目 CinematicFlow/VibeSocial 完全隔离)，宿主端口 **5000**；`.env.local` 从马来 prod 派生(API key/模型偏好一致)；空库跑全部迁移+worker 启动；home-assets 只传 lite 套；`next.config.ts` 给 `/home-assets`+`/generated` 加长缓存(修首页切视频黑闪)。**md5 核对：腾讯平台代码与马来线上 194 文件逐字节一致，仅 next.config.ts 不同(缓存改进)。**
- **测试地址**：`http://119.28.116.16:5000`(空库全新实例，需邮箱注册/登录；后台 `/admin` 管理员 `lookxun@163.com`)。
- **仓库新增未 commit**：`Dockerfile`、`.dockerignore`、`docker-entrypoint.sh`、`next.config.ts`(改)。下一个 AI 等用户测试结论后做阶段2/3。
- **⚠️ 2026-07-10 补：阶段1 原来漏了 nginx 层，图片/视频显示不出来（已修）。** 根因：Next `next start` 只服务「构建时就存在」于 `public/` 的静态文件，而 `.dockerignore` 排除了 `public/generated`，所以 Next 对 `/generated/*` 一律 404（缩略图走 `/api/media-thumbnail` 路由读盘所以能出→表现为资产库缩略图闪、点开原图空白）。马来/阿里从没暴露此问题是因为 `/generated` 一直由 **nginx** 服务、不经过 Next；腾讯阶段1 的 Docker 栈只有 app+postgres、没搭 nginx。**修复**：给 flashmuse 栈加了 `flashmuse-nginx`(nginx:alpine) 容器，占对外 5000、服务 `/generated`+`/home-assets` 静态、其余反代 `flashmuse-app:3000`；app 改为内部 3000 不再直接占 5000。只在 `flashmuse_default` 网络、只用原有 5000 端口，未碰 ps-/vibesocial。验证：`/generated` 原图从 404→200（本地+外网 IP 均 200），首页/workspace/logo 全 200。
- **⚠️ 服务器与本地仓库已不一致**：新加的 `docker-compose.yml`(含 nginx)、`data/nginx/flashmuse.conf` **只在服务器上，本地仓库/文档没有**。服务器备份原 compose 在 `/opt/flashmuse/docker-compose.yml.bak.*`。下一个 AI 需把这两个文件补进本地仓库、并把"栈含 nginx"写进迁移文档，否则将来重建栈会再次漏掉 nginx。
- **⚠️ `09-migration-to-tencent.md` 文件内容是乱码**（写入时编码被破坏，UTF-8 里存了 mojibake）。需要时重写；本条目 + 05-next-actions 是当前可读的权威记录。

## Latest 2026-07-10 — 生成媒体名称提交时原子预约 + 修复历史重复视频名 (DEPLOYED prod+Ali，含 Prisma 迁移；**GitHub 未推、本地未 commit**)

- Reply style 简洁直接中文. 详见 CHANGELOG 顶条。起因=生产两个不同视频撞名 `video_3_w6`。根因=旧逻辑在**任务完成时**才推名（完成顺序+前端计数器不可靠→并发/重连撞名）。
- **彻底改法**：名字在**提交任务的同一数据库事务里原子预约**（`GenerationJob.reservedNames`，advisory lock 串行化，扫已占用名找空号）。成功直接用预约名、失败 `reservedNames=NULL` 释放可复用。worker `ensureJobReservedNames` 兼容部署前无字段的在途 job。资产写入(MediaAsset+UserAssetState)合并进一个事务，避免半成品占名。**对话流/工作流 图片·视频 + 资产库图片(改成 async job 化)** 全覆盖。前端全部优先用服务端 `reservedNames`、只在旧 job 无名时回退旧客户端编号。
- **部署**：只打包本次 9 路径（**刻意排除**工作树里无关的 `workflow-prompt-optimization/cases/route.ts`）。迁移 `20260710000000_generation_job_name_reservations` 已 apply（`reservedNames:jsonb` 已确认）。备份 `.deploy-backups/20260710-name-reservations/`。前后快照 `stableMissing`/`fallbackUsers` 均 0（后快照 compare ok:false 仅因部署窗口用户正常新增 1 视频）。五项 HTTP 200、PM2 online。
- **历史修复**：撞名的第二条视频(user ID_636611, workflow db0a1ac5..., asset cmrec5oyoxjjtnun1d24od361)一事务改名 `video_3_w6`→`video_4_w6`（MediaAsset systemName/initialName + UserAssetState.currentName + 工作流 canvasJson 节点 mediaSystemNames 四处），备份 `.runtime/manual-fixes/20260710-repair-video-4-w6-before.json`。
- **⚠️ 三方状态**：**prod 领先** = 已含迁移+新代码；**GitHub/本地未 commit**（基线 `eec509a`，本次 9 文件 + 迁移目录仍在工作树；另有无关的 cases/route.ts 改动勿覆盖）。下一个 AI 要同步需 commit+push。
- **BROWSER-VERIFY 待验(下一个 AI 可跟进)**：并发同工作流生成多个视频/图片名字不再撞；生成失败后重生成号码复用不跳空；刷新/断线恢复后名字与资产库一致；资产库角色/场景/分镜图生成名字连续不撞。

## Latest 2026-07-09 (later session) — 右上角使用量计数修复 + 工作流@文件名卡加载/光标修复 + **全量部署 prod+Ali + 推 GitHub** (prod=GitHub=本地 三方同步)

- Reply style 简洁直接中文. 详见 CHANGELOG 顶部条目。本 session 把**之前一整批未部署/未推的改动全部部署上线并推 GitHub**（生成压缩、备案/法务页、上传大改造、网络诊断 logo + 本 session），现在 prod=GitHub=本地。
- **右上角媒体计数(只增不减是产品口径)修了两 bug**：
  1. **对话流**：累计 `addSessionGeneratedMediaCount` 无去重、重复累加致图片虚高(实测 d35 存9实际6)。改为面板直接用实时去重数 `getSessionMediaCounts`（对话流无删除→实时=累计不减）。
  2. **工作流**：累计 `generatedMediaCounts` 从没被持久化（服务器 normalizeWorkflowItems 丢弃它），刷新即丢、删节点会变小。改为存进 `canvas.generatedMediaCounts`（随 canvasJson 存/读回、`updateWorkflowCanvas` 保留、`addWorkflowGeneratedAssets` 写它）→ 真·只增不减、刷新不丢。
- **工作流 @文件名 两 bug**：① 点画布连线缩略图的 `@文件名` 会读资产库转圈卡死——改为只有"本地解析不了的库资产 @mention"才读库(连线引用本地就有)，删掉画布级"任意@就预加载"。② **光标位置(经 3 次迭代最终对齐对话流)**：要求"光标在哪插哪、插完光标在新 @文件名 后、连点依次追加光标永远在最新那个后"。工作流原用滞后的 `cursorOffset` state+`focusRequest`(经 tldraw canvas 回流有延迟→读到旧光标→新 mention 插到旧的前面)。最终改法(commit `ca28540`)：`WorkflowMentionEditor` 加 `externalEditorRef` 把编辑器 DOM 暴露给 parent；`WorkflowPromptBox` 读**实时 DOM 光标** + `focusWorkflowEditorAt`(rAF 等渲染完再 focus+定位，同对话流 `focusEditorAt`)。
- 部署：**全量快照(commit `ef33f0f`)** + 服务器 `npm install`(sharp)；无迁移；备份 `.deploy-backups/20260709-gen-compress-counts/`；快照 compare `ok:true`(assetListHash `1597871847f4c23d` 未变)；五域名 200；worker started。随后**两次窄部署单文件**修 @光标(`79ca99a`错→`ca28540`最终正确)。**现在 prod=GitHub=本地 三方同步于 `ca28540`**。
- **BROWSER-VERIFY(ali 硬刷)**：对话流右上角图片数=当前实际张数(d35 应 6)；工作流生成→累加、刷新不变、删生成节点不减、再刷新仍不减；工作流连线图点 @文件名 秒变蓝字无转圈、连点多次光标都在最新 @文件名 后；真·@库资产仍能读出。生成压缩后台"系统设置"tab + 图片落盘 JPEG/视频 ffmpeg 转码(留意马来 worker CPU)。

## Latest 2026-07-09 — 上传体验修复(已部署) + 生成压缩功能(本地未部署，下一个AI直接部署) (部分 DEPLOYED prod+Ali+Ali-nginx；生成压缩=本地only；**全程 GitHub 未推**)

- Reply style 简洁直接中文. 两大类，详见 CHANGELOG 顶部 A/B。**下一个 AI：用户明确要求直接部署——把 (B) 生成压缩部署 prod+Ali，并把 8866940 之上这一长串未推改动(备案/上传大改造/网络诊断/本session)一起推 GitHub。** 部署要点见 05-next-actions 顶部。
- **(A) 已部署 prod+Ali 的上传修复**：
  1. **上传卡95%根治**：Ali nginx `flashmuse-static-ip` 两个反代 `location /` 加 `proxy_request_buffering off;`(nginx -t+reload，备份 `.bak.20260709105656`)。Ali 边收边转发不再双重缓冲。
  2. **上传进度条真实化**：新建 `src/lib/upload-progress.ts`，前段字节映射随机 60~70% 封顶、后段合成慢爬到99、响应到才100；三个上传函数(chat-workbench 两个 + workflow-inner 一个)接入。
  3. **工作流上传节点左上角显示文件名**(image/video/audio)。
  4. **工作流上传节点刷新卡"上传中99%"修复**：`getPersistableWorkflowItems` 存库边界剥离 uploadProgress/uploadPreviewUrl(运行时保留，不动 normalizeState)。
- **(B) 生成压缩功能 —— 本地 only，未部署，tsc+build 通过**：
  - 后台"系统设置"改名"模型开关"(图标 RiToggleLine)；新增"系统设置"tab(key=generation，图标 RiSettingsLine)做**生成文件压缩控制**：图片/视频两行，各"压缩"开关+质量三档 高(95%)/标准(80%)/低(60%)，关则质量置灰。默认**都开启+标准80%**。
  - 接线：**图片**用 `sharp` 转真实质量 JPEG(开)/保留原图(关)；**视频**用 ffmpeg H.264 CRF(95→18/80→21/60→24)转码、**变小才替换**、+faststart(media-save-queue 内异步)。只影响模型生成图/视频，不动上传路径。
  - 新增依赖 `sharp` + `next.config.ts serverExternalPackages` 加 sharp。**部署时服务器要 `npm install`(sharp Linux x64 原生二进制)**。
  - ⚠️ 视频压缩默认开→马来 worker 会多 ffmpeg 转码开销，部署后留意 CPU。
- **押后(M015)**：阿里端上传压缩转发小服务(用户"以后再说")。查证阿里 2核/3.4G/全闲/已装 ffmpeg，可行，成本是维护一个阿里小服务。

## Latest 2026-07-08 (网络诊断 session) — 上传卡95%根因(nginx层) + 跨境50%丢包 + 开BBR + logo去提示语 + 跨境加速方案调研 (部分 DEPLOYED；**GitHub 未推**)

- Reply style 简洁直接中文. 本 session 主要是**诊断 + 服务器运维**，代码只改了一处(首页 logo)。详见 CHANGELOG 顶部 6 条。
- **用户报"工作流_06 视频上传又卡95%超时"**：查明 **B7(base64→二进制 multipart)没做错、部署正确在工作**；真根因在 **nginx 层**——用户那次 upload-file 返回 **408**(nginx 收 body 超时)。两个原因：① **Ali `location /` 默认 `proxy_request_buffering on`**(先缓冲整个视频再转发马来，双重缓冲慢)；② **马来接收端默认 `client_body_timeout 60s`** 太短，大视频被掐。
- **⚠️ nginx 当前状态(修了一半)**：马来两个 conf 已加 `client_body_timeout 300s`(保留)；**Ali 的 `proxy_request_buffering off` 加了又回滚了**(因当时误判它导致全站卡)。**上传卡95%的真根因(Ali 双重缓冲)仍未修**。下一个 AI 根治：重新给 Ali `sites-enabled/flashmuse-static-ip` 两个 `location /` 加 `proxy_request_buffering off`(**别加 proxy_max_temp_file_size 0**)再复测大视频。
- **全站卡真根因 = 马来↔阿里国际链路当前 50% 丢包**(从 Ali ping 马来 RTT~282ms/丢包20~50%，双向)。用户全走 ali 反代马来，动态请求全被重传拖慢。**这是双服务器架构固有软肋，非本次改动引起**(回滚 Ali 后照样卡)。
- **已开 BBR 缓解**：马来+阿里都切 `tcp_congestion_control=bbr` + `default_qdisc=fq`，写进 `/etc/sysctl.conf` 持久化。A/B 实测 cubic ≈3.7~11KB/s → BBR ≈35~48KB/s(快6~10倍)。仍治标。
- **前端小改并部署**：首页 logo 切换线路按钮去掉鼠标悬停文字(删 `title`，留 `onClick`+`aria-label`)。`tsc` 通过，窄部署 prod(build EXIT=0/PM2 online/Ali 同步)，首页 200。备份 `.deploy-backups/20260708-logo-no-tooltip/`。**未推 GitHub。**
- **长期优化方向(与用户讨论定调，写进 05-next-actions)**：把马来 BytePlus 换成**阿里海外(新加坡)**，两台都阿里→走阿里私有骨干组内网，根治跨境丢包。香港排除(被当国内、模型 403)；跨境带宽费换任何中国云都躲不掉；第三方隧道(GlobalSSH 类)因合规/稳定性不建议；CDN 已因费用放弃。GA vs CEN 价格对比已写桌面文件 `C:\Users\ASUS\Desktop\跨境加速方案对比_GA_vs_CEN.md`(100GB/月：GA≈240元、CEN≈700+元；建议先 GA 按量试)。

## Latest 2026-07-08 (备案+上传大改造 session) — 国内备案页/footer + 上传链路重构 (全部 DEPLOYED prod+Ali；**GitHub 未推**)

- Reply style 简洁直接中文. 两大块：(1) 国内上线合规——首页底部黑色70%透明备案横条(浙ICP备2026046799号/浙公网安备33010802014584号/浙B2-20200520/营业执照/杭州亿阅科技有限公司/本站内容均由AI生成，均新窗口链接)、新建 `/terms` `/privacy` 法务页(参考可灵+小云雀写法，主体=杭州亿阅/闪念，顶部切换栏)、登录框统一 50px高/5px圆角。(2) **上传链路重构**(核心，详见 CHANGELOG 顶部 B1-B8 + "⭐当前上传链路全景")。
- **上传链路当前状态(务必记住)**：
  - 图片走 `/api/asset-upload-temp`(multipart二进制+token→PATCH commit)；视频/音频/文档走 `/api/upload-file`(**已从 base64 JSON 改成 multipart 二进制流式**)。
  - ali 用户上传走**同源 ali.venusface.com**由 Ali nginx 反代到马来(A方案，客户端上传腿走国内更稳)；文件在马来落盘。
  - 上传落盘后 fire-and-forget `syncGeneratedFilesToAli` **把文件回传 Ali 本地镜像**(否则 ali 用户读取走慢回源代理→灰屏)。上传路由和读取路由是两条独立链路。
  - JPEG 需转码时服务端**内联转码**(不再抛错让客户端重传一趟)。
- **本 session 修的三个上传痛点**：① 埋点把两步式误记成失败(已修+清历史56条)；② 工作流视频卡1%(无进度→改二进制有进度)；③ 视频卡95%超时(base64 JSON 极慢→二进制 multipart 正解)。
- 手工清理了 ID_636611 工作流_06 一个卡95%的幽灵节点(WorkspaceWorkflow.canvasJson)。
- **GitHub 未推**：改动在 `8866940` 之上未提交。文件清单见 CHANGELOG。下一个 AI 若继续改上传，先读 CHANGELOG"⭐当前上传链路全景"。
- 部署健康：每次 build EXIT=0、PM2 online、Ali 同步、main/ali/api 200。tsc+build 通过。

## Latest 2026-07-08 (later session) — 工作流恢复时序 bug 修复 + 推 GitHub (DEPLOYED prod+Ali + **PUSHED GitHub `8866940`**)

- Reply style 简洁直接中文. 本 session 只做一件事:修工作流断线重连恢复的**时序竞态 bug**(用户报"关浏览器20分钟后回来，视频5秒恢复但图片要等1分钟")，窄部署单文件，并把上个 session 攒的所有本地改动**一起推了 GitHub**。现在 **prod=GitHub=本地** 三方同步。
- **根因**:工作流 4 个恢复入口靠"挂载后固定延时(1500-2400ms)各跑一次" reconcile，靠 `stateRef.current.nodes` 有节点才能对齐、找不到就跳过不重试。但节点数据(`value` prop)是异步从 workspace-state 拉回、约 2200-2400ms 才落地——纯赌运气。视频 reconcile 2400ms 赶上→5秒出;图片 reconcile 2200ms 扑空→放弃→只能等**服务端保存自愈**下次周期补回→1分钟。
- **修复**(纯前端单文件 `workflow-tldraw-canvas-inner.tsx`):新增 `pendingRecoverySignature`(useMemo，从 `value.nodes` 里"仍在转圈"节点派生签名)，4 个恢复 effect 改成**依赖这个签名**触发(节点一加载出来就立刻恢复)，setTimeout 缩到 300-400ms;图片 reconcile 的 workflowId 匹配放宽到与视频一致。**持续重问兜底本就存在**(pollImageNode 每3s / pollVideoNode 每10s 的 while 循环，用户确认无需改)。
- **对话流没改也不需要**:`chat-workbench.tsx` 对话流恢复 effect 本来就依赖 `sessions`(数据驱动)、消息一加载就跑、running 每3s重查、visibilitychange 重跑——这正是我把工作流改成的样子。工作流当初写错成固定 setTimeout 才有这 bug。
- 部署:窄部署单文件(md5校验)，备份 `.deploy-backups/20260708-workflow-recovery-timing/`，build EXIT=0，四域名全200，worker started。删了调试残留 `dbg-wf02b.sh`。`tsc` 通过。commit `8866940` push 成功(14文件，含上个 session 全部 job 化改动 + 本次修复)。
- **BROWSER-VERIFY(ali 硬刷 Ctrl+Shift+R)**:工作流生成图片/视频转圈时关浏览器，几分钟后回来→图片和视频**都应在几秒内**恢复(不再有图片等1分钟)。
- **教训**:恢复逻辑要**数据驱动**(依赖加载出的状态签名)，别用"挂载后固定延时"赌数据是否已加载。

## Latest 2026-07-08 (earlier session) — 图片 job 化上线 + 视频 job 化 + 生成恢复多层兜底 + 工作流2000字修复 (全部 DEPLOYED prod+Ali；GitHub 未推)

- Reply style 简洁直接中文. 本 session 把上个 session 只在本地的图片 job 化**部署上线**(应用了 Prisma 迁移 `20260707000000_generation_jobs`)，并**完成视频(对话流+工作流) job 化**，再修了一串生成恢复的坑。**GitHub 仍未推**(prod 领先 GitHub，本地有未提交改动)。详见 CHANGELOG 顶部 8 条。
- **现在完整链路(务必记住)**：
  - 图片(对话流+工作流)：前端 `/api/image?async` 建 job→worker 生成/挑图/扣费/写资产库/落状态→前端查 `/api/generation-status` 展示。
  - 视频(对话流+工作流)：`/api/video` 负责创建(含 BytePlus 真人审核)拿 providerTaskId→建 `GenerationJob(kind=video)`→worker 轮询平台→**等本地存盘完成**→扣费/写资产库/落状态→前端查 `/api/generation-status`。
  - 断线/刷新/退出/重启不丢；只有平台明确失败才失败卡，视频无超时。
- **本 session 关键修复(3 个根因级)**：
  1. **视频 job 存过期远程 URL + 视频不进资产库** → `runVideoJob` 加 `waitForMediaSaveJob` 等本地存盘完成再落库。
  2. **服务端保存自愈(B)** → `mergeWorkflowCanvasMedia` 原来把 `images:[]` 当有值，旧标签页空图覆盖已成功结果;现在空数组视为缺失+用 DB/succeeded job 补回+清 isRunning。**旧标签页再也覆盖不掉。**
  3. **标签页挂起恢复不重跑恢复** → 工作流+对话流加 visibilitychange/focus 监听，重新可见时重跑全部恢复。这是"关浏览器→回来视频出了图片没出"的真根因。
- **恢复兜底全景**：对话流图片/视频按 `requestId:image|video:N` 消息级 reconcile;工作流图片/视频按 workflowNodeId reconcile;三者都在打开时 + 标签页可见时触发。
- 手工修复过 ID_271898 `c211e3cb` 与 12424740 工作流_02 `11d9ce76` 的卡死节点(按 job 写回 canvasJson)。B 修复后不再复发。
- 部署健康:build EXIT=0，main/admin/api/ali 全 200，PM2 online，`[generation-worker] started`。

## Latest 2026-07-07 (最新 session) — 生成链路持久化改造 第一步：图片(对话流+工作流) job 化 (⚠️ 本地 only，未部署未推 GitHub；视频未做)

- Reply style 简洁直接中文. **本 session 全部改动仅在本地**：新增 `GenerationJob` 表 + 常驻后端 worker + `/api/generation-status`，把**图片**生成(对话流+工作流)改成"后端持久任务"：提交建 job 立即返回 jobId → worker 后台调模型/挑图/扣费/**写资产库**/落状态 → 前端只轮询状态展示。断开/刷新/退出/服务重启都不影响后端跑完，只有模型明确失败才失败卡(无超时兜底)，永不回来图也进资产库。`tsc`+`build` 通过，用户本地自测图片 OK(4 条 conversation image job 全 succeeded)。
- 新增文件：`prisma/migrations/20260707000000_generation_jobs/`、`src/lib/generation-jobs.ts`、`src/lib/generation-worker.ts`、`instrumentation.ts`、`src/app/api/generation-status/route.ts`。改动：`schema.prisma`、`api/image/route.ts`(async 分支)、`chat-workbench.tsx`、`workflow-tldraw-canvas-inner.tsx`。详见 CHANGELOG 顶部。
- **视频还是旧的前端驱动(未 job 化)**：能恢复只因服务商持 taskId + 前端 resume，但用户永不回来/重启时后端不推进、不写库。**下一个 AI 首要任务：先部署本地这批(有 Prisma 迁移)，再把视频对话流+工作流也 job 化。** 见 05-next-actions 最上。
- 也加了"发送即保存"(工作区 500ms 防抖在提交生成时改 0ms `flushNextWorkspaceSaveRef`)缩短"点完秒关没保存"窗口；工作流用 reconcile(按 workflowNodeId 从后端对齐) 兜底不依赖保存。
- 手工恢复了 312876953@qq.com 一张卡死的工作流图(GPT-5.4 2K 改图，生成成功但前端断连卡死)→补进资产库 `image_1_w1`(MediaAsset+UserAssetState)。此类问题已被本次图片 job 化根治。


## Latest 2026-07-06 (最新 session) — B_373文案幂等 + 使用提示词原样还原 + 视频存干净prompt + 参考hint统一并按意图判定 (DEPLOYED prod+Ali + **PUSHED GitHub**)

- Reply style 简洁直接中文. 全 session 窄部署(单文件 scp+部署脚本)到 prod+Ali，每次 md5 校验+三域名200。**session 末尾把攒的批次一起推 GitHub**(含上个 session 未推的 `video/route.ts` 去掉审核上限、全平台命名统一等)。改动：`error-message.ts`、`chat-workbench.tsx`、`workflow-tldraw-canvas-inner.tsx`、新增 `reference-hint.ts`。详见 CHANGELOG 顶部 5 条。
- **B_373 幂等修复**：`toUserErrorMessage` 不幂等 → 后端已翻 Case3 中文串被前端二次翻译误判成 Case2。Case3 正则补 `参考图已过审|成品(?:视频|音频).*(?:敏感|版权)`。教训：翻译函数要幂等。
- **对话流"使用提示词"原样还原**：改为按消息真实附件(`message.imageReferences`+`message.uploadedFiles`)还原图/视频/音频/文档卡，不再只靠@提及重推导。`setActiveDraftInputWithMentionCards` 加 `restore` 参；`copyPrompt`+Agent面板接入。
- **工作流"使用提示词"带回连线引用**：生成成功时把 `uploads`+连线引用快照进新字段 `generationUploads`(不动 data.uploads)，`addNodeFromPrompt` 用它还原+补 `videoReferenceMode`。因成功时会删入边、连线引用原本会丢。
- **工作流视频存干净 prompt**：视频成功回写 `data.prompt`/`sourcePrompt` 由带 hint 的 `modelPrompt` 改为干净 `prompt`(创建仍用 modelPrompt)。只对新生成生效。
- **参考 hint 统一+意图化**：新建 `src/lib/reference-hint.ts` `buildReferenceHint`，两端共用。逐张判定：@图前同句含"参考/参照/借鉴…"=松参考(可自由发挥)，否则绝对保留。全绝对/全参考/混合三态文案。

## Latest 2026-07-06 (later session) — 火山审核报错细分 + 去掉审核次数上限 + 全平台命名统一 (DEPLOYED prod+Ali, **GitHub 未推**)

- Reply style 简洁直接中文. 本 session 多次窄部署 prod+Ali，**GitHub 全程未推**(用户要求攒批)。改了 `error-message.ts`、`video/route.ts`、`chat-workbench.tsx`、`workflow-tldraw-canvas-inner.tsx`。详见 CHANGELOG 顶部。
- **火山审核报错细分**：`error-message.ts` 按 input/output 分流 → Case2 输入/参考图审核未过(建议换图)、Case3 输出视频/音频审核未过(可重试/改词、音频提示去背景音乐)。
- **去掉审核次数上限(A)**：删 `getBytePlusReferenceFailure` 短路 + auto-review `attempts>=3` 拦截。同一参考图**每次重试都重新送审、无上限**(重试可能过审，已实证)。对话流+工作流共用 `/api/video` → **两流都覆盖**。
- **全平台命名统一**：终身ID(`initialName`)出生即定写库永不变；改名存 `currentName`；显示 `改名||终身ID`，同一图按URL处处同名。新资产库生成图命名 `asset_N_role/scene/storyboard`(每用户全局计数)。停掉 `applyAssetGenerationSystemNames` 的下标重排(三名bug根因)。工作流加校准 effect 把节点 `mediaSystemNames` 按URL对齐库名(含改名同步)。补了 2 张空名老图。
- **⚠️ 已修复的崩溃**：命名校准 effect 初版用 inline 新数组派生的 Map 作依赖 → 每渲染 updateState 无限循环 → 工作流页崩("This page couldn't load")。已改为稳定内容签名字符串依赖 + 差异守卫。**教训：effect 依赖不可用父组件 inline 数组/对象引用。**
- **③ 引用发送**验证无泄漏(removeUpload/disconnectNodes 都正确)，无需改。
- **④ 对话流"让它动起来/这张图"自动带最近图(无@无缩略图)**：仍未处理，用户押后(见 05-next-actions)。

## Latest 2026-07-06 — 工作流：从资产库导入 + 视频轮询恢复 + 多项修复 (DEPLOYED prod+Ali + PUSHED, commit aad3461)

- Reply style: 简洁直接中文. 全 session 都是**本地工作流**功能与修复，最后一次性部署+推 GitHub。仅改 3 个文件(`workflow-tldraw-canvas-inner.tsx`, `workflow-tldraw-canvas.tsx`, `chat-workbench.tsx`)，无 schema 变更。快照 compare `ok:true`(assetListHash `3a057badbe5d3daa` 未变)，四个域名全 200。详见 CHANGELOG 顶部。
- 新功能「从资产库导入」：dock/右键/空工作流三入口(图标 RiExportFill)→ 弹窗(左分类tab带图标、右缩略图 grid-cols-5 直角、勾选多选蓝描边、下拉流式每屏30、只读缩略图)→ 确定把选中图片/视频拉进画布。**导入不生成/不落库/不扣积分/不写埋点**，只引用已有 URL，所以后台生成记录/积分统计不受影响、对话流与工作流生成不会混。按 `origin`(生成/上传)还原节点样式；`allowDuplicate` 允许重复导入同图；一行五张网格不重叠；确定时 `new Image()` 量原图真实尺寸(previewMeta 尺寸不可信)。
- 工作流视频轮询：部署/刷新打断后**自动恢复轮询**(对齐对话流)。加 `videoRequestId` 持久化 + `resumeInterruptedVideoNodes` effect，进入工作流扫描 `isRunning&&taskId` 的视频节点续轮询。
- 修复「点击工作流置顶」(02/04 反复出现，含刷新)：根因是打开含视频工作流时 `mediaSystemNames`/videoDimensions 等自动回填让"有意义快照"变化 → bump updatedAt → 持久化。`getWorkflowMeaningfulSnapshot` 现剔除所有运行时/派生字段(visualSize/videoDimensions/durationSeconds/videoCurrentTime/imageDimensions/isRunning/taskId/videoRequestId/startedAt/uploadProgress/error/mediaSystemNames/posterUrl)，只有真实编辑才置顶。
- 视频右上角时长改用真实 `durationSeconds`(加载回填)显示，老数据自愈；上传/导入的媒体右键"使用提示词"置灰；「上传节点」改名「从本地上传」；工具栏/右键菜单加分隔线。
## What Is Current

- The app is a deployed internal MVP for chat-based image and video generation.
- Login, admin, credits, workspace history, media persistence, and asset management are already real features. Old notes saying these are future-only are outdated.
- Workspace history is split into `WorkspaceSession` and `WorkspaceMessage`.
- Media and asset state are split into `MediaAsset` and `UserAssetState`.
- User-facing asset library now reads `MediaAsset + UserAssetState`. Current deployed code no longer uses old `UserWorkspaceState.state.assets` as a normal UI source for users with new-table rows; old asset JSON should be treated only as historical migration/recovery input.
- All user deletes are soft deletes. Do not physically delete generated files or database records unless the user explicitly changes this product rule.
- Workflow mode is now OPEN in production and WORKING (2026-07-04): `NEXT_PUBLIC_WORKFLOW_MODE_ENABLED=true` in prod `.env.local`. The entry is live for users and the canvas no longer blanks out.
- 2026-07-04 RESOLVED the "workflow nodes appear then vanish" blocker. ROOT CAUSE was NOT our bug and NOT a generic tldraw watermark: tldraw **5.1.1** `@tldraw/editor` `LicenseProvider.mjs` has a HARD gate — `shouldHideEditorAfterDelay(state)` returns true for `unlicensed-production`/`expired`, and after `LICENSE_TIMEOUT = 5000ms` it calls `setShowEditor(false)` and renders `<LicenseGate>` (an empty `display:none` div with `data-testid="tl-license-expired"`) IN PLACE OF the whole canvas app. The editor instance stays alive (so shapes/store still report data), which is why nodes existed but the DOM canvas was gone. The red "No tldraw license key" console lines are harmless and were a red herring.
- 2026-07-04 FIX (user decision: internal-only tool, not public commercial, so technical bypass is acceptable): used `patch-package` to set `shouldHideEditorAfterDelay` to always `return false` in all three builds (`dist-esm`, `dist-cjs`, `src`). Patch file `patches/@tldraw+editor+5.1.1.patch`; `postinstall: patch-package` added to `package.json` so it auto-reapplies after every `npm install`. NO tldraw license was purchased. If tldraw is ever upgraded past 5.1.1, re-generate the patch. For future public/commercial use, revisit buying a license or migrating to React Flow.

## Latest Real Work

## Latest 2026-07-05 (最新 session) — 后台媒体详情弹窗 按类分页 + 流式加载 性能优化 (DEPLOYED + PUSHED, commit f5bc38b)

- Reply style: 简洁直接中文. 用户反馈后台大表(用户管理/生成记录/积分管理)展开后"点开详情"仍卡：点「所有生成图片」读很久、关掉点「所有生成视频」秒出(说明一次把多类一起读了)，缩略图/原图加载不全。要求：只做代码优化，点哪个只读哪个、单类太多先读少量、下拉流式加载(同前端资产库右侧)。全部实现并部署 prod+Ali、推 GitHub。均 admin-only，不动用户端数据。
- ROOT CAUSE: `GET /admin/api/records/user-detail?mode=media` 一次读该用户全部 `userAssetState`(重字段)并构建 conversation/asset/workflow/upload 四数组全量返回(第一次点就全读，第二类因整包被 `admin-detail-cache` 缓存才秒出)；`AdminMediaDialog` 一次渲染全部缩略图(几百 img 并发)；积分/上传弹窗同理且积分走最重 `mode=full`(读全量 workspaceMessages)。
- 后端 `src/app/admin/api/records/user-detail/route.ts`: (1) 抽出共享 `getAssetScope()` 分类器(精度对齐 getFastMediaSummary，workflow>asset)，getMediaAssetItems 改用它。(2) 新增 `mode=media-page`(mediaType/assetType/offset/limit=12)：先用 `LIGHT_SCOPE_SELECT` 轻字段扫全行分类+排序得 total+当页 id，再仅对当页 ≤limit 行拉 DETAIL 重字段构建，返回 `{items,total}`。(3) 新增轻量 `mode=uploads`(只 `UPLOAD_RECORD_SELECT` 构建上传记录，供生成记录「上传记录」弹窗)。
- 前端: `AdminMediaDialog`(admin-users-panel.tsx) 改为自加载+滚动分页(props 变为 userId/userLabel/mediaType；loadPage/onScroll用 event.currentTarget；缩略图 lazy)；`openMediaDialogForUser` 两处(users+records)改为直接 setState 打开(传 nickname||email)；records `loadMediaDetailForDialog` 改 fetch `mode=uploads`；credits 面板新增 `useRevealOnScroll`，`CreditFlowDialog`/`CreditCategoryDialog` 分批渲染(24/批+下拉加载更多)。生成记录的对话流/工作流/资产 图片视频弹窗现全走新分页组件。
- 坑: PowerShell `Get-Content -Raw|Set-Content -NoNewline` 破坏了 credits 面板文件编码(变 binary/乱码)，`git checkout --` 还原后改用 edit 工具重做 → **改源码一律用 edit 工具**。`npx eslint` 会报一堆 `no-explicit-any`(历史写法)和 `react-hooks/set-state-in-effect` 的 error，但**不 gate `next build`**(Next16 build 不跑 eslint) → 判定以 `tsc --noEmit`+`npm run build` 为准，本次均过。
- NOT done(留给后续): 积分弹窗**首开**仍走 `mode=full`(读全量消息)可能偏慢——没动其取数逻辑(积分"最重要别弄坏"，改口径有丢"无 ledger 媒体行"风险)；若实测首开仍慢再单独做 credits 轻量接口并仔细验证。前端 `isConversationUploadedAsset` vs 服务器口径统一(旧遗留)仍未做。
- 部署: 全量源码快照(无 schema 变更)。备份 `.deploy-backups/20260705-admin-media-pagination/`；快照 `20260705-admin-media-before/after.json` compare `ok:true`(assetListHash `c626460d0ab1da0d` 未变, stableMissing/fallbackUsers 0)；`/workspace` `/admin` `/api/model-availability` `ali.venusface.com/workspace` 全 200。GitHub `f5bc38b`。

## Latest 2026-07-05 (latest session) — Asset Library / @引用资产 Alignment + Uploaded-Audio-Shown-As-Broken-Image FIX (DEPLOYED + PUSHED, final commit 55d427d)

- Reply style: concise/direct Chinese. Full session, several iterations + one self-correction (READ the correction below). All deployed to Malaysia prod + Ali and pushed to GitHub. Commits: `c7cd22b`(alignment) → `847aaa7`(revert existsSync relaxation + hide stale remote URLs) → `55d427d`(exclude audio/documents). See CHANGELOG top for full detail.
- USER'S 6 ASSET-LIBRARY RULES (must honor): (1) library shows ONLY images+videos; (2) 角色/场景/分镜 generatable in library; (3) 上传图片 allows direct upload + both conversation- and workflow-uploaded images appear there; (4) those four categories = @引用资产, and the @-popup (conversation AND workflow) count+content must EXACTLY match those four categories' current library display; (5) conversation/workflow assets can be moved into the four @-categories (already implemented); (6) library shows all generated+uploaded images/videos except recycle bin. Compliance: 1/2/3/5/6 already met; 4 fixed this session.
- ALIGNMENT (c7cd22b, still active): conversation @-popups now (a) show the group category label ("上传图片") as the gray sub-label instead of `assetTypeLabels[asset.type]`("待分类"), (b) show REAL server `assetCounts` in the tab count (new `mentionGroupToAssetCountKey`), (c) scroll-to-bottom auto-loads more (`loadMoreMentionGroup` + `onScroll`, no button). Workflow @-popup got `referenceAssetCounts`/`onLoadMoreReferenceAssets` threaded props→runtime for the same behavior. Files: `chat-workbench.tsx`, `workflow-tldraw-canvas-inner.tsx`, `workflow-tldraw-canvas.tsx`.
- ⚠️ SELF-CORRECTION (do not repeat): c7cd22b also RELAXED `src/app/api/workspace-state/route.ts` `isVisiblePersistedMediaUrl` to skip the local `existsSync` gate — that was WRONG and was fully REVERTED in 847aaa7. The original existsSync gate + `mediaExistsCache` is correct and MUST stay: it properly hides orphaned rows whose file is missing. Also: I confused the LOCAL dev DB (`12424740@qq.com`=`ID_779117`) with PROD (same email = `ID_636611`). When querying prod data use the PROD userId.
- REAL ROOT CAUSE of the "3 blank/broken cards (image_1/2/3_d0) in 上传图片, appear after switching workflow, count excludes them": they are UPLOADED AUDIO (`demo_happy/english/chinese.mp3`, `mediaType=audio`, `currentCategory=conversation_upload_audios`, stored as `.bin` under `/generated/users/ID_636611/files/`, `promptSource="upload"`, `sourceKind=workflow_upload`). Client `isConversationUploadedAsset` only excluded VIDEO, not audio → `promptSource==="upload"` made audio show in the image grid → broken. Existing `isAudioAsset` only matches `.mp3/.wav` but files are stored `.bin`, so it missed them. "Appears after workflow switch" = `/api/media-assets?workflowId` pulls workflow-uploaded audio into client `assets`. Violated rule 1.
- FIX (55d427d, `chat-workbench.tsx`): new `isNonDisplayableFileAsset(url)` = files under `/generated/.../files/` that are NOT video (not .mp4/.webm/.mov/.m4v) = audio/documents; excluded at the top of BOTH `isAssetInFilter` and `isMentionGroupAsset` (removes them from every library category + @-popup). Uploaded videos (`/files/*.mp4`) still show in the video category. Also kept `isUnhostedRemoteAssetUrl` (from 847aaa7) which excludes expired remote provider URLs (Volces TOS / OpenRouter) from library+mention, mirroring the server gate.
- VERIFIED on prod: NO file-missing orphans anywhere (2825 visible rows, 0 orphaned); no other account has this problem; the issue was purely audio-shown-as-image. prod-deploy-snapshot compares all `ok:true`; `/workspace` `/admin` `/api/model-availability` `ali.venusface.com/workspace` all 200. A transient post-deploy `ChunkLoadError` is normal (hard-refresh fixes; chunks verified 200 on main/ali/static).
- NOT done (low impact): unify client `isConversationUploadedAsset` vs server `getAssetPageWhere("conversation_uploads")` exact predicate (edge case: promptSource=upload but non-/upload_image/ path). Counts already use server truth so numbers match the library regardless.

## Latest 2026-07-05 (later session) — Asset Library + Admin Detail Performance Optimization (DEPLOYED + PUSHED)

- Reply style: concise/direct Chinese. Fixed reported slowness with NO functional/content change (compute-only). User report: 资产库打开非常卡、半天才显示、有时显示0要刷新多次、部分图片/视频封面显示不出来; 后台大表(生成记录/用户管理/积分管理)展开行详情也很卡. Deployed to Malaysia prod + Ali (full-source snapshot, no new migration) AND pushed to GitHub.
- ARCHITECTURE fact confirmed this session: 主服务器在马来(101.47.19.109)因为很多模型国内不可访问; 用户全是内部人、全走阿里镜像域名(ali.venusface.com); 图片/静态由阿里本地磁盘同源服务(对CDN用户`shouldUseStaticAssetBaseUrl()`=true)，所以图片本身不慢，卡在马来后端 API 的阻塞式代码。
- ROOT CAUSE of asset-library lag (all in `src/app/api/workspace-state/route.ts`, the real asset endpoint via `?assetsOnly=1`; NOT `/api/media-assets` which is workflow-only): a SYNC `existsSync` disk stat was run PER asset row on 3 hot paths (counts + page filter + full-list), ~1100+ blocking stats per open for a 571-asset user, stalling the Node event loop. Plus fake pagination (findMany no take/skip, fetched all rows incl. `previewMeta` JSON then JS-sliced) and `getAssetCounts` re-fetching all rows with `previewMeta` in parallel. "显示0/刷新多次" = counts vs list used independent existsSync results; mismatch made the frontend loop re-fetching forever.
- FIXES: (1) `cachedFileExists()` module cache (positive TTL 1h — files are soft-deleted only/never physically removed; negative TTL 15s). Removes the stat storm AND makes counts+list share one cache → kills the refetch-loop cause. (2) `getAssetCounts` now selects only `currentCategory/deletedAt/purgeAt/mediaAsset.url` and inlines classification (no `previewMeta`, no object build); removed now-unused `isLegacyVideoAsset`/`isLegacyUploadedAsset`. (3) `src/components/chat-workbench.tsx` new `AssetThumbnailImage` (render-time prev-src reset, not useEffect) — grid thumbnails fall back to full original media URL on load error → fixes "封面/图片显示不出来"; purely additive.
- ADMIN big-table row expand (`GET /admin/api/records/user-detail`) was slow for the SAME class of reason (NOT existsSync — admin doesn't use it): `records`/`media`/`full` modes used `include: { mediaAsset: true }` = all 40+ columns incl. `previewMeta`/`generationSettings` JSON + `UserAssetState.legacyAssetJson`, no pagination, even though `records` mode only needs `getFastMediaSummary` counts. FIX in `src/app/admin/api/records/user-detail/route.ts`: 3× `include` → explicit `select` (`RECORDS_ASSET_STATE_SELECT` minimal for the row expand, `DETAIL_ASSET_STATE_SELECT` for media/full keeping all rendered fields but dropping the 3 big JSON columns). Output identical.
- INTENTIONALLY NOT done (to bound risk): kept JS-side pagination/sorting in workspace-state (sortOrder can override firstSeenAt so DB take/skip risks wrong pages, and the existsSync cache already fixed the real bottleneck); no new DB indexes (existing ones cover the where clauses); no creditLedger select (heavy `metadata` needed anyway).
- Deploy: full-source snapshot also re-carried the already-live 2026-07-05 admin-overview/analytics work; committed+pushed together. Backup `.deploy-backups/20260705-perf-asset-admin/`; snapshots `20260705-perf-before/after.json` compare `ok:true` (assetListHash unchanged, stableMissing/fallbackUsers 0). `/workspace` `/admin` `/api/model-availability` `ali.venusface.com/workspace` all 200. `npx tsc --noEmit` + `npm run build` passed locally.

## Latest 2026-07-05 — Admin Overview Rebuilt (REAL data) + Analytics Instrumentation + Conversation Cumulative Media Count (DEPLOYED to prod + Ali; NOW pushed to GitHub)

- Reply style this session: concise/direct Chinese. Everything below is DEPLOYED to Malaysia prod (101.47.19.109) + Ali via full-source-snapshot deploy, with the Prisma migration applied on the server first. It is NOT committed/pushed to GitHub yet (user only asked to deploy this session). Next AI: `git commit` + push to sync (prod is ahead of GitHub).
- CONVERSATION top-right usage panel media counts are now CUMULATIVE (只增不减), matching workflow. Added `WorkSession.generatedMediaCounts` in `chat-workbench.tsx`; incremented on conversation image/video generation success via `addSessionGeneratedMediaCount` (seeds from the old derived count for pre-existing sessions), displayed as `activeSession?.generatedMediaCounts ?? getSessionMediaCounts(...)`. Persists automatically through `summaryJson` (no backend change). Deleting a message no longer lowers the count. NOTE: token/积分 in that panel were already cumulative (`addUsageSummary`).
- ADMIN OVERVIEW was FULLY REBUILT to a 运营/产品 dashboard with REAL data, replacing the old overview. The user disliked the old overview (useless system-status rows, image+video generation not split, retention 0%, failure-top empty). The old overview branch AND ~60 overview-only helper functions/components were DELETED from `src/app/admin/page.tsx`. The other admin tabs (users/credits/records/settings/upload-rules/gpt-image/server) are unchanged.
- New overview data loader `src/lib/admin-overview.ts` `getAdminOverviewData()`; presentational client component `src/app/admin/admin-overview-2.tsx` (receives `data` prop, is now the DEFAULT 概览 — the temporary 概览2 nav item was removed). Overview content: KPI (注册/DAU·WAU·MAU/在线/累计成本; 累计生成图片·视频 split 对话流/工作流; 消耗积分; 成功率; 对话·工作流总数; 今日生成 split); 趋势分析 (活跃·新增 / 成本 / 生成图片 / 生成视频 — image·video are 对话流vs工作流 grouped bars — with a 今日/7日/30日/全部 range toggle that ONLY controls the 4 trend charts, positioned above them); 运营与产品分析 (功能使用分布, 模型调用占比, 生成成功率, 用户留存 次日/3/7/30, 转化漏斗, 人均指标, 积分健康度, 工作流采纳率, 新老用户占比, 对话生成模式占比, 参考素材使用, 上传成功率, 重试与安全改写, 审核拦截细分, 生成平均时长, 成本Top模型, 计费异常, 模型调用次数[调用/失败红色], 失败原因[全部从多到少], Top活跃/消耗用户).
- NEW analytics埋点 tables (this is the "连埋点一起做" the user approved): `GenerationEvent` (userId/requestId/kind/source[conversation·workflow·asset·agent, mapped from creditSource]/model/provider/status/failureReason/failureCode/moderation/durationMs/referenceImage·Video·AudioCount) and `UploadEvent` (userId/kind/status/reason[timeout·reencode·other]/bytes). Migration `prisma/migrations/20260705000000_analytics_events`. Writes via `src/lib/analytics-events.ts` — raw SQL (NOT typed Prisma client, because Windows locks the query engine so `prisma generate` often fails; same approach as GptImagePromptOptimizationCase), fire-and-forget, wrapped in try/catch so埋点 failure NEVER breaks generation/upload. `GenerationEvent` upserts on `(requestId, kind)` and a success can override a prior failed row.
- 埋点 insertion points: `image/route.ts` (success / empty-delivery / catch); `video/route.ts` (poll-success, poll-error, completed-without-url, create-returned-error, missing-task-id, and outer-catch ONLY when no taskId i.e. create stage — deliberately NOT recording transient poll network errors as failures); `asset-upload-temp/route.ts` POST (success/fail). Video SUCCESS does NOT record reference counts (poll body lacks them), so 参考素材使用 stat is image-dominant by design.
- IMPORTANT for the overview data: metrics that depend on the NEW埋点 (生成成功率, 上传成功率, 生成平均时长, 审核拦截细分, 失败原因, 模型失败次数, 参考素材使用) only have data from 2026-07-05 onward; they show "暂无数据 / 上线后统计" until events accumulate. Historical-capable metrics (累计生成图片/视频 + splits, generation trends) come from `MediaAsset` (image/video, `sourceKind NOT LIKE '%upload%'`, `workspaceKind='workflow'` vs else = conversation) so they are populated immediately. New-table reads in `admin-overview.ts` are wrapped in `safeRows()` so a missing table can't crash the page.
- Verify before deploy passed: `npx tsc --noEmit` clean, `eslint` 0 errors, `npm run build` passed. Deploy backup `.deploy-backups/20260705-overview-analytics/source-before-deploy.tgz`; snapshots `20260705-before/after-overview-analytics.json` compare `ok:true` (no diffs). See CHANGELOG top + 03-deploy-and-servers for the exact deploy commands used.


## Latest 2026-07-04 — tldraw License Gate Bypass + Workflow Fixes (DEPLOYED + PUSHED, commit 65737fa)

- Reply style this session: concise/direct Chinese. Debugging was done LIVE on production (user said late night, no users). Workflow entry was flipped `true` and rebuilt at the start. All changes below are deployed to Malaysia prod + Ali and pushed to GitHub as `65737fa`.
- DIAGNOSIS METHOD (for memory): temporarily instrumented `workflow-tldraw-canvas-inner.tsx` with a `window.__wfDebug()` editor introspector, lifecycle console logs, a MutationObserver, and custom tldraw `ErrorFallback`/`ShapeErrorFallback`; had the user run snippets in the browser console. Findings: WorkflowCanvas mounts ONCE, re-renders every ~1s (parent `now` ticker), NEVER unmounts; editor reports `renderingShapes:4` not culled/hidden; but `.tl-container` had only one child `<div data-testid="tl-license-expired" style="display:none">`. That testid pinpointed the tldraw license gate. ALL debug instrumentation was removed afterward (grep `wfLC|wfDebug|__wf` = clean).
- FIX 1 (the blocker): `patches/@tldraw+editor+5.1.1.patch` neutralizes `shouldHideEditorAfterDelay` (see What-Is-Current). Workflow canvas now stays visible in production.
- FIX 2: workflow selected-node input box no longer appears while a node is uploading. `WorkflowSelectedNodeOverlay` `showEditor` now also requires `node.data.uploadProgress === undefined` (`workflow-tldraw-canvas-inner.tsx` ~2149).
- FIX 3: empty-workflow (no nodes) UI redesigned (`workflow-tldraw-canvas-inner.tsx` ~3623). Removed the icon box + gray subtitle + single blue button. Now: gray `#8a8a8a` 22px title "从一个节点开始", then 4 horizontal black (`#111111`) rounded-`10px` `h-14` buttons with leading icons: 文字输入 (RiTextBlock), 图片节点 (RiImageAiLine), 视频节点 (RiFilmAiLine), 上传节点 (RiUpload2Line → triggers the dock upload input).
- FIX 4: workflow list "reorder-to-top on select" bug. `updateWorkflowCanvas` (`chat-workbench.tsx` ~10415) previously always set `updatedAt: Date.now()`, so opening a workflow that contains generated media re-derived `visualSize`/viewport and bumped it to the top of the list. Added `getWorkflowMeaningfulSnapshot()` (strips `viewport` and each node's `data.visualSize`); `updatedAt` now only changes when meaningful content (nodes/edges/text/media) changed. Rule confirmed: only real edits reorder to top.
- FIX 5: usage panel (top-right diamond) media counts. (a) Now EXCLUDES upload nodes — `getWorkflowMediaCounts` skips nodes whose `title` starts with "上传" — so counts match the generation-credits shown above. (b) Made CUMULATIVE: new `WorkflowItem.generatedMediaCounts { images, videos }`, incremented in `addWorkflowGeneratedAssets` (skips `silent` metadata-backfill; dedups new urls against existing workflow assets; seeds from current node count for pre-existing workflows). Deleting generated nodes NO LONGER decreases the count (statistic is monotonic, like credits). Display uses `activeWorkflow.generatedMediaCounts ?? getWorkflowMediaCounts(...)`. Persists via the existing `...workflow` spread in persist/normalize (same path as `usageSummary`).
- CONVERSATION-FLOW CHECK: `getSessionMediaCounts` only iterates assistant-message generated media (uploads live in user-side `uploadedImages`/`uploadedFiles`), so it already only counts generated media. Left as-is. NOTE: conversation counts are still node/message-derived (would decrease if a message were deleted); the cumulative treatment was only applied to workflow this session.
- Deploy method: per-file `scp` of changed source to `/var/www/flashmuse/src/...` + `/usr/local/bin/deploy-flashmuse-production.sh` (build + PM2 restart/save + Ali sync). For the license patch, also scp'd `patches/` + `package.json` and ran `npx patch-package` on the server before the build. `npx tsc --noEmit` passed locally before each deploy. Prod `.env.local` `NEXT_PUBLIC_WORKFLOW_MODE_ENABLED=true` (old value backed up to `.deploy-backups/20260704-workflow-open-debug-env.before`).


## Latest 2026-07-03 FULL DEPLOY — Workflow Entry OPENED Online + 24h Session Timeout (DEPLOYED + PUSHED)

- Deployed ALL previously-undeployed local source to Malaysia production and OPENED the online workflow entry (`NEXT_PUBLIC_WORKFLOW_MODE_ENABLED=true` appended to prod `.env.local` before build; verified `暂未开放` gone from built JS). Production workflow mode is now LIVE for users.
- Session idle-timeout changed to a flat 24h for all environments (`src/lib/auth.ts` + `src/lib/admin-auth.ts`); production is no longer 1h.
- Also shipped: workflow multi-select connect handle, initial `工作流_01` + cannot-delete-last-workflow, removed "还没有工作流" card, workflow success-toast `silent` refresh fix, recycle-bin trash count excludes expired items, `/workspace` client-only render (no chat flash on refresh), workflow preview thumbnails filtered like conversation.
- Deploy backup `.deploy-backups/20260703-workflow-open-24h/`; before/after snapshot compare `ok:true` (stableMissing/fallbackUsers 0, assetListHash unchanged); `/workspace` `/admin` `/api/model-availability` 200. Committed + pushed to GitHub. See CHANGELOG top entry.


## Latest 2026-07-03 Image Rate-Limit Hotfix (Deployed) + Local Admin Workflow Surfacing / Video Billing Hardening / Admin Read Optimizations

- User asked for concise Chinese replies; deploy deferred ("下次部署"). Only the image rate-limit hotfix was deployed this session (`src/lib/openrouter.ts` + `src/lib/error-message.ts`); everything else is local-only, uncommitted, unpushed. Nothing pushed to GitHub. `npx tsc --noEmit` passed after each change. See CHANGELOG 2026-07-03 top entry for full detail.
- DEPLOYED image rate-limit fix: production `B_296/B_308/B_315` were all the same `openai/gpt-5.4-image-2` upstream error `Request too large ... on tokens per min (TPM): Limit 180000000, Requested <small>` (OpenAI-side throttling, not oversized). `isTransientImageError` now retries these (2 retries via existing `createOneWithRetry`); `toUserErrorMessage` now maps them to "图片服务当前繁忙（限流），请稍后重试。" instead of the misleading 413 "请减少参考图" message. Deploy backup `/var/www/flashmuse/.deploy-backups/20260703-image-ratelimit-retry/`. Root cause (org rate limit/quota) still needs dashboard verification by the user; retries only cover transient spikes.
- LOCAL video billing hardening: `/api/video` now returns 400 `MISSING_REQUEST_ID` if `requestId` absent, and charges using `requestId` directly (was `body.requestId ?? taskId`), removing a latent double-charge path. Both existing callers already send a stable requestId.
- LOCAL admin work (all in the admin pages + `user-detail` API): (1) 生成记录 table now shows a 工作流 count column, combined image/video totals, upload columns removed; expanded detail adds 上传图片/视频/音频/文档 dialogs (deduped) and makes 工作流图片/视频 real. (2) 积分管理 separates workflow credits out of conversation into a new "工作流消耗积分详细" row + dialog (`workflowConsumedCredits`/`workflowCreditDetails`), reason labels added; totals already include workflow. (3) 用户管理 third column reworked to 历史对话 / 历史工作流(count) / 所有生成图片 / 所有生成视频 (combined conversation+workflow+asset, newest-first), removed 资产库图片 + 工作区保存.
- LOCAL admin read optimizations: added lightweight `media` mode (asset-states only) and `credits` mode (ledger only) to `user-detail` API; records-panel + users-panel media dialogs use `media`, credits-panel expand uses `credits`; dropped unused `workspace.state` from `records` mode; workflow media detection made robust via `mediaAsset.workspaceKind === "workflow"`.
- Admin credit model (for memory): conversation and workflow share the identical `chargeCredits` path and `/api/image` `/api/video` routes, differing only by `metadata.creditSource`. Charge is after-success; dedup by `(requestId, kind)` unique on `CreditLedger`; no refunds.


## Latest 2026-07-03 Local Workflow Layer-Panel, Menu, Drag, Tooltip, And Shortcut Iteration

- All changes this session are local-only in `src/components/workflow-tldraw-canvas-inner.tsx` (plus a whole-project popup-menu audit). Nothing committed, pushed, or deployed. Production workflow entry remains disabled. `npx tsc --noEmit` passed after every change. User asked replies to stay concise/direct in Chinese.
- Layer panel: renamed `历史记录` to `已删除的节点` (label, empty state, aria/title, resize separator). The bigger redesign the user first proposed (left panel = master node list, canvas-delete = hide, new left-side red delete-to-30-day-recycle-bin with confirm) was discussed and DEFERRED after listing its conflicts; only the rename was implemented. See 05-next-actions for the parked decision points (text/audio recoverability, real 30-day auto-clean, dedup one-to-many delete rule).
- Layer panel three-dot row menus: switched to horizontal `RiMoreLine`, and now close on outside click + are mutually exclusive with all other workflow menus via new shared hook `useWorkflowRowMenu()` (reuses the `workflow-close-popups` event bus + a capture-phase outside `pointerdown` close). Whole-platform menu audit found these were the only non-compliant menus; everything else (chat-workbench `closeAllPopupMenus`, workflow prompt-box bus, login dropdowns) already closes on outside click and is single-open.
- Layer<->canvas drag added. Dragging a `画布上的节点` row onto the canvas duplicates that node at the drop point (`duplicateWorkflowNodeToCanvas`, source kept). Dragging any `已删除的节点` row (image/video/text/persisted-asset) onto the canvas restores it; restored nodes append to the end of `nodes` so they appear as the first row in the reversed on-canvas list. New dataTransfer keys `application/x-flashmuse-workflow-history-media` and `-history-text`; `WorkflowAssetLayerRow` gained a `dragType` prop.
- Two drag bugs fixed: (1) duplicate needed `startLayerDrag` `effectAllowed="copyMove"` (was `"move"`, mismatched canvas `dropEffect="copy"` so drop was rejected); also added new drag types to shell `onDragOverCapture` and a `.workflow-layer-panel` guard in `handleWorkflowAssetDrop`. (2) One drag produced two copies because the canvas `drop` handler fires twice; fixed with a 200ms `lastHandledDropStampRef` time-window guard (exact-timeStamp guard failed, so it is two distinct events; the underlying double-dispatch cause is not yet identified). Restore paths only looked single because `restoreWorkflowAssetToCanvas` dedupes by existing same-URL node.
- Single-click a layer row now selects AND pans the node to screen center at the current zoom (`centerWorkflowNodeInViewport` / `editor.centerOnPoint`); double-click still zoom-focuses (unchanged).
- `WorkflowToolMenu` (select/hand) lost its persistent gray active-highlight; only hover gray remains.
- Added black-bg white-text hover tooltips (name + lighter shortcut) via new `WorkflowHoverTip` + `WorkflowTipButton` (the tip is suppressed on click until the pointer re-enters). Applied to bottom toolbar (文本输入 T, 图片节点 I, 视频节点 D, 上传节点, 缩小 Ctrl -, 放大 Ctrl +, 定位节点 Shift 1) and the four bottom-left buttons (画布背景/图层/小地图/缩放). The rightmost select `WorkflowToolMenu` has no tooltip (own flyout). Native `title` removed.
- Video-node keyboard shortcut moved from `V` to `D` (V now switches to the select tool). Updated keydown handler, video tooltip, and right-click menu `插入视频节点` shortcut.
- tldraw default shortcuts: confirmed no extra work needed. `<Tldraw hideUi>` means `useKeyboardShortcuts` (tldraw UI only) never mounts, so tool shortcuts (draw/eraser/shapes) + undo/redo/copy/paste/select-all/keyboard-delete are already off. Kept editor-core canvas controls (space-pan, arrow-nudge, Escape, Ctrl+/-/0, modifier tracking). A blanket key-suppressor was added then removed this session because it broke space-pan; do not re-add.

## Latest 2026-07-03 Full Deploy Of Workflow Code (Entry Disabled), Plus Later Local Success-Toast And Deleted-Media History

- This session: reviewed workflow code, fixed found issues, deployed ALL previously-undeployed code to production (workflow entry stayed disabled), synced GitHub, then continued local-only workflow work.
- Deploy: pushed commit `3455532 Deploy workflow code, connections, and GPT image safety rewrite`. Applied Prisma migration `20260701043000_gpt_image_prompt_optimization_cases` on server (DATABASE_URL taken from the FIRST `.env.local` line; the second DATABASE_URL line is malformed with a leading space and must be ignored). Deploy backup `.deploy-backups/20260703-workflow-code-full/source-before-deploy.tgz`. Post-deploy checks all passed (workspace/admin/model-availability 200, upload OPTIONS 204, new `/api/workflow-prompt-optimization/rewrite` 400 and `/cases` 401, snapshot compare safe: stableMissing/fallbackUsers 0). Production workflow entry still disabled (`NEXT_PUBLIC_WORKFLOW_MODE_ENABLED` absent). See CHANGELOG and 03-deploy-and-servers for full detail.
- Pre-deploy fixes shipped: removed accidental UTF-8 BOM before `"use client"` in `workflow-tldraw-canvas-inner.tsx`; added cancellation + hard ceiling to workflow `pollVideoNode` (`pollMountedRef`, `loadedWorkflowIdRef` check, `videoAbsoluteMaxPollAttempts=360`, stops instead of polling forever); simplified dead-code ternary in `getWorkflowNodePortPagePoint`; removed duplicate `prompt` key in `generateImageForNode`.
- AFTER the deploy, two local-only workflow changes were made and are NOT committed/deployed yet:
  1. Workflow generation success now shows the same green success toast as conversation flow. `addWorkflowGeneratedAssets` in `chat-workbench.tsx` now calls `notifyGenerationCompleteOnce(\`workflow:${nodeId}:${url}\`, "图片生成已完成" / "视频生成已完成")`, reusing the existing `ReminderToast` (green `#75d06a`, check-circle, `fixed` top-center), gated by the same `notifyOnGenerationComplete` user setting. The toast already renders at ChatWorkbench top level regardless of active panel, so no workflow-component render change was needed.
  2. Deleted workflow image/video nodes now go to the layer-panel `历史记录` even when they are NOT persisted DB workflow assets (previously image/video history only came from `workflowAssets`, so uploaded or not-yet-synced images like the one showing fallback name "[Image 1]" vanished on delete). Added `historicalMediaNodes` to `WorkflowCanvasState`; `addHistoricalNodes` (replacing the 4 `addHistoricalTextNodes` call sites) now captures deleted image/video nodes too; `normalizeState` parses and persists it; the layer panel renders these as `WorkflowAssetLayerRow` history rows (thumbnail + 三点菜单 恢复进画布/删除), de-duplicated by URL against `workflowAssets`-based `historicalAssets` and against on-canvas media. Restore re-adds the node and removes it from history; delete only removes the local history record (no physical DB delete, matching soft-delete rule and text-node history). Audio upload nodes are intentionally NOT captured yet (`WorkflowAssetSummary` kind is only image/video). `npx tsc --noEmit` passed after each change.

## Latest 2026-07-03 Local Workflow Connection, Input Upload, Prompt, And Mention Iteration

- User asked replies to stay concise/direct in Chinese. Current local worktree remains dirty with many uncommitted changes. No deploy, commit, or GitHub sync was performed. Production workflow entry must remain disabled unless user explicitly asks to open it. `npx tsc --noEmit` passed after the latest local workflow and conversation input edits.
- Workflow connection highlight/selection was iterated in `src/components/workflow-tldraw-canvas-inner.tsx`. The accepted direction is: selected workflow node highlights all directly connected lines blue `#2f80ed` without selecting those lines and without an external overlay. This is implemented through a small local highlighted-node subscription plus `sourceNodeId/targetNodeId` metadata on `workflow_connection` shapes. tldraw selection indicators for connection shapes remain hidden. Drag-box selection should not keep connection lines selected; line click should still select one line and show the disconnect scissors.
- Workflow connection behavior changed further: generated image/video node success now automatically removes all incoming edges to that generated node. The node keeps its output media and can still be used as a source for later connections. Closing a connected-input thumbnail in a target prompt box also disconnects the corresponding edge and removes the matching `@文件名` text.
- Workflow input prompt boxes now treat connection media as read-only upload cards. Incoming image/video/audio nodes appear in the target image/video prompt box as thumbnails, count toward model upload rules, hide upload buttons when limits are reached, participate in validation, and are sent to `/api/image` or `/api/video` like normal uploads. Incoming video/audio references are now included in workflow video generation requests. Input-local uploads from the prompt box no longer attach directly to the target node; they create or restore an upload node on the left of the current target node, avoid overlap, connect it to the target, and then appear as connected thumbnails. Duplicate file names on the current canvas are reused and connected; matching historical image/video assets are restored and connected. Bottom-dock `上传节点` remains standalone and does not auto-connect.
- Workflow text connections were clarified. Text node content is not an upload; it is appended to the target generation prompt after the target prompt-box text. The final prompt order is target prompt-box text first, connected text-node content after. Text connection only has the combined prompt length limit. Connecting a text node is rejected if target prompt text plus existing connected text plus new text would exceed `MAX_WORKFLOW_PROMPT_LENGTH` (`2000`). If text nodes are connected first, the target prompt editor only allows the remaining character count and shows `输入框和连接文本合计最多2000字`. Generation also rechecks the combined length. A target prompt box send button is enabled if either its own text is non-empty or connected text nodes contain text.
- Workflow generated image/video success now writes the full prompt back to node `data.prompt`, not only to asset `sourcePrompt`. This keeps future workflow right-click `使用提示词` complete after incoming text connections are removed on success. Conversation flow was checked: generated media already saves per-item `imagePrompts` / `videoPrompts`, `generationMeta.originalPrompt` / `itemPrompts`, and asset `sourcePrompt`; there is no equivalent missing text-node source in conversation flow.
- Workflow prompt-box thumbnails now display `@文件名` consistently and clicking inserts `@文件名`. The editor mention highlighter now includes connected-input thumbnails in `validReferenceNames`, so inserted connected names render blue like normal upload/reference mentions. The same full-mention popup bug was fixed in conversation and workflow editors: when deleting the trailing space after a complete `@文件名`, the cursor at the end of a valid full mention no longer opens an empty reference-asset popup.
- Conversation flow `使用提示词` was fixed in `src/components/chat-workbench.tsx`. When using a prompt that contains `@文件名`, the input now restores matching asset thumbnails/cards too, not just the text. Covered entrances include message `使用提示词`, Agent media prompt panel `使用提示词`, and preview-side `使用提示词`. It restores image cards from asset library or conversation references, and video/audio/document file cards from assets when possible.
- Local `B_177` was investigated and can be ignored for local workflow testing. It was a local workflow Seedance Fast video request in `工作流_04` where BytePlus returned `content[4].video_url resource not found` for reference video `video_2_w4`. Root cause is provider cannot access the local/generated video URL from BytePlus cloud. Test real video/audio references after deployment with provider-accessible media.
- Workflow image/video prompt placeholders were unified to `输入提示词，也可以连接文本节点`. Text-node placeholder was not changed.

## Latest 2026-07-02 Production Media Error Cleanup Hotfix, B_246/B_257 Diagnostics, And Local Workflow Connection Iteration

- User asked to keep replies concise/direct in Chinese. Current local worktree remains dirty with many uncommitted changes. Local `npx tsc --noEmit` passed after the latest workflow connection edits. Production workflow entry must remain disabled unless user explicitly asks to open it.
- Diagnosed production `B_257`: user `ID_523106`, conversation `5de9299b-e3b9-4140-b9d2-56e0e18e7e23`, request `a7c9c737-08bc-4395-afbc-0f36e4704173:video:0`, task `cgt-20260702150552-p7cz8`, model `byteplus:video.seedance-2-0`, prompt included `保留原音乐`, references were 2 images and `下载.mp4`, with `referenceAudioCount=0`. The deployed B_254 auto-review fix worked: reference images were Active assets, reference video was created/reviewed as `asset-20260702150547-zxgw2`, and task creation succeeded. Polling later failed with BytePlus `OutputAudioSensitiveContentDetected`, so this was generated-output audio moderation, not uploaded audio and not a B_254 regression.
- Diagnosed production `B_246`: user `ID_963115`, conversation `2a52bda5-4f98-4c75-999d-f2f127e6c293`, request `9bbb04eb-0519-47a2-aa46-f584feb288ad:image:0`, model `byteplus:conversation-image.seedream-4-5`, no reference images. Upstream returned `InputTextSensitiveContentDetected` for a sexualized prompt. Existing user-facing text incorrectly said reference image/privacy, even though `referenceImageCount=0`; this error-message mapping should be corrected later.
- Fixed and deployed a narrow production hotfix for stale media failure red text after successful retry. The issue was seen after `B_246`: first failed card produced red `(B_246)` text, then admin lifted restriction and user retry succeeded, but old red text remained under the success card. Only production `src/components/chat-workbench.tsx` was patched, with backup `/var/www/flashmuse/.deploy-backups/20260702-clear-media-error-on-retry-success/chat-workbench.tsx.before`. The fix clears matching `mediaErrorReasons` and `error` when image/video failed-slot retry succeeds, avoids preserving stale media reasons when `failureCount === 0`, and hides media red text unless a visible failed image/video card exists. Production `npx tsc --noEmit`, build/deploy, PM2 restart/save, Ali sync, `/workspace` 200, and `/api/model-availability` 200 passed.
- Checked workflow for the stale red-error/success coexistence issue. Workflow success paths already clear `error`, and workflow cards render mutually by `isRunning`, `error`, then media result, so no equivalent production workflow hotfix was needed.
- Local-only workflow connection work continued in `src/components/workflow-tldraw-canvas-inner.tsx`. After checking tldraw Workflow starter kit docs/source, the correct direction is to use tldraw custom connection shape + binding utilities, not just default arrows or external SVG. Local code now has a `workflow_connection` custom shape, `workflow_connection` binding type, port helpers, edge-to-binding rebuild, and binding-to-`WorkflowCanvasState.edges` synchronization. Business rules remain text -> image/video, image -> image/video, audio -> video with self/duplicate/unsupported/cycle rejection.
- Current local connection visuals: stored line is a tldraw custom connection shape; line endpoints attach to node edges. Normal width/color `5px #6f7782`; hover `10px #6f7788`; selected `10px #454e5a`; tldraw default blue indicator hidden. A `28px` transparent hit path enables hover. Center disconnect button appears only on line hover/selection, fixed at the curve midpoint, as a black `100px` round button with horizontally flipped white scissors; clicking deletes the connection. Node drag handles are separate: `60px` blue/white concentric circles, `10px` away from node, visible only on hover/selection or as input target during dragging, with a short hover bridge and about `40px` hit/absorb range.
- Right-output drag path should remain the baseline: dashed temporary curved line, two white endpoints, one endpoint following mouse, release on valid empty image/video node creates `edges` business edge and tldraw bindings. A left-input reverse-drag path was added after an initial broken attempt was reverted: it should create the same business edge when dropped on a valid source node and use the same temporary visual direction. This is not yet accepted and must be browser-tested carefully. If right drag visual or creation regresses, restore right-output behavior first before continuing left-input dragging.
- No GitHub sync or commit was performed. Do not deploy local workflow connection/GPT/upload-node changes until user accepts browser behavior.

## Latest 2026-07-02 Local Workflow Upload/Connection Iteration And Production B_254 Hotfix

- User asked to keep replies concise/direct in Chinese. Current local worktree remains dirty with many uncommitted changes. `npx tsc --noEmit` passed after the local workflow/API edits in this session.
- Local-only workflow upload-node follow-up in `src/components/workflow-tldraw-canvas-inner.tsx` and `src/components/chat-workbench.tsx`: bottom-dock `上传节点` file input now supports multiple file selection. Dropping files directly onto workflow canvas now uses the same upload-node path instead of conversation input upload. Workflow drag overlay text now shows upload-node limits: images `jpg/jpeg/png/webp <=5MB`, videos `mp4/mov <=50MB and 2-15s`, audio `mp3/wav <=15MB and 2-15s`, text `.txt <=2000` chars. Multiple uploaded nodes are selected together and the viewport focuses the whole batch, like selecting all new nodes then pressing the focus button. Empty-canvas right-click menu also gained `上传文件` after insert text/image/video, using the same hidden upload input as the dock; future toolbar functions of this kind should be mirrored in the blank-canvas context menu.
- Local-only workflow connection iteration in `src/components/workflow-tldraw-canvas-inner.tsx`: user approved the interaction where selected/hovered nodes show blue circular connection handles. Output handles appear on the right for nodes with usable content/result: non-empty text, generated/uploaded image/video, and uploaded audio. Input handles appear on the left for empty image/video nodes. Dragging a right handle starts a temporary blue curved dashed line; releasing on a valid target creates a business edge in `WorkflowCanvasState.edges`. Valid first-version connections are text -> image/video, image -> image/video, and audio -> video; self, duplicate, unsupported type, and cycle connections are rejected with black toast. User preferred no toolbar/right-click connect button.
- Local connection implementation is still not visually settled and should be treated as unfinished. Several attempts were made: first custom SVG overlay drifted during zoom/pan; then tldraw native `arrow` + `defaultShapeUtils/defaultBindingUtils` was added for stable stored line display while keeping custom connection handles. Current local code uses native `arrow` shapes with `meta.workflowEdgeId` generated from business `edges`, plus a temporary SVG line while dragging. User reported the result was still not matching the desired previous curved-handle interaction. Next AI should inspect the current diff before continuing; likely direction is either fully restore the custom curved overlay but put it in the correct tldraw coordinate layer, or properly integrate tldraw arrow visuals while preserving the exact handle-to-handle curved UX. Do not assume the current local connection state is final.
- Investigated production `B_254`. Logs showed user `ID_523106`, conversation `5de9299b-e3b9-4140-b9d2-56e0e18e7e23`, request `b4c87f9e-f7f0-413c-abb3-709070295f3e:video:0`, model `byteplus:video.seedance-2-0`, reference mode `reference`, 2 reference images plus 1 reference video `下载.mp4`. BytePlus returned HTTP 400 `InputVideoSensitiveContentDetected.PrivacyInformation` with message `The request failed because the input video may contain real person`. The existing auto-review flow only reviewed images; both images were reviewed to Active, but the retry still failed because the reference video was not reviewed.
- Production hotfix deployed only `src/app/api/video/route.ts` for `B_254`. Backup: `/var/www/flashmuse/.deploy-backups/20260702-b254-video-audio-review/route.ts.before`. Production `npx tsc --noEmit` passed, `npm run build` passed with existing Turbopack/NFT broad-file warnings, PM2 `flashmuse` restarted online, PM2 state saved, and Ali `_next/static` synced. Post-deploy checks returned `200` for `https://main.venusface.com/workspace` and `https://api.venusface.com/api/model-availability`.
- The deployed `B_254` fix expands BytePlus auto review in `/api/video` from image-only to image/video/audio references. The helper now creates BytePlus assets with `AssetType: Image`, `Video`, or `Audio`, reuses existing Active asset IDs across all reference types, and retries video creation with reviewed `asset://...` values for images, videos, and audios. Diagnostics now include video/audio asset replacement counts. Other local workflow/GPT/upload-node changes were not deployed.

## Latest 2026-07-01 Local Workflow GPT Rewrite Follow-Up, Admin Display, Failed Card Layout, And Upload Nodes

- Continued local-only workflow work. No deploy, commit, or GitHub sync was performed. `npx tsc --noEmit` passed after the latest code changes. Production workflow entry must remain disabled unless user explicitly asks to open it.
- Fixed GPT image rewrite success-case attempt counting. Previous logic saved only the current clicked retry round's successful attempt index, so if the user clicked `AI改写重试3次`, all three failed, then clicked `AI改写重试3次` again and the first attempt succeeded, admin showed `3 次` instead of the correct cumulative `4 次`. `src/components/workflow-tldraw-canvas-inner.tsx` now counts prior unique optimized attempt prompts excluding the original prompt and saves `previousImageAttempts + currentAttempt`. The local existing case `gptopt_ef0cdda0-102e-4a30-b241-4f309880c821` for `12424740@qq.com / ID_779117` was manually corrected from `3` to `4`.
- Updated admin `GPT生图优化` display in `src/app/admin/page.tsx` and new client component `src/app/admin/admin-gpt-image-thumbnail.tsx`. The table width was aligned to the other admin tables at `min-w-[1180px]`. Columns are now `尝试次数 / 原提示词 / AI成功提示词 / 信息 / 缩略图`; the information column contains media name, parameters, time, user, and optimizer model. Thumbnail hover preview uses a portal and computes browser-boundary-aware placement so the large image is not clipped and does not cover the thumbnail. Admin query now joins `MediaAsset + UserAssetState` for system/current name and media parameters.
- Reworked workflow failed-card display. It is now logically split into top-left failure title, middle retry/AI rewrite controls, and bottom red error text. Internal text remains fixed screen-size relative to tldraw zoom. When the card becomes too small, it hides the middle controls first, then hides bottom red error text, and finally keeps only the top-left failure title to avoid overlap.
- Added workflow bottom-dock `上传节点` entry after the video-node button in `src/components/workflow-tldraw-canvas-inner.tsx`. It accepts image, video, audio, and `.txt` files. Upload creates a transparent in-canvas uploading node first with a fixed-screen-size progress bar and percent. Image upload uses existing workflow image upload with progress; text reading uses progress and now auto-decodes UTF-8 with GB18030/GBK fallback to avoid Chinese TXT mojibake; video/audio currently use existing `/api/upload-file` fetch path, so progress is start/finish rather than true streaming upload unless that path is later converted to XHR.
- Workflow upload nodes now render as real nodes: `上传图片` uses `RiImageLine`, `上传视频` uses `RiVideoOnLine`, `上传文本` uses `RiTextSnippet`, and new `audio` workflow node kind uses `RiVoiceprintLine` with title `上传音频`. Uploaded image/video nodes use real media dimensions and existing generated-media proportional resize rules. Uploaded text nodes reuse text-node behavior and remain editable/resizable. Uploaded audio nodes are fixed default size `500x200`, white with the same `20px` radius and `5px #b8b8b8` border as text nodes, and show a native audio player; no right-side size is shown.
- Workflow upload-node validation is local and deliberately strict. Images allow `jpg/jpeg/png/webp`, max `5MB`, to remain compatible with the strictest common image model path. Text is limited to `2000` characters. Videos allow `mp4/mov`, max `50MB`, `2-15s`, and reuse Seedance-style dimensions/aspect/pixel checks. Audio allows `mp3/wav`, max `15MB`, `2-15s`. Count is not limited for upload nodes because each file becomes its own node.
- Workflow upload-node persistence now reuses the same `/api/media-assets` POST pattern as conversation upload instead of a new path. Uploaded images use `currentCategory: "conversation_uploads"` so they appear in the asset library `上传图片`. Uploaded videos/audio/text use `workflow_upload_videos`, `workflow_upload_audios`, and `workflow_upload_documents` so they are stored but not shown in the user-facing upload-image category. `/api/media-assets` was extended to allow these workflow upload categories and to persist `originalFileName`, `mimeType`, and `fileSize`; upload metadata is also written into `sourceDetail`.

## Latest 2026-07-01 Local Workflow GPT Image Safety Rewrite Retry First Version

- User approved a mini project for workflow-only `GPT-5.4 Image 2` failures where the upstream model refuses to generate or returns no image with safety-style text. This first version is local only and not deployed or committed yet.
- Implemented eligible failed-card UX in `src/components/workflow-tldraw-canvas-inner.tsx`: the original bottom red error stays visible; the center area explains that safety/privacy/minor/intimate/likeness issues may be refused; buttons `AI改写重试3次`, `AI改写重试5次`, and `AI改写重试10次` trigger controlled retry. The middle explanation is centered as a block, left-aligned inside, uses `information-2-line`, and says `由AI尝试安全改写后重试`.
- Implemented rewrite retry flow. Each attempt calls new backend rewrite API, then existing `/api/image`; attempts avoid duplicate prompt wording and keep previous failed attempt prompts on the node while failure continues. On success, the node uses the successful optimized prompt for actual generation/persistence, and normal media/credit/workflow asset flow remains unchanged.
- Added backend rewrite endpoint `src/app/api/workflow-prompt-optimization/rewrite/route.ts` and helper `rewriteGptImagePromptForSafety()` in `src/lib/openrouter.ts`. Fallback order is `openai/gpt-5.5`, `openai/gpt-5.4`, then `byteplus:chat.seed-2-0-pro`. The prompt explicitly frames this as compliant safety rewriting, not bypassing review. After user testing, the rule was changed from broad safety rewriting to minimal-patch editing: first attempts should add only a short phrase such as `穿日常连衣裙` or `穿着得体`, preserve reference image face and clothing, and avoid changing scene/action/outfit unless later attempts require slightly stronger wording.
- Added successful-case persistence through new table `GptImagePromptOptimizationCase`, API `src/app/api/workflow-prompt-optimization/cases/route.ts`, admin tab `GPT生图优化`, and migration `20260701043000_gpt_image_prompt_optimization_cases`. Local `npx prisma migrate deploy` applied the migration successfully. `npx prisma generate` was attempted but blocked by a running local Node/Prisma engine lock on Windows; code uses raw SQL for the new table so TypeScript and runtime do not depend on regenerated Prisma model access yet.
- During local testing, a stuck waiting-card bug was diagnosed and fixed: `attemptedPrompts.push(...)` mutated a frozen workflow/tldraw array and threw `TypeError: Cannot add property 4, object is not extensible`, leaving `isRunning=true`. The retry loop now uses immutable array replacement and an outer catch to clear running state.
- Local OpenRouter diagnostics showed direct requests may return `403 This model is not available in your region` from China, while curl fallback can fail with Windows `schannel` TLS errors. This is expected to be local-only; production Malaysia should not have the same region issue. Future UX should avoid letting curl fallback network errors mask the clearer region-unavailable reason.
- Other local fixes in this same work: workflow asset-reference image format validation now falls back to URL extension when asset names have no suffix; real uploads fall back to MIME extension; shared `getFileExtension()` was corrected; tldraw default snap indicator color is now `#00ff00`; workflow failed-card red error text wraps before clipping.
- Added dedicated handover doc `handover/08-gpt-image-prompt-optimization.md` with status, important files, cautions, current minimal-patch rules, known local issues, and second-phase plan. `npx tsc --noEmit` passed after the code changes.

## Latest 2026-06-30 Upload Reliability, Video Poll Recovery, And Manual Video Restore

- User asked to diagnose high image upload failure rate, then implement two fixes and deploy directly. Production workflow entry stayed disabled. Local code is now dirty with deployed but uncommitted changes in `src/app/api/asset-upload-temp/route.ts`, `src/app/api/upload-image/route.ts`, `src/components/chat-workbench.tsx`, and `src/components/workflow-tldraw-canvas-inner.tsx`; next AI should inspect `git status --short`, `git diff`, and run `npx tsc --noEmit` before any commit or further deploy.
- Diagnosis: recent upload diagnostics showed most app-level image upload failures were `temporary-upload-jpeg-needs-reencode`, meaning the server intentionally rejected some JPEGs on the first pass and expected a retry with `forceReencode=1`. Nginx also showed `499` and `408` upload failures, consistent with China-to-Malaysia upload path interruptions. Ali static was not the bottleneck; the unstable segment is user browser to Malaysia API.
- Deployed upload reliability fix. Conversation input and asset-library image uploads now automatically retry once with `forceReencode=1` when the first upload error contains `转码`, instead of surfacing the first failure to the user. Workflow image upload already had this retry pattern and was kept aligned. Production browser uploads now default to `https://api.venusface.com/api/asset-upload-temp` unless `NEXT_PUBLIC_UPLOAD_BASE_URL` is explicitly set; cross-origin uploads use `/api/upload-token` Bearer auth. `/api/asset-upload-temp` and legacy `/api/upload-image` CORS allow `https://ali.venusface.com`, `https://static.venusface.com`, `https://main.venusface.com`, and `https://api.venusface.com` by default. Production CORS preflight from Ali to API returned `204` with the expected allow-origin/header/methods.
- User then reported a failed Seedance video in account `ID_636611`, conversation `bd20879c-44b4-4760-91a8-83c4b0441ce3`, message `fdb7d980-c06d-4ac9-af30-6473003d222e`, request `2538a032-2874-4f6c-b51c-299e056dbdbc`, task `cgt-20260630212727-nnttf`. Root cause was not a model failure: BytePlus task creation succeeded and polling was `running`, but the 2026-06-30 21:29 PM2 restart during the upload-fix deploy caused one `/api/video` poll to return `502`; the old frontend treated that transient poll interruption as `请求失败，请稍后再试。`, with no `B_xxx` because the backend never saw a provider failure.
- Deployed video poll recovery behavior for conversation and workflow. Once a video `taskId` exists, transient poll failures such as network errors or HTTP `408/409/425/429/500/502/503/504` no longer flip the UI to a failed card; the waiting/running state is preserved and polling continues. Explicit provider states `failed`, `error`, or `expired`, missing task id, invalid parameters, auth/credit errors, and completed-without-url still become failures. Conversation poll failure handling now preserves backend `errorCode` by prefixing `(B_xxx)` when `/api/video` returns `errorCode` separately. Workflow video polling uses the same transient-error waiting behavior and keeps the node running instead of clearing `taskId` on recoverable poll errors.
- Manually recovered the interrupted video. Queried BytePlus task `cgt-20260630212727-nnttf` and found `status=succeeded` with a valid `video_url`. Downloaded it to `/generated/users/ID_636611/videos/1782829256422-78469703-e341-4bd2-b59d-c7bcaf44ce0b.mp4`, generated poster `/generated/users/ID_636611/video-posters/1782829256422-78469703-e341-4bd2-b59d-c7bcaf44ce0b.jpg` with `ffmpeg-static`, synced both to Ali, updated `WorkspaceMessage` and `WorkspaceSession.messagesJson`, cleared `error/mediaErrorReasons`, set `failedVideoCount=0`, `pendingVideoCount=0`, added `videoPosters[videoUrl]`, and upserted `MediaAsset + UserAssetState` in `conversation_videos` with system name `video_142059`. Public video and poster URLs returned `200` on both main and Ali. Backup of the pre-recovery row/session was written under `.runtime/manual-fixes/*-recover-video-cgt-20260630212727-nnttf-before.json`.
- Production deploy backups for the two narrow deploys: `.deploy-backups/20260630-upload-retry-api-base` and `.deploy-backups/20260630-video-poll-recovery-upload-base`. Both production deploys passed `npx tsc --noEmit`; build passed with only existing Turbopack/NFT broad-file warnings; PM2 restarted online; Ali `_next/static` synced. Workflow code changed but `NEXT_PUBLIC_WORKFLOW_MODE_ENABLED` remained unset/false.

## Latest 2026-06-30 Full Production Deploy, Workflow Code Online But Entry Disabled, And GitHub Sync

- User asked to deploy all current local changes including admin/backend/workflow code, keep production workflow entry disabled, then upload to GitHub. Full production deployment was performed from the current local source snapshot.
- Deployed changes include the conversation-flow BytePlus Seedance explicit `参考模式` menu, official Seedance upload-rule correction, editable admin upload-rule overrides, backend upload-rule enforcement, local-development 24-hour auth timeout split with production still 1 hour, and the accumulated workflow code/UI/generation-chain updates. Workflow code is now deployed, but the production entry remains disabled because `NEXT_PUBLIC_WORKFLOW_MODE_ENABLED` is still unset/false.
- Production deploy details: local and production `npx tsc --noEmit` passed. The first production validation failed before build because server `node_modules` did not yet contain `tldraw`; ran `npm install` on production, then TypeScript/build passed. `npm run build` passed with only the existing Turbopack/NFT broad-file warnings, PM2 restarted online, PM2 state was saved, and Ali `_next/static` sync completed.
- Production backup before deploy: `.deploy-backups/20260630-full-seedance-upload-rules/source-before-deploy.tgz`. Source archive uploaded to `/tmp/flashmuse-20260630-full-seedance-upload-rules.tgz`.
- Deploy guard snapshots: before `.runtime/deploy-checks/20260630-before-full-seedance-upload-rules.json`, after `.runtime/deploy-checks/20260630-after-full-seedance-upload-rules.json`. Compare returned `ok: false` only because live data changed during deploy: `workspaceMessages +2`, `userAssetStates +2`, `visibleActiveAssets +2`, with changes under users `ID_193006` and `ID_955937`; `stableMissingInNewTable` stayed `0` and `fallbackUsers` stayed `0`, so this looked like normal concurrent user generation rather than a deployment data regression.
- Post-deploy checks passed: `https://main.venusface.com/workspace` 200, `https://main.venusface.com/admin` 200, `https://api.venusface.com/api/model-availability` 200, `/api/asset-upload-temp` OPTIONS 204, PM2 `flashmuse` online. Production `.env.local` still did not enable `NEXT_PUBLIC_WORKFLOW_MODE_ENABLED`.
- Next priority is browser focus testing online: conversation Seedance `参考模式`, selected-mode upload restrictions, `/api/video referenceMode`, admin upload-rule save/refresh, and online BytePlus human-reference review with provider-accessible media. Workflow entry should remain disabled unless user explicitly asks to open it.

## Latest 2026-06-30 Local Conversation Seedance Mode Menu, Workflow Failure UI, And Generation Chain Parity Follow-Up

- User continued local-only work and asked replies to stay concise/direct in Chinese. No production deploy, GitHub sync, or commit was performed. The next AI should first list all currently uncommitted/undeployed changes, then user plans to ask for deployment so they can focus-test the new conversation-flow Seedance reference-mode feature online.
- Checked BytePlus official docs page `https://docs.byteplus.com/en/docs/ModelArk/1520757` through the doc API because the public page is JS-rendered. Confirmed only `Multimodal reference-based video generation / 融合模式` supports reference video/audio. `Image to video - first frame / 首帧模式` and `Image to video - first and last frames / 首尾帧模式` support images only, and the three scenarios are mutually exclusive.
- Updated `src/lib/upload-rules.ts` and `src/app/admin/admin-upload-rules-panel.tsx` so BytePlus Seedance `首帧模式` and `首尾帧模式` show/treat video/audio as unsupported. `融合模式` keeps image/video/audio support. Admin fallback text now matches the official rule.
- Refined workflow Seedance video-node mode UI in `src/components/workflow-tldraw-canvas-inner.tsx`. `融合模式` uses `image-circle-line`, `首帧模式` uses `image-2-line`, and `首尾帧模式` uses `multi-image-line`. `首帧模式` shows one image upload button labeled `首帧` with no `1-1`; uploaded image occupies that slot and send stays disabled until one ready image/reference exists. `首尾帧模式` shows `首帧` and `尾帧` slots with no `1-2`; uploaded images occupy those two slots and send stays disabled until two ready images/references exist. Other workflow modes/models keep the previous layout: upload buttons before uploaded cards, and buttons disappear only when their own max count is reached.
- Added workflow input-local black tips above the current selected prompt box instead of using only the parent workspace toast. Workflow input-box upload/limit/pre-submit messages now appear above that input box. Added hover-to-large-image previews for workflow upload image cards, workflow reference-popup thumbnails, and workflow layer/history thumbnails.
- Improved workflow failed/waiting cards. Failed/waiting internal text and icons are fixed screen-size relative to tldraw zoom. Failed-card `修改后重试` now actually retries the image/video node generation and stops event propagation. Failed cards now show the stored error text inside the lower-left of the card, fixed size, clipped inside the card, and using the unified `toUserErrorMessage` path with `(B_xxx)` preserved for new backend-coded failures.
- Fixed workflow error-code and video creation chain issues. Workflow `readJson` now extracts `error`, `error.message`, and `errorCode`, prefixes `(B_xxx)` when needed, and uses `GENERIC_MEDIA_ERROR_MESSAGE` for media fallback. Workflow video creation now mirrors conversation flow for BytePlus human-reference review: first `reviewing` response shows the review notice and retries with `autoBytePlusAssetReview=true` instead of locally failing with `视频任务创建失败`. Local `B_162` was diagnosed as a local-only BytePlus auto-review URL access problem: first-frame image triggered human/privacy review, then BytePlus failed to download the local image via `main.venusface.com` because the object was not present online. This should be retested only after deploy with online-accessible media.
- Brought key workflow generation-chain behavior closer to conversation flow. Workflow image/video requests append the same `参考图顺序...` reference hint, workflow image generation treats empty `images` as failure instead of silent success, workflow video applies BytePlus image reference truncation by selected mode before request, checks first-frame/first-last-frame minimums before request, and carries create-stage usage into video polling success when poll response lacks usage.
- Added explicit conversation-flow Seedance reference-mode menu in `src/components/chat-workbench.tsx`. In conversation video mode, when selected model is `byteplus:video.seedance-2-0` or `byteplus:video.seedance-2-0-fast`, the input toolbar shows a `参考模式` button with `融合模式 / 首帧模式 / 首尾帧模式`. Default is `融合模式`. The menu is wider and shows gray support descriptions: `融合模式` supports `1-9 张图片，1-3 个视频，1-3 个音频`; `首帧模式` supports `1 张首帧图片`; `首尾帧模式` supports `2 张图片：首帧和尾帧`. The input width expands only for these two BytePlus Seedance models and returns to the old width for other video models.
- Removed the old conversation-flow prompt-word-triggered Seedance mode behavior for new sends/replays/Agent-or-General-to-video paths. Conversation flow now sends `/api/video referenceMode` from the explicit menu selection, not from prompt words like `首帧` / `首尾帧`. Upload rules and submit validation also use the selected mode: fusion allows image/video/audio, first-frame requires at least one image and disallows video/audio, first-and-last-frame requires at least two images and disallows video/audio.
- Local validation after these changes: `npx tsc --noEmit` passed repeatedly. Browser retesting is still needed before deploy, especially the new conversation Seedance reference-mode menu, upload button support changes after switching modes/models, prompt send disabled/blocking behavior, backend request `referenceMode`, and workflow failed/waiting card behavior.

## Latest 2026-06-30 Local Upload Rules, Workflow Upload Validation, Session Split, And BytePlus Workflow Mode Work

- User continued local-only work and asked replies to stay concise/direct in Chinese. No production deploy, GitHub sync, or commit was performed. Production workflow entry remains disabled.
- Read current handover docs first. Current worktree already had local workflow/handover changes. This session continued on top and did not revert unrelated changes.
- Changed local-vs-production auth timeout rule in code and handover. `src/lib/auth.ts` and `src/lib/admin-auth.ts` now keep local development (`NODE_ENV=development`) idle timeout at 24 hours, while production (`NODE_ENV=production`) remains 1 hour unless the user explicitly changes the online rule. This environment split is intentional and should survive future deploys.
- Aligned workflow upload validation with conversation upload validation in `src/components/workflow-tldraw-canvas-inner.tsx`. Workflow upload now checks format, size, per-kind count, video/audio single duration, video/audio total duration, video dimensions, video aspect ratio, video total pixels, upload-in-progress/error state, and audio-only video requests before generation. Workflow uploaded video/audio metadata is stored on upload items for later validation. Local validation passed with `npx tsc --noEmit`.
- Added editable upload-count configuration above the existing backend `上传规则` fallback table in `src/app/admin/admin-upload-rules-panel.tsx`. The new table writes to local `.env.local` through new API `src/app/admin/api/upload-rules/route.ts` and `UPLOAD_RULE_OVERRIDES`. The old text table remains a fallback/reference only. Supported upload cells show a count input plus switch; unsupported cells show `不支持`. GPT-5.4 Image 2 file input is now treated as not implemented and displays/acts as unsupported.
- Upload rule priority is now: editable `UPLOAD_RULE_OVERRIDES` first, then static fallback from `src/lib/upload-rules.ts`. `src/lib/system-settings.ts` reads/writes/sanitizes the overrides, `/api/model-availability` returns them to the workspace, `ChatWorkbench` uses them for conversation upload controls, asset-generation upload controls, and submit-time validation, and `WorkflowCanvas` uses them for workflow upload controls and generation-time validation.
- Backend generation routes now also use the editable upload rule overrides. `/api/chat` validates uploaded image count for general/agent chat requests; `/api/image` validates reference image count for conversation/asset image generation; `/api/video` validates image/video/audio reference counts through the same override table. This prevents frontend/backend count mismatch after admin changes.
- Reworked BytePlus Seedance upload-rule rows. `Seedance 2.0` and `Seedance 2.0 Fast` are unified in the admin upload-rule table and split by mode instead: `融合模式`, `首帧模式`, and `首尾帧模式`. Both BytePlus video models share these three configuration keys. Official-doc follow-up confirmed only `融合模式` supports reference video/audio; `首帧模式` and `首尾帧模式` support images only. Default fallback image counts are fusion/reference `9`, first-frame `1`, first-and-last-frame `2`; video/audio fallback counts are `3` each only in fusion/reference mode.
- Added workflow video-node reference-mode selection for BytePlus `Seedance 2.0` / `Seedance 2.0 Fast`. In workflow video-node prompt boxes, when either BytePlus Seedance model is selected, a menu appears before the send button with `融合模式 / 首帧模式 / 首尾帧模式`. The selected mode is persisted on `WorkflowNodeData.videoReferenceMode`, changes the visible upload buttons/count labels immediately, prunes excess uploads when switching to stricter modes, and is sent to `/api/video` as `referenceMode` during generation. New video nodes default to `融合模式`. Latest local UI rule: `首帧模式` shows one image button labeled `首帧` with no `1-1` count and disables send until one ready image/reference exists; `首尾帧模式` shows two image buttons labeled `首帧` / `尾帧` with no `1-2` count and disables send until two ready images/references exist.
- Workflow prompt-box menus now close when clicking or focusing another non-menu area inside the same workflow input box, including the text editor/upload area. This specifically aligns the workflow input-box menu behavior with the conversation input behavior while preserving menu-internal clicks. Local validation after the latest changes: `npx tsc --noEmit` passed.
- Browser retesting is still needed for the new admin upload rule table save/refresh behavior, conversation/asset/workflow upload count updates after admin changes, backend rejections matching UI limits, BytePlus Seedance three-mode rows, workflow video mode menu persistence, mode-based upload pruning, and workflow input-menu close behavior.

## Latest 2026-06-29 Local Workflow Prompt Input, Upload Cards, Asset Ordering, And Layers Follow-Up

- User continued local-only workflow mode work and asked replies to stay concise/direct in Chinese. No production deploy, GitHub sync, or commit was performed. Production workflow entry remains disabled.
- Reworked selected image/video node prompt-box placement in `src/components/workflow-tldraw-canvas-inner.tsx`. The prompt box now stays in the workflow canvas coordinate system instead of mixing page/sidebar offsets. It keeps the bottom edge 8px above the workflow dock, grows downward when there is room, then keeps its bottom fixed and expands upward when close to the dock. It no longer jumps above the node; overlap with the selected node is allowed. Left/right clamping now uses the canvas viewport so sidebar expand/collapse does not change prompt-box edge distance.
- Updated workflow upload cards. Uploaded image/video/audio/file cards now match upload-button size (`64x70`). Audio/file cards use white background and gray border, centered larger icon, and bottom `@文件名` text aligned to the same position/size as image/video cards. Video upload cards show a centered play-triangle overlay. The remove `X` is a separate black circular button outside the clipped card container so it is fully visible; clicking remove also deletes the corresponding `@文件名` from the prompt.
- Fixed workflow upload persistence/display issues. Successful image/video uploads no longer persist stale `blob:` preview URLs; after upload success `previewUrl` is cleared and durable `url` is preferred. Workflow image upload now mirrors conversation upload behavior more closely: XHR upload progress, 90-second timeout, `POST /api/asset-upload-temp` then `PATCH /api/asset-upload-temp`, and automatic `forceReencode=1` retry when the server says the JPEG needs re-encoding. Workflow upload progress overlay now matches the conversation circular percent overlay.
- Improved workflow upload/model behavior. Upload buttons show supported count ranges such as `1-9` / `1-3` from the current model upload rule. Switching image/video models prunes already-attached uploads that the new model no longer supports and removes their `@文件名` mentions from the prompt. Video duration menu options are sorted descending by seconds, highest at top and lowest at bottom.
- Improved workflow external file drag/drop. Dropping files onto workflow now uses an `onExternalFilesDrop` callback to route through the outer `ChatWorkbench` upload path, and a capture-phase window `drop` / `dragend` cleanup prevents the global `在此处拖放文件` overlay from getting stuck when tldraw swallows drop events.
- Fixed workflow `@引用资产` behavior. Selecting an asset-library image from the workflow reference popup now adds the image card to the prompt upload area and inserts `@文件名`, matching conversation mode. It avoids duplicate cards and respects the current model image-reference max count.
- Fixed asset-library/reference ordering side effects in `src/components/chat-workbench.tsx`. Moving an asset to another category now updates local `createdAt/updatedAt` so the moved asset stays at the top immediately. The move action no longer marks filters unloaded and triggers an immediate refetch that could race before backend `sortOrder` persisted. Opening conversation/workflow reference popups no longer reorders global `assets`; `loadMentionAssetFilters()` updates existing asset records in place and appends only missing assets, so returning to the asset library keeps its order. Workflow reference assets are built directly from the asset-library filters (`character_image`, `scene_image`, `shot_image`, `conversation_uploads`) instead of a mixed mention-group pass.
- Improved workflow layer-panel selection sync. tldraw selected node state is now mirrored to React `selectedNodeId` through a narrow `editor.store.listen()` that only syncs selection and does not trigger full-store autosave. Selecting a node on canvas highlights the corresponding row in `画布上的节点`. Non-empty text rows now correctly show lock/eye controls on hover because the row has the required `group` class.
- Current local validation after these changes: `npx tsc --noEmit` passed. Browser retesting is still needed for prompt-box boundaries with sidebar expand/collapse, upload card visuals/removal/refresh, external drag-drop overlay cleanup, workflow image JPEG re-encode retry, model-switch upload pruning, reference asset popup ordering/card insertion, asset-library ordering after moves and popup open/close, and layer-row selection/control sync.

## Latest 2026-06-29 Local Workflow Context Menu, Layers Panel, Input Box, And Sidebar Iteration

- User continued local-only workflow work and asked replies to stay concise/direct in Chinese. No production deploy, GitHub sync, or commit was performed. Production workflow mode remains disabled.
- Reworked workflow custom right-click menu in `src/components/workflow-tldraw-canvas-inner.tsx`. Menu items now use Remix icons. Empty canvas right-click shows insert actions `插入文本节点 / 插入图片节点 / 插入视频节点` with shortcuts `T / I / V`, plus `画布放大`, `画布缩小`, `显示全部节点`, `粘贴`, and `全选` only when valid. Disabled items are hidden rather than greyed out.
- Added keyboard shortcuts for workflow canvas: `T` inserts text node, `I` inserts image node, `V` inserts video node, `Ctrl +` zooms in, `Ctrl -` zooms out, and `Shift 1` shows all nodes. Shortcuts are ignored in inputs/contenteditable/IME composition. `V` no longer switches to select tool in workflow because it now inserts a video node.
- Node right-click menu is now content-sensitive. Empty nodes do not show the media/text-specific middle section. Text nodes with content show `复制文字` and `下载` to `.txt`. Generated image nodes show `使用提示词`, `导出 > 导出 SVG / 导出 PNG / 导出 JPG`, and `下载`. Generated video nodes show `使用提示词`, `导出 > 导出首帧 / 导出尾帧 / 导出当前帧`, and `下载`. `导出当前帧` uses saved `videoCurrentTime`.
- Added right-click `锁定/解锁` as the first node-only menu item using tldraw action `toggle-lock`. Added `使用提示词` for generated image/video nodes, using the same `RiTBoxLine` icon as conversation preview. It creates a new same-kind node near the source node and preserves prompt including `@` names, uploads, model, ratio, resolution, and video duration, but not generated media/result state.
- Improved custom export/download helpers. Image `导出 JPG` converts the current source image to JPEG with white background. Video frame export captures first/last/current frame through canvas. Download still downloads text as `.txt`, image source, or video source.
- Reworked the workflow `图层` panel. Clicking outside no longer closes it; only the top-right `X` closes it. The `历史记录` and `画布上的节点` section headers now share lighter text/icon styling. Historical rows and canvas rows use matching height, font size, color, hover background, and left indentation.
- Added a draggable divider between `历史记录` and `画布上的节点`, matching the conversation document-preview resize style: a visible darker 1px line with a small center pill and transparent drag hit area. Dragging is real-time with no height transition during resize, the divider is the visual boundary, and the history content area no longer leaves white dead space above it.
- Changed history item actions from `移入` buttons to a three-dot menu with `恢复进画布` and red `删除`. Text history delete removes the text history entry from current workflow state only; database records/files are not physically deleted. Image/video history delete calls `/api/media-assets` `PATCH { delete: true }`, so it follows the existing asset soft-delete/recycle-bin path.
- Added text-history de-duplication. When non-empty text nodes enter `historicalTextNodes`, identical text content is stored only once; new duplicate content replaces the older entry. Existing duplicate history rows are also de-duplicated during state normalization. Comparison trims outer whitespace and normalizes CRLF to LF.
- Added layer-row controls for content nodes. Generated image/video rows and non-empty text rows show lock and eye controls on row hover. Locked rows keep the lock control visible; hidden rows keep the closed-eye control visible. Because lock and hide were interfering, the controls are mutually displayed: locked state shows only lock/unlock, hidden state shows only show/hide. Hiding uses tldraw's official `getShapeVisibility` hook, and lock/hidden state is persisted on `WorkflowNodeData.isLocked` / `isHidden`.
- Changed workflow mode first-entry sidebar behavior in `src/components/chat-workbench.tsx`: during the same login/session, the first time a user enters workflow mode the left sidebar is forced visible and collapsed; after that, user expand/collapse choices are respected. Logout clears the sessionStorage marker so next login again collapses on first workflow entry. Repeated workflow entry also no longer refetches the same workflow assets after the first successful load for that workflow.
- Improved selected image/video node input-box boundary behavior. The input box now clamps to the tldraw viewport, moves above or upward when near the bottom, avoids overlapping the bottom workflow dock with about 8px visual spacing, and measures its real height with `ResizeObserver` so long prompts do not push it out of the browser. The outer panel no longer scrolls; only the middle prompt editor scrolls.
- Workflow input prompts now reuse the conversation input black toast pipeline. `WorkflowCanvas` accepts `onShowTip`, and `ChatWorkbench` passes `showInputTip`. Workflow upload/type/limit errors and `最多输入2000字` now show via `ReminderToast` instead of red inline text inside the input box.
- Current local validation after these changes: `npx tsc --noEmit` passed. Browser retesting is still needed for all new menu variants/actions, keyboard shortcuts, layer divider resizing, history delete/restore, lock/hide persistence and interactions, first-workflow-entry sidebar collapse, input-box viewport clamping with long prompts, and black toast placement in workflow mode.

## Latest 2026-06-29 Local Workflow Custom Context Menu And Default Snapping

- User continued local workflow work. No production deploy was performed. The accumulated local source-code changes through this workflow/custom context-menu/snapping and diagnostics work were committed and pushed to GitHub as `0f4c97c Implement workflow canvas updates and diagnostics`; production workflow mode remains disabled.
- Investigated the workflow right-click menu bug: opening the menu, then clicking blank canvas to close it, made the next right-click look ineffective until browser focus left and returned. Selecting a menu item such as copy did not trigger the bug. Temporary local diagnostics wrote `.runtime/workflow-context-menu-debug.jsonl`; the diagnostic API and frontend probe were removed afterward, but the runtime log file may still exist locally and must not be committed.
- Root cause found from logs: after blank-click close, `editor.menus` was already clear and later `contextmenu` events reached `img` / video overlay targets, but Radix/tldraw default `DefaultContextMenu` did not reopen. Remounting `DefaultContextMenu` fixed reopening but was rejected because it remounted the canvas and caused visible multi-second lag/refresh-like behavior.
- Final local direction: workflow no longer uses tldraw default `DefaultContextMenu`. `workflowTldrawComponents` sets `ContextMenu: null`, and `WorkflowCustomContextMenu` is opened from the workflow shell `onContextMenu`. The custom menu is `180px` wide and includes equivalent actions: cut, copy, paste, duplicate, delete, bring/send layer actions, copy/export SVG/PNG, custom `下载`, and select all. It uses tldraw `useActions()` where possible and direct editor APIs for delete/select-all/download.
- Workflow nodes now add `data-workflow-node-id` so right-clicking a node selects that workflow node before opening the custom menu. The custom menu avoids the old Radix/tldraw context-menu state conflict caused by complex HTML custom shapes (`img`, `video`, textarea, overlays, buttons) inside `workflow_node`.
- Enabled tldraw default snapping/reference lines locally for workflow by calling `editor.user.updateUserPreferences({ isSnapMode: true })` in `handleMount`. This uses tldraw's built-in bounds/center snapping and snap indicators; no custom snapping geometry was added.
- Current local validation after code changes: `npx tsc --noEmit` passed before and after the source-code commit. Browser retesting is still needed for the custom workflow context menu opening/closing repeatedly on text/image/video/blank areas, all menu actions, no browser native menu overlap, no canvas lag, and default snapping/reference-line behavior while dragging workflow nodes.

## Latest 2026-06-29 Local Workflow tldraw Page Disable, Text Persistence, Workflow_04 Recovery, Cursor, Ratio, And Media Metadata Fixes

- User continued local-only workflow work. No production deploy, GitHub sync, or commit was performed. Production workflow mode remains disabled.
- Investigated tldraw `移动到页面 / 新页面`: it is tldraw's multi-page whiteboard action (`MoveToPageMenu` / `move-to-new-page`) that creates a new tldraw page and moves selected shapes. It is not a FlashMuse workflow move. Since FlashMuse workflow uses one workflow = one canvas, workflow `<Tldraw />` now sets `options={{ maxPages: 1 }}` so the menu item is hidden.
- Repaired local `ID_779117 / 工作流_04` after the page-move accident. Current row id is `cmqutwz2b03r9w6q8lw94v1t4`, workflow id `7677e646-d066-43a8-8de9-78e2006965c9`, code `w4`. Canvas now contains `image_1_w4` node `workflow_node_df3c4dea-c1fc-44e1-b1ee-6bdaea15a744`, `video_1_w4` node `workflow_node_f1eeb05c-6876-4e88-9343-7ff257b40474`, and the newly tested text node `workflow_node_ef89108c-9916-4454-967a-6b6be641fd7f`.
- The text node lost during the tldraw page-move accident could not be recovered from DB: it was not in `canvasJson.nodes`, `canvasJson.historicalTextNodes`, `UserWorkspaceState`, or any scanned workflow JSON. After the fixes, a newly typed `工作流_04` text node was confirmed saved in DB at `WorkspaceWorkflow.canvasJson.nodes[].data.text` and `data.prompt`.
- Strengthened text persistence. Text edits update `WorkflowCanvasState.nodes[].data.text/prompt`, and `ChatWorkbench` now detects text-node content snapshots and performs a 250ms workflow-text-specific `/api/workspace-state` save. Non-empty text nodes that are deleted or otherwise disappear from the current tldraw page are also copied into `canvasJson.historicalTextNodes` as a safety net.
- Fixed video node cursor by changing the transparent video overlay from `cursor-move` to `cursor-default` and preventing browser drag-start on video/overlay. User confirmed the disabled cursor icon disappeared.
- Fixed restored image ratio handling. `image_1_w4` `MediaAsset` correctly stored `ratio=16:9`, but the canvas node had been overwritten to `ratio="2848:1600"` from actual pixel dimensions. Added ratio normalization for workflow image nodes and asset restore; generation parameter ratio stays one of the supported model ratios, while true size stays in `imageDimensions` / `visualSize`. Current `image_1_w4` node was repaired to `ratio=16:9`, `resolution=2K`, `imageDimensions=2848x1600`.
- Improved workflow media metadata persistence. Workflow generated media persistence now carries `model`, `settings.ratio`, `settings.resolution`, `settings.duration`, `dimensions`, `durationSeconds`, prompt, `workflowId`, `workflowNodeId`, poster, and stable names. `/api/media-assets` now accepts `durationSeconds`. Workflow video nodes read browser `loadedmetadata` and write `videoDimensions` / `durationSeconds` back to both node state and `MediaAsset` through `onGeneratedMedia`.
- Added a name-preservation guard so workflow canvas `mediaSystemNames` participate in `reserveWorkflowMediaSystemNamesForItems()`, preventing metadata re-posts for an existing URL from allocating a new name like `video_2_w4`.
- Current known limitation: existing `video_1_w4` has generation parameters and poster/URL in DB, but its real `width/height/durationSeconds` are still null until the browser loads that video metadata. The physical local mp4 was not found in this repo and local `ffprobe` was unavailable. Local validation after code changes: `npx tsc --noEmit` passed.

## Latest 2026-06-28 Local Workflow Layers Panel, History, Reorder, Recovery, And Context Menu Fixes

- User continued local-only workflow work. No production deploy, GitHub sync, or commit was performed. Production workflow mode remains disabled.
- Fixed a destructive tldraw default action issue: right-click `展平/Flatten` converted custom workflow nodes into ordinary tldraw image shapes, which are not persisted in `WorkflowCanvasState`; after refresh the image disappeared. The workflow canvas now removes `flatten-to-image` from tldraw actions while keeping other default actions.
- `工作流_04` local data was manually recovered during debugging. For local user `ID_779117`, `image_1_w4` was restored from persisted `MediaAsset + UserAssetState` into `WorkspaceWorkflow.canvasJson.nodes`; `MediaAsset.workflowNodeId` was updated to the restored node id. Temporary recovery scripts were deleted afterward.
- Made the workflow layer panel history real instead of relying on already-loaded asset filters. Added `GET /api/media-assets?workflowId=...`, returning current user's non-deleted/non-hidden persisted workflow assets for that workflow. `ChatWorkbench` now loads these assets on workflow panel entry/switch, so generated assets deleted from the canvas can appear in history after refresh.
- Reworked the left `图层` panel. The lower section label is now `画布上的节点` with `RiNodeTree`. History is collapsible; collapsed history shrinks to the header so `画布上的节点` moves up. Empty history shows the three node icons horizontally and text `暂无历史记录` / `删除后的节点在这里`.
- Layer display rules now match user requirements. Empty nodes show only their correct bottom-toolbar icon (`RiTextBlock`, `RiImageAiLine`, `RiFilmAiLine`) plus small text; no white icon box. Text nodes with content show the text preview truncated with ellipsis. Generated image/video nodes replace the empty row with the thumbnail and file name only. The list order mirrors canvas z-order, with topmost canvas layer displayed first.
- Layer interactions were added. Single click on empty-node rows or generated asset rows selects the canvas node. Double-click selects and focuses/zooms to it. Dragging rows in `画布上的节点` changes real canvas layer order by calling tldraw's built-in `bringToFront` API; a black horizontal line indicates the exact insertion position before dropping.
- History rules were defined and implemented. Empty nodes deleted from canvas do not enter history. Generated image/video nodes deleted from canvas enter history via persisted workflow assets. Text nodes with non-empty content deleted from canvas are stored in `WorkflowCanvasState.historicalTextNodes` and can be restored with `移入`; empty text nodes do not enter history. Image/video history assets can be restored by `移入` or by dragging from history to the canvas.
- Right-click/download was rechecked. The `下载` context-menu action was kept and re-added using the tldraw default `DefaultContextMenu` shell plus `DefaultContextMenuContent` and `TldrawUiMenuActionItem`. The likely root cause of the “right-click opens only once” bug was not download itself but interactive text/video children marking right-click events as handled. Text and video event handlers now skip `stopPropagation` / `editor.markEventAsHandled()` for right-click/context-menu events; video's top drag overlay only selects on left-click. Video cursor was set back to normal `default` instead of showing disabled cursor.
- Local validation after latest changes: `npx tsc --noEmit` passed. Browser retesting is still needed for repeated right-click on all workflow node types, `下载`, default right-click actions, video cursor/native controls, history loading/restoring, non-empty text-node deletion history, and layer drag-reorder persistence.

## Latest 2026-06-28 Local Workflow Text Input Node, Resizing, Context Menu, Export, And Download Work

- User asked to continue local-only workflow work, keep replies concise/direct in Chinese, and then update handover. No production deploy, GitHub sync, or commit was performed in this local workflow session. Production workflow mode remains disabled.
- Changed workflow text nodes from AI text-generation nodes into pure `文本输入` nodes in `src/components/workflow-tldraw-canvas-inner.tsx`. Text nodes now act as editable saved text boxes for long prompts/scripts, unrelated to any model. The old text-node model menu/generation UI was removed locally. Downstream image/video nodes still read the text node's current `data.text` as upstream prompt context.
- Implemented tldraw official editing-state behavior for text input nodes. Text nodes set `canEdit=true`: single click/drag selects and moves the node, double-click or Enter enters editing mode, Escape/canvas click exits editing. Editing mode allows typing, paste, selection, copy, delete, and internal scrolling. Double-clicking a text input node also auto-focuses/zooms to that node.
- Text input node UI is now white with rounded `20px`, persistent `5px #b8b8b8` gray border, default size `720x480`, title `文本输入（双击进入编辑模式）`, Remix icon `RiTextBlock` in the node header and bottom toolbar, and placeholder `大段提示词或者剧本可以输入到这里...`. Text scrollbars use a dedicated always-visible scrollbar class `yinzao-workflow-text-scroll`: visible when content overflows, absent when not needed.
- Changed workflow tldraw selected outline thickness by replacing only the selection foreground overlay with a workflow-local subclass of `SelectionForegroundOverlayUtil` using `lineWidth: 3`. This changes visible blue selection stroke only; hit/interaction geometry is unchanged.
- Added generated image/video node resizing. Before generation, image/video nodes are not resizable. After generation, image/video nodes can be resized proportionally only. Maximum size is the natural/real result size; minimum size is the same aspect ratio with longest edge `256px`. Resized size is stored in `WorkflowNodeData.visualSize`, survives refresh, and the selected-node top-right size label updates live from the current shape size. Regenerating media clears old `visualSize`.
- Changed workflow video playback to not loop. Native video controls remain clickable; video now stops at the end. Local saved video nodes record `videoCurrentTime` on time update/pause/seek/end. Export behavior for video nodes: remote temporary video URLs export the poster only; saved local `/generated/...` videos export the frame at recorded `videoCurrentTime`, falling back to poster, then a placeholder.
- Investigated tldraw right-click menu via official docs. The right-click context menu is tldraw's default `ContextMenu`; menu commands are tldraw actions. To avoid broken persistence from actions mutating only the tldraw store, workflow now uses official `editor.sideEffects.registerAfterCreateHandler`, `registerAfterChangeHandler`, and `registerAfterDeleteHandler` for `workflow_node` shapes. This syncs create/change/delete/reorder actions back into `WorkflowCanvasState` without reintroducing full `editor.store.listen -> onChange` autosave. Pasted/copied workflow nodes are normalized so their inner `node.id` matches the new tldraw shape id and does not duplicate the source node id.
- Added `toSvg()` for workflow custom shapes so tldraw export/copy image commands no longer output empty boxes for custom HTML shapes. Text nodes export as SVG text with white background and gray border. Image nodes fetch the image and embed it as a data URL before SVG export. Video nodes export the saved local current frame when possible, otherwise poster as data URL, otherwise a video placeholder.
- Added a workflow context-menu download action using tldraw official `actions + TldrawUiMenuActionItem` pattern rather than a raw custom menu item. The default context menu remains, and a `下载` action is appended below it. It downloads selected single workflow node content: text nodes as `.txt`, image nodes as image source files, and video nodes as video source files. This was adjusted after a raw custom menu item made the right-click menu unstable after one open.
- Local validation after these changes: `npx tsc --noEmit` passed. Browser retesting is still needed for text editing/moving, resize bounds, right-click context menu repeat opening, copy/paste/delete/reorder persistence, export PNG/SVG for text/image/video, video current-frame export after local save, and context-menu download for text/image/video.

## Latest 2026-06-28 Production Diagnostics, Upload Timeout, And Upload Failure Investigation

- User asked to keep replies concise/direct in Chinese, diagnose whether Ali static server was slow, diagnose `B_164` / `B_167`, then add detailed logs and deploy directly. Multiple narrow production deploys were performed. Local workflow/tldraw work remains not intentionally deployed and production workflow entry remains disabled.
- Network diagnosis found Ali static server was not the bottleneck. From local tests, `ali.venusface.com` / `static.venusface.com` were fast: Ali ping around `8ms`, no packet loss, static mp4 download around tens of MB/s, and Ali server load was low (`load average` about `0.03`, Nginx active, disk about 20%). Malaysia main server resources were also healthy, and local app access on the server was about `8ms`, but public access from China to `main.venusface.com` / `api.venusface.com` was slow and sometimes lossy (`main` ping about `221ms`, observed packet loss, `/workspace` several seconds). Conclusion: Ali static was fine; China-to-Malaysia public path is the slow part for dynamic HTML/API/upload.
- Investigated `B_164` and `B_167`. They were `image-generation request failed`, not video, not Ali static, and not upload/reference-image related. `B_164` was persisted for `312876953@qq.com / ID_686996`, conversation `945cbe20-05a8-43e0-bea4-c748feac2d18`, request `a116552d-e015-48cd-b8d2-b792452124dc`, model `openai/gpt-5.4-image-2`, `referenceCount=0`, user-facing error `(B_164) 请求失败，请稍后再试。`. `B_167` existed in PM2 error logs in the same continuous image-failure batch but was not separately persisted in `WorkspaceMessage`, likely overwritten/not saved by later state. Nearby successful `openai/gpt-5.4-image-2` image requests took about `100-160s`, so the likely cause was OpenRouter/image-model upstream instability or long-request failure, not Ali speed or obvious app logic.
- Added production generation diagnostics in `src/lib/generation-diagnostics-log.ts`, writing `.runtime/generation-diagnostics-log.jsonl`. It is intentionally non-blocking and redacts tokens/API keys. It records `requestId`, `userId`, conversation IDs, provider/model, status, settings, prompt length/hash/preview, summarized references, upstream status/body snippets, timings, error details, and save-job metadata. It avoids storing full signed remote URLs by summarizing host/path tails.
- Wired generation diagnostics into `src/lib/openrouter.ts`, `src/app/api/image/route.ts`, `src/lib/openrouter-video.ts`, `src/app/api/video/route.ts`, and `src/lib/media-save-queue.ts`. Covered text/Agent/intent provider requests, OpenRouter/BytePlus image requests, image route success/failure/empty delivery, OpenRouter/BytePlus video create/poll success/failure/no-url paths, media-save queue/download/saved/failed events, and inline image save events. Deployment was narrow: only logging-related files were uploaded. Production backup: `.deploy-backups/20260628162851-generation-diagnostics`. Local and production `npx tsc --noEmit` passed; production build passed with existing Turbopack/NFT broad-file warnings; PM2 stayed online; `/workspace`, `/admin`, and `/api/model-availability` returned 200.
- Investigated conversation-flow image upload failures. Found three categories: network/upload path interruptions (`408` timeout and `499` client closed in Nginx, related to China-to-Malaysia path), JPEG encoding needing server re-encode (`图片编码需要转码，请点击重试。` from `jpegNeedsReencode()`), and generic upload failures/500 where previous server logs were insufficient.
- Added upload diagnostics in `src/lib/upload-diagnostics-log.ts`, writing `.runtime/upload-diagnostics-log.jsonl`. Wired `src/app/api/asset-upload-temp/route.ts` and `src/lib/local-assets.ts` to log upload start, auth, form/file receipt, file name, MIME, size, `forceReencode`, missing file/token, temporary buffer save, JPEG-needs-reencode detection, ffmpeg re-encode start/success/failure, commit/delete, status, timings, and server errors. Deployment was narrow: only upload-related files were uploaded. Production backup: `.deploy-backups/20260628164248-upload-diagnostics`. Local and production `npx tsc --noEmit` passed; production build passed with existing warnings; PM2 stayed online; `/workspace` returned 200 and `/api/asset-upload-temp` OPTIONS returned 204.
- User asked whether the upload-card display was unified. The conversation input uploaded-image card previously showed white text `上传失败` for all upload error types. Changed only the production-current `src/components/chat-workbench.tsx` conversation-flow image-card UI so if `image.error` contains `上传超时`, the card white text shows `上传超时`; otherwise it still shows `上传失败`. Asset-library upload modal was not changed. Deployment was narrow from a production file copy, not local dirty workflow code. Production backup: `.deploy-backups/20260628170012-upload-timeout-label`. Production build passed, PM2 online, `/workspace` and `/api/model-availability` returned 200.
- User then saw upload stuck at 95%. Confirmed frontend XHR progress caps upload progress at 95 until the server responds; existing `xhr.timeout` was `10 * 60 * 1000`, so timeout label could take 10 minutes. User asked to change to 90 seconds. Changed `uploadFormDataWithProgress()` in production-current `src/components/chat-workbench.tsx` to `xhr.timeout = 90 * 1000`, and also made the same local edit. Deployment was again narrow from a production file copy to avoid shipping local workflow/tldraw changes. Production backup: `.deploy-backups/20260628171000-upload-timeout-90s`. Production TypeScript/build passed with existing warnings; PM2 online; `/workspace` and `/api/model-availability` returned 200.
- Checked the user's recent upload failures after the new upload diagnostics. For `12424740@qq.com / ID_636611`, recent logged failure at `2026-06-28T09:13:39Z` was file `微信图片_20260621163713_225_428.jpg`, `image/jpeg`, `730605` bytes, `forceReencode=false`, event `temporary-upload-jpeg-needs-reencode`, then `asset-upload-temp-post-failed`, message `图片编码需要转码，请点击重试。`; this is an image encoding/re-encode path, not network. The next later Nginx-only failure at local time `17:15:05` was `POST /api/asset-upload-temp` status `408`, which is upload timeout/network path and did not reach app logging.
- Important for future work: local worktree remains dirty with prior local-only workflow/tldraw changes plus these deployed logging/UI changes. The production deploys in this 2026-06-28 session intentionally used narrow file uploads/production file copies to avoid deploying unrelated local workflow changes. Before any commit or broad deploy, inspect `git status --short`, `git diff`, and recent commits carefully, then run `npx tsc --noEmit`.

## Latest 2026-06-27 Local Workflow Input, Upload, Text Node, And Model-Control Iteration

- User asked to continue local-only workflow work and keep replies concise/direct in Chinese. No production deploy, GitHub sync, or commit was performed in this session. Production workflow mode remains disabled.
- Read current handover docs first. Current local working tree already had uncommitted workflow/handover changes from 2026-06-26. This session continued on top of those changes and did not revert unrelated files.
- Fixed and iterated workflow `@` mention behavior in `src/components/workflow-tldraw-canvas-inner.tsx`. The goal was to keep the workflow prompt/upload layout, but make all non-upload input behavior match conversation mode. Work included better contenteditable caret/selection helpers, mention deletion, blue real `@资产名` rendering, manual `@` popup/filtering, replacing typed `@query` with selected asset, and suppressing stale `@` popup reopening after asset insertion. Important bug found: `focusRequest` could repeatedly reapply after an asset insert and pull the cursor back after the first mention; a handled-key guard was added so focus requests run once.
- Fixed refresh/asset-load mention rendering behavior. Workflow now proactively loads reference assets when a prompt contains `@`. To avoid visible “one by one turns blue” behavior, if an input contains `@` and reference assets are still idle/loading, the editor temporarily shows a loading row instead of black raw text; after assets load, mentions render blue in one pass.
- Reworked workflow upload display to match conversation input cards while preserving the workflow upload button area. Image and video uploads show thumbnail/video preview cards at the upload-button position with a black bottom gradient and clickable `@文件名`; audio and file uploads show an icon plus `@文件名`; uploading items show a progress bar; every upload card has a top-right `X` remove button. Upload status updates now use a ref-backed latest `uploads` list to reduce stale overwrites when multiple uploads progress concurrently.
- Corrected workflow upload button visibility and `accept` rules based on actual backend/code support, not only the backend table display. `getWorkflowUploadAccept()` now emits both file extensions and MIME types. Unsupported upload kinds are hidden rather than disabled. For video nodes, workflow upload rules use `server-url` mode so BytePlus Seedance video nodes can show image/video/audio support. Document/file upload is hidden for image/video nodes because `/api/image` only accepts `referenceImages`, not documents/files.
- Clarified and implemented actual workflow text-node document support. Conversation mode supports readable documents by reading text client-side and appending it to the user message; it is not model-native file input. Workflow text nodes now support only actually readable document files: `md`, `txt`, and `csv`. Files are read with `FileReader`, stored on `WorkflowNodeData.uploads[].text`, shown as upload cards, and text-node `/api/chat` requests append a `已读取文档内容如下...` context block just like conversation mode. PDF/DOC/DOCX/XLS/PPT remain not implemented for workflow because there is no parser path.
- Fixed workflow text-node model control. Previously workflow text nodes used the full `frontendConversationModels` list and did not honor backend model switches. `/api/model-availability` now also returns `chatModels` and `chatModelProviders` for the backend group `对话 / Agent 规划 / 意图识别` (normal `Seed 2.0 Lite`, advanced `GPT-5.4`). `ChatWorkbench` stores `enabledAgentChatModelIds` / `agentChatModelProviders` and passes them into `WorkflowCanvas`; workflow text nodes now use that Agent chat group, not the `通用模式` group. Text model menus preserve backend order and show BytePlus icons when the provider is `byteplus`.
- Reworked workflow text-node generation output. `/api/chat` can return Agent JSON such as `{ "intent": ..., "content": ... }`; workflow now extracts `content`/`displayText` before saving `outputText`, and display sanitization also handles old JSON-like output. Text-node requests add output instructions: Chinese only, no code/code blocks/commands/JSON/Markdown tables, first line a short Chinese title without `#`, structured short paragraphs/lists, optional light symbols/emoji, and support for `**加粗**`, `[blue]...[/blue]`, `[red]...[/red]`.
- Replaced raw text-node output rendering with a workflow-local formatted display modeled on conversation `FormattedMessage`: first short line can become a title, paragraphs and bullet/labeled list items get spacing and typography, blue/red callouts and bold spans render visually, code fences/backticks/JSON wrappers are filtered, and text cards have internal vertical scrolling.
- Investigated text selection inside tldraw custom text nodes. Earlier event interception allowed selecting text poorly and broke node dragging. Checked tldraw official `Clickable custom shape` guidance: custom interactive children need `pointer-events: all` and stopped propagation, but broad interception breaks shape dragging. Current local fix narrows interaction handling to the inner text content layer and leaves the outer card/edge as tldraw drag area; this still needs browser retesting for both text selection/copy and node dragging.
- Files touched in this session include `src/components/workflow-tldraw-canvas-inner.tsx`, `src/components/workflow-tldraw-canvas.tsx`, `src/components/chat-workbench.tsx`, and `src/app/api/model-availability/route.ts`, plus current handover docs. Local validation after the latest changes: `npx tsc --noEmit` passed.
- Still needs browser retest before commit/deploy: workflow `@` insertion/caret/popup closing, refresh showing blue mentions without visible transition, upload cards and progress, upload type filtering per node/model, text-node `md/txt/csv` file reading and prompt context, text-node model menu using Agent chat backend group, text-node formatted output without JSON/code, internal text scrolling, selecting/copying generated text, and dragging text nodes from non-text areas.

## Latest 2026-06-26 Local Workflow Input, Upload, Reference, And Credit Iteration

- User asked to continue local-only workflow work and keep replies concise/direct in Chinese. No production deploy or GitHub sync was performed during this session; production workflow entry remains disabled.
- Fixed workflow usage/credit display. Root cause: workflow `onCredit` received API `credit.chargedCredits`, but `activeWorkflow.usageSummary.credits` was updated from `usage` only, so the right-top usage button showed `0`. `src/components/chat-workbench.tsx` now merges `chargedCredits` into workflow usage immediately. `src/lib/workspace-workflows.ts` now reloads workflow usage summaries from `CreditLedger` rows where `workspaceKind="workflow"`, so refresh does not keep stale `0` usage.
- Reworked the selected workflow-node input in `src/components/workflow-tldraw-canvas-inner.tsx`. The old compact textarea became a taller prompt box with an upload area above the editable text. The four upload chips are `图片 / 视频 / 音频 / 文件`, with default gray chip backgrounds. Icons are fixed by user decision: image uses Remix `image-line`, video uses `video-on-line`, audio uses `voiceprint-line`, and file/document uses `file-text-line`.
- The workflow prompt editor was changed from a plain `<textarea>` to a contenteditable editor modeled on conversation mode. It supports blue `@资产名` rendering, `Enter` to generate, `Shift+Enter` for newline, max prompt length `2000`, and max editor height `500px`. A tldraw/global font issue was found: tldraw root uses `font-size: 12px` and project CSS has `textarea, select, button { font: inherit; }`; the workflow editor now uses an inline `font: "14px / 24px var(--font-geist-sans), Arial, Helvetica, sans-serif"` to keep true 14px text.
- Important bug during this session: an earlier workflow mention implementation re-rendered the contenteditable DOM whenever any `@...` text existed. That broke text selection, made the cursor jump when typing another `@`, and made blue mentions unstable. This was corrected to mirror conversation behavior: only real matched references render blue; hand-typing `@` opens the reference menu but does not make arbitrary following text blue; DOM is not rebuilt on every input.
- Workflow `@` reference popup now reuses the conversation asset-loading path more closely. `ChatWorkbench` passes `referenceAssets` plus `onLoadReferenceAssets={() => loadMentionAssetFilters()}` to `WorkflowCanvas`; clicking the workflow `@` button or manually typing `@` calls that loader so the same `角色图片 / 场景图片 / 分镜图片 / 上传图片` groups populate instead of showing empty. The popup has group tabs and thumbnail rows like conversation mode. Clicking an asset replaces the current typed `@query` with `@资产名 ` and focuses after the inserted mention.
- Workflow upload chips now perform real uploads instead of being only visual. Each chip uses the current node/model upload rule from `src/lib/upload-rules.ts` and sets `accept` only for that kind. Images upload through `/api/asset-upload-temp`; video/audio/document uploads use `/api/upload-file`. Uploaded items are stored on `WorkflowNodeData.uploads`, shown above the text input, and clicking a ready uploaded item inserts `@文件名 `.
- Workflow generation now parses references from the node prompt and upload list. Text nodes send uploaded/mentioned image references in `/api/chat` `messages[].images`. Image nodes combine upstream node images with mentioned/uploaded image references for `/api/image referenceImages`. Video nodes combine upstream node images plus mentioned/uploaded image references for `referenceImages`, uploaded video references for `referenceVideos`, and uploaded audio references for `referenceAudios`. Uploaded image/video/audio attachments are included even if not manually mentioned, matching conversation input semantics; asset-library references still require explicit `@资产名`.
- Workflow input-button styling was adjusted locally: bottom prompt buttons default to no visible button fill and use a light gray hover/active fill; `@` is a square `36px` button; other parameter buttons auto-size to content instead of fixed `190px`; opened input menus use `opacity: .9` instead of `.72`.
- Local validation after the latest changes: `npx tsc --noEmit` passed. This session still needs browser testing for workflow input selection/caret, manual `@` filtering, refresh restoring blue mentions, four upload buttons, generated image/video references, and upload/video/audio reference behavior before any deploy.

## Latest 2026-06-26 Local Cache Fix, GitHub Rename, And Sync

- After the local root folder rename, the user reported the local homepage kept flashing and videos did not display. Homepage video files still existed and `http://localhost:3000/home-assets/hero-background-lite.mp4` returned `200 video/mp4`, so the issue was not missing media.
- `start-project.log` showed repeated Turbopack panics: `Failed to write app endpoint /page` caused by `Next.js package not found`. This was caused by stale `.next/dev` state after the project directory rename.
- Stopped local `node.exe` processes for `next dev`, `start-server.js`, and `.next\dev\build\postcss.js`, deleted generated cache folder `.next`, then restarted with `scripts/start-project.ps1 -Worker`. The local homepage recovered and no new Turbopack panic appeared in the fresh log.
- Verified local routes after the cache fix: `/`, `/workspace`, `/admin`, `/dev/tldraw-test`, and the homepage mp4 returned `200`. `npx tsc --noEmit` passed.
- Renamed GitHub repository from `lookxun/AI-Video-Assistant` to `lookxun/FlashMuse_Agent` through the GitHub REST API using the local Git credential. Local `origin` was updated to `https://github.com/lookxun/FlashMuse_Agent.git`; `git ls-remote --heads origin main` succeeded.
- Updated `README.md` clone instructions to the new repository name. The GitHub page `https://github.com/lookxun/FlashMuse_Agent` returned `200`.
- Committed and pushed all then-uncommitted local code/docs changes to GitHub: `1c9211d Sync local workflow updates and repo rename`. The pushed commit includes local tldraw workflow work, repo/local folder rename updates, `src/lib/error-message.ts` message mapping, and current handover updates that existed before this final handover write. Remote `main` points to `1c9211d`; local working tree was clean immediately after that push.
- The first commit attempt failed because Git had no local/global author identity. No global Git config was changed. The commit used the previous author identity, then repository-level Git identity was set to `lookxun <lookxun@users.noreply.github.com>` for future commits.
- No production deploy was performed during this local cache/GitHub sync work. Production workflow entry remains disabled unless the user explicitly approves opening workflow mode.

## Latest 2026-06-26 Local Root Folder Final Rename

- User noticed the previously copied `E:\project\FlashMuse_Agent` folder was smaller than the original `E:\project\AI-Video-Assistant` folder and asked to keep the original directory state instead.
- Compared both folders before changing anything. Git recent commits matched. Excluding `.git`, `node_modules`, `.next`, and `.runtime`, the source/code manifests matched apart from handover docs, `start-project.log`, and `tsconfig.tsbuildinfo`; no important source-only difference was found.
- The size difference was partly because the original folder had `.runtime/` and the copied folder did not. The original folder was treated as safer because it preserved more local runtime/debug state.
- Stopped the copied-folder `next dev` Node processes that locked `E:\project\FlashMuse_Agent\node_modules\.prisma\client\query_engine-windows.dll.node`.
- Deleted the copied `E:\project\FlashMuse_Agent`, renamed the original `E:\project\AI-Video-Assistant` to `E:\project\FlashMuse_Agent`, and renamed the internal planning folder to `FlashMuse_Agent_Project Planning`.
- Final checks: old path `E:\project\AI-Video-Assistant` no longer exists, new path `E:\project\FlashMuse_Agent` exists, `.runtime/` is preserved, final directory count was `37888` files and `3545318274` bytes, and `npx tsc --noEmit` passed. No production deploy or GitHub sync was performed.

## Latest 2026-06-26 BytePlus Audio-Sensitive Error Prompt Fix

- User asked to inspect online `B_150` and `B_155`. Read-only production log/DB diagnostics found both were BytePlus Seedance video polling failures for `273146499@qq.com / ID_315163` in conversation `ad300f7a-4e0f-4692-8df7-5f28ebecd08f` (`Ming Dynasty sch...`). Both used `byteplus:video.seedance-2-0`, `9:16 / 1080p / 5秒 / 写实风格`, prompt length `910`, and one image reference. There were no uploaded audio files or `reference_audio` entries.
- `B_150` provider error: `The request failed because the output audio may contain sensitive information`, BytePlus request id `02178243959703900000000000000000000ffffc0a8782688a63b`, task `cgt-20260626100636-9z9rs`, request `5d17a1e9-fa8a-4da9-83da-8345932bee88:video:0`, reference image `/generated/users/ID_315163/upload_image/129ff75d5c12efbde2ceffd6.jpg`. A later retry with the same reference succeeded, so no persistent failed `B_150` message remained in `WorkspaceMessage`.
- `B_155` provider error was the same output-audio-sensitive failure, BytePlus request id `02178244453754800000000000000000000ffffc0a87bf4c78739`, task `cgt-20260626112857-2pmgp`, request `1a27fb5d-07f8-46cd-bb31-40fec2f9394d:video:0`, reference image `/generated/users/ID_315163/upload_image/cd9bd4cc2e5f14c9ff359af3.jpg`. Stored failed message: `WorkspaceMessage.messageId=9f52b441-47ea-4fe1-968c-ac7ea7c96678`.
- User clarified this should not show the old reference-image warning. Changed `src/lib/error-message.ts` so output audio sensitive errors now show: `生成结果中的音频可能触发平台敏感内容审核，平台拒绝输出。请调整提示词后重试。` Output copyright errors now show: `生成结果可能涉及版权限制，平台拒绝输出。你可以调整提示词、换参考图或重新生成。` Reference/image/asset review errors now show: `参考图可能包含真人、隐私或版权敏感内容，平台拒绝生成。请换一张参考图后重试。`
- Local `npx tsc --noEmit` passed. Only `src/lib/error-message.ts` was uploaded to production; no local workflow/tldraw work was deployed. Production backup for the old file: `/var/www/flashmuse/.deploy-backups/20260626-error-message-audio-sensitive/error-message.ts.before`. Ran `/usr/local/bin/deploy-flashmuse-production.sh`; build passed with only existing Turbopack/NFT broad-file warnings; PM2 stayed online; Ali `_next/static` synced.
- Verification passed: deployed `error-message.ts` had the new messages, `https://main.venusface.com/workspace`, `https://main.venusface.com/admin`, and `https://api.venusface.com/api/model-availability` returned 200.

## Latest 2026-06-26 Static Domains HTTPS And Certificate Automation

- User said domain review passed and asked to make the domain/certificate state fully normal with no later manual work. This work changed server configuration only; no app code was deployed.
- Ali `ali.venusface.com` and `static.venusface.com` public HTTP/HTTPS access was rechecked. After Nginx changes, HTTP for those hosts redirects to HTTPS, while `/.well-known/acme-challenge/` is served from `/var/www/letsencrypt` for Let's Encrypt HTTP-01 validation. The IP/default HTTP entry remains separated so old IP access is not intentionally broken by the host redirect rule.
- Ali Nginx config `/etc/nginx/sites-available/flashmuse-static-ip` was backed up to `/etc/nginx/sites-available/flashmuse-static-ip.bak.20260626134138-http01-https` before editing. `nginx -t` passed and Nginx was reloaded.
- Reissued Ali certificate `flashmuse-ali-static` for `ali.venusface.com` and `static.venusface.com` using certbot HTTP-01 webroot. New expiry is `2026-09-24 04:43:56+00:00`. Renewal config now uses `authenticator = webroot` and `/var/www/letsencrypt` instead of manual DNS-01.
- Added Ali certbot deploy hook `/etc/letsencrypt/renewal-hooks/deploy/reload-nginx.sh`, which runs `nginx -t` and reloads Nginx after future renewals. `certbot.timer` exists. `certbot renew --dry-run --cert-name flashmuse-ali-static --no-random-sleep-on-renew` passed.
- Fixed Malaysia PM2 environment residue: `/root/.pm2/dump.pm2` still had `FORCE_INSECURE_AUTH_COOKIE=true` even though `/var/www/flashmuse/.env.local` had `false`. Backed up PM2 dump, changed the value to `false`, restarted `flashmuse` with `--update-env`, and saved PM2. `pm2 env 0` now reports `FORCE_INSECURE_AUTH_COOKIE=false`.
- Verification passed: `http://ali.venusface.com/` and `http://static.venusface.com/flashmuse-cache-health` return 301 to HTTPS; `https://ali.venusface.com/`, `https://static.venusface.com/flashmuse-cache-health`, a real `/_next/static` file, and a known generated video returned 200; `https://main.venusface.com/workspace` and `https://api.venusface.com/api/model-availability` returned 200; logout `Set-Cookie` includes `Domain=.venusface.com; Secure; HttpOnly; SameSite=lax`; PM2 stayed online.

## Latest 2026-06-25 Local Workflow Video Controls And Focus Button Fix

- User asked to continue local-only workflow tldraw work in concise Chinese. No production deploy or GitHub sync was performed. Production workflow entry must remain disabled unless explicitly approved.
- Investigated workflow generated video nodes not playing in `src/components/workflow-tldraw-canvas-inner.tsx`. Initial attempts to remove poster, use native `<video controls>`, add a custom play overlay, reserve the bottom controls strip, disable `.tl-selection__bg` pointer events, and shrink `getGeometry()` all failed or caused side effects. The `getGeometry()` shrink attempt produced an unwanted blue selection line across the video and was reverted.
- Root cause/final fix came from tldraw official docs, especially `https://tldraw.dev/examples/interactive-shape` (`Clickable custom shape`). Custom interactive HTML inside a tldraw shape needs `pointer-events: all` and stopped event propagation. Current workflow video node keeps native `<video controls>` with no poster/cover and no custom center play button. `HTMLContainer` uses `style={{ pointerEvents: "all" }}`; video wrapper/video use `pointerEvents: "all"`; video capture handlers call both `event.stopPropagation()` and `editor.markEventAsHandled(event)`. User confirmed playback controls became clickable.
- Important future rule: when tldraw custom shape interaction problems are hard to solve, check `https://tldraw.dev/` docs/examples before guessing. For buttons, inputs, video controls, and other interactive children inside custom shapes, first try the official `pointer-events: all` plus `stopPropagation` pattern, and use `editor.markEventAsHandled()` when tldraw canvas handling still interferes.
- Changed the bottom dock focus button behavior. The `RiFocus3Line` button now uses `zoomToSelectedOrWorkflowNodes()`: if one or more workflow nodes are selected, it zooms to the selected workflow shapes; if no workflow node is selected, it zooms to all workflow nodes. The left-bottom zoom menu item `显示画布所有元素` still always zooms to all nodes.
- Local validation during this work: `npx tsc --noEmit` passed after the latest changes.

## Latest 2026-06-25 Local Workflow Real-Size Nodes And Dock Iteration

- User asked to continue local-only workflow tldraw work in concise Chinese. No production deploy or GitHub sync was performed. Production workflow entry must remain disabled unless explicitly approved.
- Reworked workflow node card styling in `src/components/workflow-tldraw-canvas-inner.tsx`. Node cards are square-corner blocks with `#e6e6e6` background, `1px #f5f5f5` border, and empty-state icon color `#d1d1d1`. The tldraw selection outline now wraps only the node media/card body, not the title row or input box.
- Split selected-node chrome out of the tldraw shape body. When a node is not selected, only the card is visible. When selected, a top overlay shows the node icon/title on the left and blue parameter text on the right; the input box is a separate overlay below the node. The old selected-node top-right close button was removed.
- Top selected-node overlay is intentionally one line and positioned close to the node. Left content has priority; the left icon must remain visible, left text truncates only after right parameters are reduced. Right parameters are right-aligned and dynamically drop fields in this order as width shrinks: model, ratio, resolution/duration, then size. The final kept right-side value is size when possible.
- Workflow image/video cards now use real model output dimensions as tldraw shape size. Example: `2848x1600` is a `2848x1600` canvas node, so 100% zoom can exceed the browser width. The old 320px max-long-edge scaling was removed. Text nodes default to `720x1280` and can be resized.
- Added `WorkflowNodeData.visualSize` for text node custom size. Text nodes use tldraw native resize, hide rotate handles, do not lock aspect ratio, and save `visualSize` on resize. Image and video nodes cannot be resized and keep their parameter-derived output size.
- Default new image node settings are now `Seedream 4.5 / 16:9 / 2K` using `byteplus:conversation-image.seedream-4-5`. Default new video node settings are `Seedance 2.0 / 16:9 / 720p / 8秒` using `byteplus:video.seedance-2-0`.
- Workflow image ratio menu no longer includes `智能比例`. The settings menu now includes the same lower `尺寸 W x H PX` section as conversation mode. All input-box menus are mutually exclusive; opening one closes the others, and clicking the input area closes open menus.
- New node placement now avoids overlap, prefers the most recently operated existing node, skips deleted nodes in that recent-action stack, and focuses the newly created node roughly centered at about 70% of browser viewport. Recent action is updated by node click, edit, generation, resize, and other explicit node updates.
- Workflow dock was refined. The select/hand tool moved to the far right of the dock. The dock is centered with a flex wrapper instead of `left-1/2 -translate-x-1/2`, because the transform made SVG icons look blurry. Icons were restored to their previous `20px` size after removing the transform. The dock remains local-only.
- Left-bottom zoom menu now includes `缩放至25%`, and tldraw camera options allow zoom down to `1%` with zoom steps `[0.01, 0.025, 0.05, 0.1, 0.25, 0.5, 1, 2, 4, 8]`. Zoom presets use current-view anchoring. `显示画布所有元素` and dock focus button use current tldraw `workflow_node` shape bounds via `editor.getShapePageBounds()` and `editor.zoomToBounds()`.
- Workflow camera/viewport saving was tried and then removed because low-frequency viewport persistence still caused workflow switching to feel sticky. Current intended behavior: do not save last camera. On workflow load/switch, auto focus all nodes instead. Do not reintroduce frequent camera autosave without explicit approval.
- Added lightweight geometry persistence only for node location and text node size. A 900ms poll reads current tldraw `workflow_node` shapes and writes back only `nodes[].x`, `nodes[].y`, and text-node `data.visualSize` when changed. It does not save camera, selection, or full tldraw store. Full `editor.store.listen -> onChange` remains forbidden because it previously froze the browser.
- Workflow video node behavior was iterated. Nodes no longer use `posterUrl` for the node body; poster remains available for asset/library use. This was later fixed further in the same 2026-06-25 local session using tldraw's official clickable custom shape guidance: native video controls are used with `pointer-events: all`, `event.stopPropagation()`, and `editor.markEventAsHandled()` so controls remain clickable inside the custom shape.
- Waiting text/icons for text, image, and video nodes now scale with node height so they do not appear too small on real-size nodes or too large on small nodes.
- Local validation during this work: `npx tsc --noEmit` passed after the latest changes.

## Latest 2026-06-25 Local Workflow tldraw UI Iteration

- User asked to keep replies concise/direct in Chinese and continue local-only workflow mode work. No production deploy was performed. Production workflow entry must remain disabled unless explicitly approved.
- Changed the main workspace left-sidebar controls in `src/components/chat-workbench.tsx`. The logo keeps its old behavior and only toggles the left sidebar collapsed/expanded width. The top-left header button in conversation/assets and the matching workflow button now toggle full left-sidebar visibility. When hidden, the main area uses full width. Workflow no-active-item fallback also keeps the same restore button so users can show the sidebar again.
- Added `isSidebarVisible` local UI state and adjusted document-preview width calculations to account for sidebar hidden/collapsed/expanded states.
- Updated workflow top-left UI. The old white Lovart-style top-left tool pill was removed. Current top-left workflow UI is only a sidebar visibility button plus the workflow title, colored `#5c626b`, aligned to the same `left-4 top-3` position as the conversation header button.
- Reworked the workflow left-bottom status controls in `src/components/workflow-tldraw-canvas-inner.tsx`. Current order is canvas background, layers, minimap, divider, zoom percentage. The old generated-files icon was intentionally removed because that feature is not implemented yet. Buttons use 30x30 hover/click boxes, 2px gaps, and sit at `bottom-[5px] left-5`.
- Canvas background control is a local popover with background color choices and a tldraw grid toggle. It uses CSS variable `--workflow-canvas-bg` in `src/app/globals.css`. It is not the full tldraw default background UI because a usable default background popover was not available in the hidden-UI setup.
- Added a workflow layers side panel. It opens from the layers icon, closes on outside click, and has two sections: `历史记录` for workflow asset-table rows not currently visible in the canvas, and current canvas contents shown as `Frame -> node -> current media`. Clicking a node row selects the matching tldraw node.
- Added a custom minimap popover instead of `DefaultMinimap`, because the default tldraw minimap showed a white block for this hidden-UI/custom-shape setup. The custom minimap is `200 x 130`, shows workflow nodes as gray rectangles, shows the current viewport rectangle, and supports click/drag to move the canvas view. It uses Remix `RiRoadMapLine` as the icon.
- Added a zoom percentage menu. Clicking the percentage opens a `200px`-wide rounded popover with `放大`, `缩小`, `显示画布所有元素`, `缩放至50%`, `缩放至100%`, and `缩放至200%`. The current zoom preset is highlighted with an inset rounded rectangle. Shortcut labels are plain text: `Ctrl +`, `Ctrl -`, and `Shift + 1`.
- Hidden the tldraw watermark locally inside the workflow canvas by injecting a scoped style under `.workflow-tldraw-shell` and also adding scoped CSS selectors in `globals.css`. This was done for internal local testing only. Do not treat this as production license resolution. Before opening workflow mode beyond internal use, resolve tldraw licensing properly.
- Fixed workflow node deletion from keyboard. Because full tldraw store syncing is intentionally disabled, raw tldraw `Delete/Backspace` did not update `WorkflowCanvasState`. A safe keyboard listener now deletes selected workflow nodes through our state path and removes connected edges, while ignoring inputs/textareas/contenteditable.
- Fixed workflow deletion/active-workflow recovery in `chat-workbench.tsx`. Deleting the active workflow now immediately selects the next visible workflow or the newly ensured `新工作流`. A guard effect corrects `activeWorkflowId` if it points to a deleted or missing workflow while the workflow panel is active.
- Fixed workflow switching where only the title changed but the tldraw canvas did not. `WorkflowCanvas` now has `key={activeWorkflow.id}` in `ChatWorkbench`, and `workflow-tldraw-canvas-inner.tsx` tracks `loadedWorkflowIdRef`; when `workflowId` changes it force-loads the new canvas into tldraw and clears the emitted-key guard. This avoids stale tldraw canvas content after switching workflows.
- Refined bottom-center workflow dock icons. The first tool is now a hover menu for `选择` and `移动`, using Remix `RiCursorLine` and `RiHand`. The menu appears on hover, is horizontally centered over the first button, and touches the button with no gap so it does not disappear while moving the mouse upward. Shortcuts are `V` for select and `H` for hand/pan. The attempted automatic hover-based canvas/node tool switching was removed because it conflicted with node selection.
- Unified workflow node and dock icons. Text generation uses `RiAiGenerateText` on the dock, node header, and empty text-node card. Image generation uses `RiImageAiLine`. Video generation uses the same conversation video-generation icon `RiFilmAiLine` in the dock and video node header. This corrected the earlier accidental use of generic `RiVideoLine`.
- Current workflow tldraw integration still must not use full `editor.store.listen -> onChange -> ChatWorkbench/workspace autosave` syncing. That path previously froze the browser. Position persistence for pure drag remains a separate future task.
- Validation during this local work: `npx tsc --noEmit` passed after the changes. No production deploy or GitHub sync was performed.

## Latest 2026-06-24 Local tldraw Workflow Canvas Experiment

- User asked to continue local workflow mode work and keep replies concise/direct in Chinese. This section is local-only and has not been deployed. Production workflow entry must remain disabled unless explicitly approved.
- Investigated workflow preview instability for generated image/video names, parameters, dimensions, and prompts. Local audit found `MediaAsset + UserAssetState` workflow rows usually had prompt and dimensions, while several `WorkspaceWorkflow.canvasJson.nodes[].data.imageDimensions` entries were missing. Preview was inconsistent because workflow preview sometimes trusted node data and sometimes table assets, unlike conversation preview which canonicalizes from message/table data.
- Fixed workflow preview canonicalization in `src/components/chat-workbench.tsx`: workflow preview now merges asset-table metadata with current workflow node metadata, uses table data when available, falls back to node prompt/settings/dimensions, and derives expected dimensions/ratio/resolution when exact node dimensions are absent. Node-click preview and right-side workflow preview rail now share the same merged path.
- Found local `image_5_w2` had bad `MediaAsset.sourcePrompt="????"` while the workflow node prompt was `生成帅哥`. Fixed `/api/media-assets` workflow POST so if `sourcePrompt` is blank or only question marks it reads the corresponding `WorkspaceWorkflow.canvasJson` node and writes `prompt/text/outputText/title` instead. Also changed remote-to-local workflow re-persist fallback in `chat-workbench.tsx` so workflow node prompt wins over stale/bad asset prompt. Local `image_5_w2` row was manually repaired to `sourcePrompt="生成帅哥"`, with `width=2848`, `height=1600`.
- Compared Lovart and tldraw. Lovart appears to use tldraw as the infinite-canvas base plus custom AI/product UI. tldraw docs confirmed React 18/19 compatibility, required `tldraw/tldraw.css`, explicit-size wrapper, custom shapes via `ShapeUtil`, store/snapshot persistence, performance culling/signals, and production license-key requirement. tldraw SDK is source-available, not permissive open source; production/commercial use needs trial/commercial/hobby license key.
- Installed `tldraw@5.1.1` and added `import "tldraw/tldraw.css"` in `src/app/layout.tsx`. `package.json` / `package-lock.json` changed. npm install reported 6 audit vulnerabilities; no `npm audit fix` was run to avoid unrelated changes.
- Added independent tldraw test route `/dev/tldraw-test` under `src/app/dev/tldraw-test/`. It uses default `<Tldraw />` UI only, with no workspace state, autosave, generation chain, or asset persistence. User tested it and said it is smooth and does not freeze.
- Added root launcher `open-tldraw-test.bat`, which starts dev if needed and opens `http://localhost:3000/dev/tldraw-test`.
- Replaced old hand-written workflow canvas file. `src/components/workflow-canvas.tsx` was deleted; current exported workflow canvas is `src/components/workflow-tldraw-canvas.tsx`, dynamically loading `src/components/workflow-tldraw-canvas-inner.tsx` client-only. A temporary `src/components/workflow-tldraw-minimal-canvas.tsx` remains as a minimal diagnostic component.
- First tldraw integration attempt used low-level `TldrawEditor` and full store listening/exporting back to `ChatWorkbench`; clicking workflow froze the browser. Root cause is likely high-frequency tldraw store changes being converted to parent React/workspace state and autosave loops, plus possible Turbopack/HMR duplicate tldraw library instances. Do not reintroduce full `editor.store.listen -> onChange` syncing.
- Stable integration currently uses official `<Tldraw hideUi shapeUtils={[WorkflowNodeShapeUtil]} onMount={...} />`, client-only dynamic loading, and custom `workflow_node` shape. It does not listen to full store changes. Node selection is read inside shape components with `useEditor/useValue`; node business updates and generation results call explicit update paths. User confirmed this version opens in main workflow without freezing.
- Current workflow UI was adjusted toward Lovart style per user feedback, but only partially. Background is now pure `#cccccc`; tldraw default UI remains hidden. Bottom center dock includes select/text/image/video/zoom controls. Left-bottom status strip shows small icons and zoom percentage. Node shadows and all node child `box-shadow`/`filter` were removed. Left/right node `+` connection buttons were removed. User specifically rejected the earlier dark theme.
- Known current limitation: pure dragging in tldraw is not yet persistently saved to `WorkspaceWorkflow.canvasJson` unless a later business update reads current tldraw positions before saving. Next work should add safe, throttled position persistence on drag/interaction end only, not full store syncing.
- Validation during this local work: `npx tsc --noEmit` passed repeatedly. `/workspace` returned 200 after dev restarts. `npm run build` was attempted earlier and failed on existing Google Geist font/Turbopack internal font fetch/module resolution, not on TypeScript; do not treat that as a tldraw-specific proof of production readiness.

## Latest 2026-06-24 Production Deploy, Input Scroll Fix, And GitHub Sync

- User asked to deploy all remaining undeployed local work while keeping production workflow mode disabled, then sync GitHub and update handover.
- Fixed the conversation-flow input editor scroll/caret behavior before deployment. When long input text is scrollable and the user scrolls upward inside the input box, typing, newline, paste, deleting mention spans, or inserting `@` references now preserves the user's scroll/caret area instead of jumping the text/caret to the bottom. If the editor is already at the bottom, it still follows the bottom as the user keeps typing.
- Local `npx tsc --noEmit` passed before deploy.
- Production pre-deploy snapshot: `/var/www/flashmuse/.runtime/deploy-checks/20260624-before-workflow-input-deploy.json`. It recorded `userAssetStates=1330`, `visibleActiveAssets=1293`, `stableMissingInNewTable=0`, `fallbackUsers=0`, and `assetListHash=81ece40e2d3c6134`.
- Uploaded full local source to Malaysia from archive `/tmp/flashmuse-20260624-workflow-input-deploy.tgz`, with local temporary copy `C:\Users\ASUS\AppData\Local\Temp\opencode\flashmuse-20260624-workflow-input-deploy.tgz`. Production source backup: `/var/www/flashmuse/.deploy-backups/20260624-workflow-input-deploy/source-before-deploy.tgz`.
- Confirmed production `NEXT_PUBLIC_WORKFLOW_MODE_ENABLED` was disabled/unset before deploy, so workflow entry remains hidden/disabled online.
- Applied production Prisma migration `20260624090000_workflow_media_names`, ran `npx prisma generate`, then `/usr/local/bin/deploy-flashmuse-production.sh`. Build passed with only existing Turbopack/NFT broad-file warnings. PM2 `flashmuse` restarted online and Ali `_next/static` synced.
- Deployed previously local workflow stabilization: history ordering by row `updatedAt`, workflow media naming fields/counters, `image_N_wX` / `video_N_wX` naming, stale async workflow canvas fix, workflow generated media success persistence, node media metadata preservation on stale saves, workflow preview scoping/parameter display fixes, node drag/input fixes, and root layout `Script` warning fix.
- Production post-deploy snapshot: `/var/www/flashmuse/.runtime/deploy-checks/20260624-after-workflow-input-deploy.json`. Snapshot compare against before returned `ok: true`; totals, category counts, per-user assets, `stableMissingInNewTable`, fallback users, and asset hash were unchanged.
- Verified production endpoints returned 200: `https://main.venusface.com/workspace`, `https://main.venusface.com/admin`, `https://main.venusface.com/admin?tab=upload-rules`, `https://main.venusface.com/admin?tab=server`, and `https://api.venusface.com/api/model-availability`. PM2 process was online.
- User requested GitHub sync after deploy. Main deployed-change commit: `90ae17d Deploy workflow stabilization and input fixes`. A small follow-up handover-only commit may exist after this to record the commit hash.

## Latest 2026-06-24 Local Workflow Stabilization

- User asked for concise direct Chinese replies to continue.
- Read current handover docs first, then continued workflow work. These fixes were later deployed on 2026-06-24 with production workflow mode still disabled.
- Renamed workflow sidebar section label from `我的工作流` to `历史工作流` to match `历史对话`.
- Changed history ordering intent locally. `历史对话` and `历史工作流` are intended to sort by each row's own `updatedAt` descending, so whichever conversation/workflow changes moves to the top and other items keep their relative updated-time order. Conversation API sorting now uses `WorkspaceSession.updatedAt` rather than latest message time.
- Fixed root layout client error reporter. Replaced raw `<script dangerouslySetInnerHTML>` in `src/app/layout.tsx` with Next `Script` to avoid React client script/hydration warning. A separate hydration warning may still appear locally when stored UI starts in workflow mode while SSR initially renders chat mode; treat it as a local recoverable mismatch unless it breaks UI.
- Audited production Agent media table coverage. Online `WorkspaceMessage` query found `67` Agent-generated messages and `153` unique Agent media URLs (`90` images, `63` videos), all stable local `/generated/...` URLs and all present in `MediaAsset` by `url/normalizedUrl/originalUrl`; missing Agent generated image/video rows found: `0`.
- Added local workflow media naming fields and migration. `WorkspaceWorkflow` now has `workflowCode`, `nextImageNumber`, and `nextVideoNumber`; local migration `prisma/migrations/20260624090000_workflow_media_names/migration.sql` was applied locally with `npx prisma migrate deploy`; `npx prisma generate` succeeded after stopping local `next dev` that had locked Prisma engine files.
- Workflow numbering rule is local and mirrors conversation media naming, but with `w` instead of `d`: `工作流_01 -> w1`, `工作流_02 -> w2`, and workflow-generated media names are `image_N_wX` / `video_N_wX`. Each workflow has independent image/video counters. Failed generations must not consume a number. Names are assigned only on successful generation and should then be fixed.
- Workflow generated media now follows the conversation-mode immediate path more closely. `WorkflowCanvas` calls `onGeneratedMedia` on successful image/video generation; `ChatWorkbench` immediately reserves a workflow system name, writes a local `AssetItem` with preview metadata, and posts local saved media to `/api/media-assets`. Runtime node-scanning fallback that re-posted existing workflow node URLs was removed because it re-numbered old assets and caused repeated name drift.
- Fixed a major workflow stale-state bug. `WorkflowCanvas` now stores latest canvas state in `stateRef`; async image/video completion updates from the latest state instead of the state captured at generation start. This prevents one node's async completion from overwriting another node's newer canvas changes or making another image node appear to generate nothing.
- Added `WorkflowNodeData.mediaSystemNames` and preserves it through remote-to-local URL replacement. This is the workflow equivalent of conversation `message.mediaSystemNames`; node preview can show `image_N_wX` immediately without waiting for asset table reload.
- Hardened `upsertWorkspaceWorkflows()` on the server. When a stale/partial client payload lacks media metadata, existing node media fields are preserved: `images`, `imageDimensions`, `mediaSystemNames`, `videoUrl`, and `posterUrl`. This prevents refresh/autosave from erasing node media names and dimensions.
- Workflow preview behavior was fixed. When opening preview from a workflow node or workflow asset, right-side thumbnails are built from the current workflow canvas nodes only, not from all historical workflow assets in the asset table. Preview names come from node `mediaSystemNames` or table system names. Preview parameters use table metadata when available, otherwise node model/settings/dimensions. Ratios are derived from actual dimensions when possible, so `2848 x 1600` displays as `16:9` rather than `智能比例` or question marks.
- Fixed workflow node interaction issues. Generated image/video cards have `draggable={false}` and no longer trigger browser drag-download; nodes remain draggable in empty, waiting, success, and failed states. Workflow input now sends on `Enter` and inserts newline on `Shift+Enter`, matching conversation input.
- Waiting card experiment was reverted per user: no extra center icon should appear on workflow image/video waiting cards.
- Local data was repeatedly repaired after earlier buggy numbering attempts. Current verified local `12424740@qq.com / ID_779117` state after final repair: `工作流_01 / w1` has `image_1_w1` and next image number `2`; `工作流_02 / w2` has current visible node media `image_1_w2` through `image_6_w2` and next image number `7`. The latest `image_6_w2` table row has `model=byteplus:conversation-image.seedream-4-5`, `ratio=智能比例`, `resolution=2K`, `width=2848`, `height=1600`, and thumbnail URL. Re-check before relying on this if user has generated more since handover.
- Consulted old project docs under `E:\project\clean_project_code\docs`, especially `handoffs/2026-05-29-workflow-nodes-shipped.md`, `handoffs/2026-05-10-next-session-sprint-2-canvas-logic.md`, and `handoffs/2026-05-11-next-session-imagegen-followups.md`. Important workflow principles carried forward: controlled nodes/edges, generation writes back to the same node for self-preview, multiple nodes of the same type are independent, fan-in text should concatenate upstream text instead of overwrite, and node data should not be re-derived from unrelated global asset state.
- Also checked React Flow docs on core concepts/interactivity. Relevant general rules: nodes and edges are controlled state, changes should be applied to the latest state, handles/edges represent graph connections, and selected/draggable node behavior should remain independent from embedded media elements.
- Local validation after these changes: `npx tsc --noEmit` passed and `http://localhost:3000/workspace` returned `200`.

## Latest 2026-06-23 Protected Production Deploy And Hotfix

- User requested concise Chinese replies and Chinese task-list labels going forward.
- Backend/admin idle login behavior was changed to match normal frontend idle behavior. Admin session max age is now 1 hour and `/admin/api/auth/activity` extends it only on real admin browser activity. `AdminActivityTracker` is mounted in the admin shell. This is deployed.
- Admin overview card `今日生成任务` was fixed. The large number now uses today's generated image count plus today's generated video count; the small text now shows today's image/video counts, not historical totals. This is deployed.
- Agent-generated image/video prompt persistence was hardened. For Agent media, the main model prompt is stored in `MediaAsset.sourcePrompt`; Agent hard constraints are stored in `MediaAsset.sourceDetail` as JSON with `agentConstraints`. Admin media and credit views show the main prompt in black and constraints in gray. Multi-image/multi-video results are mapped by URL so each asset keeps the correct prompt. This is deployed.
- The workflow/new-table-only asset work is now deployed to production with Prisma migrations `20260623043000_workspace_workflows` and `20260623044000_backfill_workspace_workflows`. `WorkspaceWorkflow` is now the deployed workflow history table. Production workflow entry remains disabled because `NEXT_PUBLIC_WORKFLOW_MODE_ENABLED` is unset/false.
- Production asset-library source of truth remains `MediaAsset + UserAssetState`. Current online `/api/workspace-state?assetsOnly=1` returns new-table assets when a user has any `UserAssetState`; old `UserWorkspaceState.state.assets` fallback is not used for any current production user. Online audit found `fallbackUsers=0`.
- Online pre-deploy snapshot: `/var/www/flashmuse/.runtime/deploy-checks/20260623-before-risk-deploy.json`. It recorded `visibleActiveAssets=1256`, `userAssetStates=1292`, category counts `character_image=102`, `conversation_images=498`, `conversation_uploads=180`, `conversation_videos=371`, `shot_image=44`, `scene_image=61`, `stableMissingInNewTable=4`, and `assetListHash=ff9ef4f9f85ff233`.
- Protected deploy backup: `/var/www/flashmuse/.deploy-backups/20260623-risk-flow/source-before-deploy.tgz`. Deployment ran Prisma migrate deploy, Prisma generate, then `/usr/local/bin/deploy-flashmuse-production.sh`. Build passed with existing Turbopack/NFT broad-file warnings only, PM2 stayed online, and Ali `_next/static` was synced.
- Post-deploy snapshot `/var/www/flashmuse/.runtime/deploy-checks/20260623-after-risk-deploy.json` matched the before snapshot exactly by totals, per-category counts, per-user asset counts, and `assetListHash`.
- A production browser crash was reported immediately after deploy: `This page couldn’t load`. PM2 client diagnostics showed `Cannot read properties of undefined (reading 'some')` in the workspace chunk. Root cause was workflow state with `canvas` present but `canvas.nodes` missing while production workflow entry was disabled; global workspace code still evaluated `workflow.canvas?.nodes.some(...)`. Hotfix changed workflow node access to safe optional chaining for `forEach`, `length`, `some`, and `map`.
- Hotfix backup: `/var/www/flashmuse/.deploy-backups/20260623-workspace-crash-hotfix/source-before-hotfix.tgz`. Hotfix deployed successfully. `https://main.venusface.com/workspace`, `https://main.venusface.com/admin`, and `https://api.venusface.com/api/model-availability` returned `200`; PM2 `flashmuse` was online.
- Hotfix snapshot `/var/www/flashmuse/.runtime/deploy-checks/20260623-after-hotfix.json` again matched the before snapshot exactly: no asset count, per-user, per-category, fallback-user, stable-missing, or asset-list-hash differences.
- Added reusable deployment guard script `scripts/prod-deploy-snapshot.mjs`. It is also copied on production under `/var/www/flashmuse/.runtime/deploy-checks/prod-deploy-snapshot.mjs`. It creates sanitized asset/workspace snapshots and compares before/after deploys without writing signed provider URLs into Git/docs.
- Online asset audit clarification: the 1284/1292 new-table rows are the new-table asset state, not all historical URL references. Historical message/old-state media references are higher because one asset can be referenced multiple times. Stable local media missing from new table remains `4`, all uploaded audio/video file references, not normal generated image/video assets. Remote provider temporary URLs should not enter `MediaAsset.url/normalizedUrl`; they belong only in runtime mapping/debug files such as `.runtime/media-url-map.md`.
- Follow-up backfilled those 4 uploaded-file gaps into `MediaAsset + UserAssetState`: 3 audio files and 1 video file under internal categories `conversation_upload_audios` / `conversation_upload_videos`. Backup: `/var/www/flashmuse/.runtime/manual-fixes/1782216984903-upload-files-mediaasset-backfill.json`. Post-backfill snapshot `20260623-after-upload-file-backfill.json` showed `stableMissingInNewTable=0`. Current deployed `/api/upload-file` already auto-writes future uploaded video/audio/document files into the new tables.

- Fixed runtime duplicate media caused by remote temporary URLs and local `/generated/...` URLs both being visible.
- Added `src/lib/media-assets.ts` to canonicalize saved media jobs and merge duplicates.
- Added `/api/media-assets` with `POST` for media upsert and `PATCH` for rename, category move, soft delete, and restore.
- Asset library category switching and pagination now do front-end de-duplication by `mediaId -> normalized URL -> id`.
- Asset moves write timestamp-level `sortOrder`; server only lets timestamp-level sort orders override latest-first ordering.
- Admin record/detail/stat pages now read media from new tables instead of old workspace JSON where possible.
- New media generated or uploaded should be written into new tables through `/api/media-assets` and `upsertWorkspaceMessages()`.
- Admin user/credits/records expanded rows were optimized. Expansions now load lightweight summaries first; concrete dialogs such as history, media lists, upload lists, and credit details load full data on demand. Same-page admin detail requests use a browser memory cache that survives client-side admin tab switches but clears on browser refresh.
- Admin sidebar navigation uses Next `Link` instead of plain `<a>` so switching backend categories does not destroy the in-memory detail cache.
- Workspace asset sidebar was restructured. `上传图片` now appears under the top asset area below `分镜图片`, separated from `对话流资产` by a light gray divider. The `资产生成` label row was removed.
- Asset upload dialog no longer has category selection. All uploads from asset library, conversation flow, and future workflow should go into the unified `上传图片` category.
- Asset card `移动到` menu now offers `上传图片` as the last target. Normal conversation generated images show no checkmark in the move menu; only actual uploaded images show the `上传图片` checkmark.
- Fixed multiple upload-category edge cases: moved generated images now appear in `上传图片`; uploaded images referenced through assistant `imageReferences` are synced/backfilled into the new media tables; freshly uploaded images use `/api/media-thumbnail` fallback to avoid broken thumbnails.
- Upload failures around 1MB were traced to Nginx config. The production `main/api` Nginx server block now has a valid `server_name ...;` and `client_max_body_size 20m;`. Frontend asset upload also compresses large images before temporary upload.
- Media asset rules were clarified: all generated images/videos and uploaded images/videos/audio-like references are assets for platform tracking; user-facing `@` asset menu still only shows role, scene, shot, and upload-image categories, but manually typed `@` can still resolve existing conversation references.
- Upload-image placeholder text is now unified as `上传图片`. Reverse prompt for uploaded images writes to `MediaAsset.reversePrompt`; workspace and admin displays prefer reverse prompts where present.
- Conversation uploads and generated media now write to `/api/media-assets` immediately in addition to workspace-message sync, reducing reliance on delayed workspace autosave.
- BytePlus Seedance 2.0 / Fast now support reference video and reference audio uploads in video mode. Audio/video uploads use the existing `uploadedFiles` structure, save under `/generated/users/{userId}/files/`, render as file cards in the input, and can be referenced with `@文件名`.
- Seedance reference media limits are enforced client-side: videos `mp4/mov`, up to 3, each 2-15s and <=50MB, total video duration <=15s, aspect ratio 0.4-2.5, width/height 300-6000px, total pixels 409600-2086876; audio `mp3/wav`, up to 3, each 2-15s and <=15MB, total audio duration <=15s. A small 0.35s duration tolerance exists for browser metadata rounding. Audio cannot be used alone; it requires at least one reference image or video.
- Fixed Seedance audio/video reference UI details: uploading media cannot open preview, completed input cards insert `@文件名`, sent prompts render `@文件名` in blue with an audio/video icon, and only the icon opens preview playback.
- Fixed replay/retry media-reference loss. `video_5_d24` was diagnosed as having saved `uploadedFiles` for `abbbbbb.mp4` and `demo_chinese.mp3`, but the actual BytePlus request only had one `reference_image` because replay/retry restored only image references. New replay/retry requests now restore `referenceVideos` and `referenceAudios`; old generated videos cannot be retroactively changed.
- BytePlus human-reference review notices now show once per video request, not once per whole conversation. Each new image auto-review should add the blue system notice `系统检测到真人图片，需要审核才能生成视频，此次视频生成任务会延长时间，请稍候....`.
- Admin system settings upload-rule table now documents the completed BytePlus Seedance video/audio reference rules in the video/audio columns and no longer marks them as unfinished.
- Admin credits user list now sorts by latest credit-ledger change time descending, not consumed-credit amount.
- Admin system settings upload-rule table now also documents image-reference rules for BytePlus image models and Seedance 2.0 video models. Image reference display rules include formats, count, single-image size, Seedance video reference modes, and the current lack of separate image width/height constraints.
- Active memo task tracking was added in `handover/06-memo-tasks.md`; future deferred tasks should be added there with checkbox IDs, temporary reason, and later action.
- Asset category persistence was hardened. `/api/media-assets` `POST` preserves existing user-recategorized or locked categories; explicit `PATCH` is still used for moves. This prevents assets moved from conversation flow into `角色图片` or `场景图片` from later being reset to `conversation_images`.
- Asset category loading and pagination were stabilized. Asset pages fetch 30 items per page; scrolling loads more. The previous loop that repeatedly reloaded page 1 when server count exceeded loaded count was fixed. Bottom `正在加载中...` is only for user-triggered bottom loading.
- Asset sidebar group header totals were removed for `对话流资产` and `回收资产30天删除`; individual category counts remain.
- Backfilled six missing upload-image references for `312876953@qq.com` / `ID_686996` into `MediaAsset + UserAssetState`. Final verified `WorkspaceMessage` media gap for that account is `0`; all backfilled rows use `sourcePrompt="上传图片"` and `promptSource="upload"` while preserving old current position/deleted state.
- Image upload failures for two small unusual JPEGs were diagnosed. The files were `1280x720` and under 1MB, but had unusual `Lavc61.3.100`/MJPEG-style encoding. Server-side ffmpeg re-encoding succeeds.
- Image upload handling now falls back if browser canvas conversion fails or times out after 5 seconds, and server upload saving always re-encodes uploaded images through ffmpeg into standard JPG.
- Final root cause of the reported upload failure was cross-origin temporary upload from `ali.venusface.com` to `api.venusface.com`: browser XHR errored after `/api/upload-token` before image POST reached the server. Temporary image upload now uses same-origin `/api/asset-upload-temp` for POST/PATCH/DELETE instead of `NEXT_PUBLIC_UPLOAD_BASE_URL`.
- Upload failure cards show only generic `上传失败` to users. Detailed client-side upload diagnostics are still logged through `/api/client-error` with `source="client-diagnostic"`.
- Image temporary upload was adjusted after the 95% progress UX concern. Normal same-origin image upload now uses a fast server write path instead of always ffmpeg re-encoding every image. Failed image cards in both the workspace input and asset-library upload dialog show a blue `重试` action; retry sends `forceReencode=1` to `/api/asset-upload-temp` and uses server ffmpeg re-encoding as the fallback.
- The upload retry/re-encode fallback change was deployed to Malaysia production with backup `.deploy-backups/20260622-upload-retry-reencode`. Production build passed with only existing Turbopack/NFT warnings; PM2 stayed online; `/workspace`, `/admin`, and `/api/model-availability` returned 200.
- Follow-up fixed the remaining old behavior: frontend image selection no longer pre-converts every uploaded image through canvas before temporary upload. JPEG first attempts now upload the original file to the fast server path; failed-card retry is the re-encode fallback. The upload progress overlay shows `处理中` instead of a stuck `95%` while waiting for the server response.
- The upload original-first follow-up was deployed with backup `.deploy-backups/20260622-upload-original-first-followup`. Production build passed with only existing Turbopack/NFT warnings; PM2 stayed online; `/workspace`, `/admin`, and `/api/model-availability` returned 200.
- Final upload retry correction: historical docs showed the old flow pre-converted uploads through canvas, so the new target flow is intentionally different. First upload now keeps the original file and the server fast path rejects JPEGs that are not common 4:2:0 sampling; the two desktop `aaa` JPEGs match this rejection path. Failed cards expose blue `重试`; retry sends `forceReencode=1` and ffmpeg re-encodes. Progress display is numeric again. Deployed with backup `.deploy-backups/20260622-upload-first-fail-retry-reencode`; production build passed and endpoint checks returned 200.
- Diagnosed `video_1_d27` for `ID_636611`: workspace message and files were valid, local mp4 and poster existed, and `https://main.venusface.com/generated/...mp4` returned 200. Static domains `static.venusface.com` and `ali.venusface.com` failed HTTPS checks, so frontend static-base rewriting could make generated videos invisible on the main workspace. `getStaticMediaUrl()` now disables static-base rewriting on `main.venusface.com`, `api.venusface.com`, and `101.47.19.109`, using same-origin `/generated/...` instead. Deployed with backup `.deploy-backups/20260622-main-generated-media-url`.
- Follow-up on temporary video display: `video_1_d27` and the next generated video both received BytePlus/TOS temporary mp4 URLs at poll success while media save status was still `pending`; local save completed later, which is when posters appeared. Temporary TOS URLs returned `206` for ranged GET but `403` for HEAD and had no poster. Frontend now uses `preload="auto"` for inline videos without a poster, so temporary no-poster videos should load the first frame instead of sitting as an empty metadata-only player. Deployed with backup `.deploy-backups/20260622-temp-video-preload-auto`.
- Temporary remote video playback was investigated and later confirmed to be a home-network/environment issue. The temporary `/api/media-proxy` experiment and direct debug artifacts were removed; remote temporary videos now use direct provider URLs again until local `/generated/...` replacement.
- Further investigation showed the old working path used direct provider temporary URLs, not a proxy. The proxy made Ali -> Malaysia -> TOS streaming slower and browser requests appeared as `200` full streams, while BytePlus mp4 files have the `moov` atom at the end. Remote temporary videos are now direct provider URLs again; only the temporary no-poster stage uses `muted autoPlay preload="auto"` so the browser can start loading/playing enough to show frames. Local saved videos with posters keep normal non-muted behavior. Deployed with backup `.deploy-backups/20260622-temp-video-direct-muted-autoplay`.
- Temporary video rendering was adjusted again after a standalone local HTML with the same direct provider URL played correctly. The key difference was project markup: `<video controls>` was nested inside an outer `<button>` and lacked full card sizing, while the standalone test had a normal full-width `<video>`. `InlineVideoResult` now uses an outer `div`; the poster state uses a button, and the actual video element is no longer nested inside a button and uses `h-full w-full`. Double-clicking the video opens preview. Deployed with backup `.deploy-backups/20260622-video-element-no-button-wrapper`.
- User then tested a local HTML modified to mimic the original project structure (`button > video`, hover play/pause, click preview behavior), and the temporary URL still played. Per user request, project `InlineVideoResult` was restored to the original `button > video`, `preload="metadata"`, no forced muted/autoplay behavior, and deployed with backup `.deploy-backups/20260622-video-card-restore-original`. For controlled testing, production `video_6_d27` in user `ID_636611` was replaced from local `/generated/users/ID_636611/videos/1782066178925-fe4138eb-99a4-4cec-a1d8-04abb16589fd.mp4` to the latest BytePlus temporary URL. Original message JSON backup: `.runtime/manual-fixes/1782067671244-video_6_d27-replace-url.json`.
- Diagnosed `312876953@qq.com` / `ID_686996` video request `23f5c910-dea5-4542-8dbb-79d7525e1eb2:video:0`: BytePlus task `cgt-20260621173327-b4t82` had succeeded and local mp4 `/generated/users/ID_686996/videos/1782035548330-deeaf7f6-f09f-4f9f-a0a6-378b30bd48e7.mp4` existed, but the workspace message was still pending until later autosave/recovery. The message and media table now show `video_19_d1`, `pendingVideoCount=0`, and `statusText="视频已生成完成"`.
- Hardened media completion persistence with the intended fast-display flow. Provider temporary image/video URLs may be shown immediately in chat, preview, and download controls. Temporary remote URLs must not become `MediaAsset.url/normalizedUrl`. After `.runtime/media-save-jobs.json` reports a saved local `/generated/...` URL, the frontend replaces chat media, preview source, download source, asset list items, and then persists the local URL into `MediaAsset + UserAssetState`. Workspace message saving no longer fails if media-asset sync hits an asset-only error.
- Remote-to-local media mappings are now also appended to `.runtime/media-url-map.md` on the server for troubleshooting. This runtime file may contain signed provider URLs and must stay outside Git/docs.
- Follow-up audit hardened the core media chain further. Saved local media is now persisted to `MediaAsset` even if UI replacement is delayed by preload/static sync issues. Workflow canvas nodes are included in remote URL polling and replacement so future workflow media uses the same temporary-URL-first then local-URL-persist flow. `/api/media-assets` accepts `workflowId` and `workflowNodeId` for future workflow assets.
- `07-remote-video-url-debug.md` is closed. The temporary remote video display issue was confirmed by the user to be a home-network/environment problem, not an application bug. Test artifacts and `/api/media-proxy` were removed; future similar reports should be treated as a fresh issue and reproduced before code changes.
- Admin `生成记录` right-side table sorting was changed and deployed. Rows now sort by latest model interaction per account using latest `CreditLedger.createdAt` where `direction="consume"`, covering image generation, video generation, Agent/chat, reverse prompt, prompt optimization, and recorded failed model requests. This avoids login, upload, workspace update, credit grant, and admin adjustment changing the generation-record order.
- Backend admin navigation changes are deployed: `上传规则` is now a standalone backend tab between `系统设置` and `服务器信息`; `服务器信息` is also a standalone tab, not part of system settings.
- `服务器信息` is deployed through `/admin/api/server-info` and `AdminServerInfoPanel`. It shows Ali vs Malaysia rows for hostname/IP, disk total/free in G with three decimals, current network speed, NIC bandwidth, default NIC, CPU cores/load, memory, uptime, and server time. It should still be checked with an authenticated admin browser session because local Windows/Node SSH could time out while production is the intended environment.
- `上传规则` split is deployed. The existing upload-rule table moved from `AdminSystemSettingsPanel` to `AdminUploadRulesPanel` without changing the displayed rule content.
- Workflow foundation code is deployed but production workflow entry remains disabled because `NEXT_PUBLIC_WORKFLOW_MODE_ENABLED` is unset/false. Workflow state now saves through `UserWorkspaceState.state.workflowItems` as a first-class workspace area similar to conversations. The summary workspace load now includes `workflowItems` and `nextWorkflowNumber`, fixing the bug where `/api/workspace-state?summary=1&panel=chat` could load an empty workflow list and later autosave over previously created workflows.
- Workflow list rules now mirror conversation basics: first load guarantees one untitled `新工作流`; clicking new while `新工作流` exists reuses it; when the untitled workflow first gets real canvas action it is renamed to `工作流_01`, then `工作流_02`, etc.; deleted numbers are not reused because `nextWorkflowNumber` is persisted; deleting the last workflow creates a new untitled workflow; workflow sidebar initially shows 10 items and loads 5 more at a time.
- Workflow assets were added to the asset sidebar under `对话流资产` as a new `工作流资产` group with `生成图片` and `生成视频`. Frontend asset filtering now distinguishes `librarySource="workflow"` from conversation assets. Workflow generated local media is persisted to `/api/media-assets` as `workflow_images` / `workflow_videos` with `workflowId` and `workflowNodeId`; remote-to-local replacement polling also covers workflow nodes.
- Workflow canvas now has first real text/image/video node UI and generation behavior. Text nodes call `/api/chat` in Agent mode, image nodes call `/api/image`, and video nodes call `/api/video` create + poll. Nodes have display cards, waiting cards, failed cards, selected-state blue 1px card border, left/right `+` connection buttons, and a separate 680px input box visually matching the conversation input. Workflow image/video model menus use the same enabled model lists as conversation mode, so backend model availability switches affect both modes.
- Workflow usage display was added at the top-right of the workflow workbench using the same `UsageSummaryButton` concept as conversation mode. Workflow usage accumulates from node text/image/video calls, and media counts are computed from workflow node outputs.
- Login/session idle behavior is deployed. The normal user session idle timeout changed from 30 days to 1 hour. Ordinary auth checks no longer extend expiry. New `POST /api/auth/activity` extends the session only on real user activity events such as click, keydown, wheel, touch, or scroll. While any generation is running, the workspace sends keepalive every 5 minutes so long image/video waits do not log users out. Closing the browser still lets the idle timer expire after 1 hour.
- Server-info backend tab was polished after deploy: production now reads Malaysia locally and Ali through the server key, hard-disk rows show blue usage bars, and entering the tab automatically loads data once while keeping the refresh button.
- Conversation history sidebar count was fixed. The small number beside `历史对话` now uses the server total count, including conversations not loaded in the sidebar page yet.
- `07-remote-video-url-debug.md` is closed as a confirmed home-network/environment issue. All debug/test code, test pages, test messages, legacy test residue, and `/api/media-proxy` were removed. Remote temporary videos now play directly from provider URLs until local `/generated/...` replacement.
- User requested a GitHub sync after this work; memo `M004 Commit Deployed Local Changes` was marked completed and this handover was updated for the next AI. Main deployed-change commit pushed to GitHub: `17806ea Sync deployed media and workflow updates`.

## Latest 2026-06-23 Deploy

- User requested deploying all current local changes while keeping online workflow mode disabled.
- Local `npx tsc --noEmit` passed.
- Production source backup was created at `/var/www/flashmuse/.deploy-backups/20260623-full-local-deploy/source-before-deploy.tgz`.
- Deployed full local code to Malaysia with `/usr/local/bin/deploy-flashmuse-production.sh`. Build passed with only existing Turbopack/NFT broad-file-pattern warnings; PM2 `flashmuse` stayed online; Ali `_next/static` was synced and cache cleared.
- Verified `NEXT_PUBLIC_WORKFLOW_MODE_ENABLED` was disabled/unset on production, so workflow code is deployed but the user-facing workflow entry remains disabled.
- Verified 200: `https://main.venusface.com/workspace`, `https://main.venusface.com/admin`, `https://main.venusface.com/admin?tab=upload-rules`, `https://main.venusface.com/admin?tab=server`, and `https://api.venusface.com/api/model-availability`.
- Follow-up fixed `服务器信息`: production now reads Malaysia locally instead of SSHing to itself with the local Windows key path; Ali still reads through `/root/.ssh/flashmuse_to_ali_ed25519`. Redeployed successfully and confirmed direct Malaysia -> Ali SSH works.

## Verified During This Rebuild

- Malaysia server project path: `/var/www/flashmuse`.
- Malaysia PM2 process `flashmuse` was online.
- Production endpoints returned 200: `https://main.venusface.com/workspace`, `https://api.venusface.com/api/model-availability`, `https://main.venusface.com/admin`.
- Ali server static root exists at `/var/www/flashmuse-static` and Nginx is active.
- Ali local SNI test for `static.venusface.com` and `ali.venusface.com` returned 200 from the Ali server itself.
- Public/local-machine access to `http://static.venusface.com/...` and `http://ali.venusface.com/...` returned 403, and HTTPS returned curl code 000. This needs later review if static domains are expected to be directly public.
- Latest repeated production deploys on 2026-06-21 were frontend/backend fixes. They used `/usr/local/bin/deploy-flashmuse-production.sh`, builds passed with only the existing Turbopack/NFT warning, PM2 stayed online, and Ali `_next/static` was synced.
- Latest deployment backups from this conversation include `.deploy-backups/20260621053806-seedance-av-upload`, `20260621055453-media-mention-preview-fix`, `20260621060108-media-duration-epsilon`, `20260621061058-media-input-mention-click`, `20260621061807-media-prompt-inline-render`, `20260621062457-assistant-uploaded-files-fix`, `20260621063426-byteplus-review-notice-once`, `20260621064500-review-notice-per-request`, `20260621065215-replay-media-references`, `20260621065646-admin-upload-rules-text`, `20260621065952-admin-upload-rules-columns`, `20260621-admin-credit-last-change-sort`, `20260621-admin-upload-rules-image-reference`, `20260621-asset-category-preserve`, `20260621-asset-sidebar-group-counts`, `20260621-asset-page-size-loading`, `20260621-asset-pagination-loop-fix`, `20260621-upload-image-fallback`, `20260621-upload-image-conversion-timeout`, `20260621-upload-diagnostics`, and `20260621-same-origin-image-upload`.
- Latest deployment backup added after video completion chain hardening: `.deploy-backups/20260621-video-completion-chain-fix`.
- Latest deployment backup after restoring the fast temporary-URL-first flow: `.deploy-backups/20260621-temp-url-first-media-chain`.
- Latest deployment backup after core media chain audit follow-up: `.deploy-backups/20260622-core-media-chain-audit-fix`.

## Git State At Rebuild

Modified files:

- `handover/00-README.md`
- `handover/03-progress-and-status.md`
- `handover/04-keys-and-integrations.md`
- `handover/05-chat-history-highlights.md`
- `handover/CHANGELOG.md`
- `src/app/admin/admin-users-panel.tsx`
- `src/app/admin/api/records/user-detail/route.ts`
- `src/app/admin/page.tsx`
- `src/app/api/media-save-status/route.ts`
- `src/app/api/video/route.ts`
- `src/app/api/workspace-state/route.ts`
- `src/components/chat-workbench.tsx`
- `src/lib/error-message.ts`
- `src/lib/workspace-sessions.ts`

Untracked paths:

- `src/app/api/media-assets/`
- `src/lib/media-assets.ts`

After this handover rebuild, the handover files themselves will also be changed by the archive/rewrite operation.

## Old Docs That Are Now Outdated

- Old notes saying the project has no real login, no credits, no admin, or no persistence are outdated.
- Old notes saying assets are only in `localStorage` are outdated.
- Old notes saying history/media split tables are future work are outdated.
- Old notes saying production is HTTP test-only are partly outdated. Main/API are HTTPS; Ali static domain public access still needs review.
- Old migration instructions that say other accounts still need migration are outdated. The 20 online media accounts were already migrated and audited.
- Old model lists and exact provider availability may be stale. Check current code and `/api/model-availability` before changing models.
